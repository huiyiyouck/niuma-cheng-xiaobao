# vX.Y 设计文档

## 文档状态
- 整体状态：待Review
- Review 轮次：R1
- 关联 PRD：

> 从此处插入 Review 计划、Review 状态和 Review 记录三个区块，内容见 `docs/templates/review-plan.md`

## 1. 概述

## 2. 数据模型

> 每个数据模型用字段表描述（结构化便于下游无歧义实现）；spike / 纯预研无数据模型可整节标 `N/A` 并注明原因。

### <模型名>

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
|  |  |  |  |

## 3. API / 接口契约

> 接口契约是隔离会话之间的唯一交接带宽（下游实现会话及其并行子 Agent 只读此处，问不到上游），须逐项列清、可被无歧义实现。无接口变更可整节标 `N/A` 并注明。

### <接口名>

| 方法 | 路径 | 入参（类型） | 出参（类型） | 错误码 |
|------|------|--------------|--------------|--------|
|  |  |  |  |  |

## 4. 核心流程

## 5. 前端或交互影响

## 6. 迁移和兼容性

## 7. 风险和取舍

## Review 记录
