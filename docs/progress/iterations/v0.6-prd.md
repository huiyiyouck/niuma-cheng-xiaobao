# v0.6 PRD

## 文档状态
- 整体状态：Review中
- Review 轮次：R1
- 关联迭代：v0.6

## Review 计划
- Review 模式：动态 Review
- 影响领域：
  - 用户流程、页面、交互、视觉状态：基于 `/root/news-aggregation-platform` Figma 导出原型重构现有 Vue 前端。
  - 架构、数据流、接口、技术选型：新增 X 信息 L0/L1 处理链路、库内检索、外部搜索、链接读取、评分和重试状态。
  - 实现复杂度、工程成本、兼容性：前端重构、Worker 异步处理、LLM 输出结构化、失败重试、空间图标上传。
  - 验收标准、边界条件、回归风险：L0 skip 边界、L1 四段式展示、搜索失败、AI 失败、重试、前端真实数据联调。
  - 部署方式、环境变量、云服务、发布风险：外部搜索、链接读取、图片上传存储、LLM 调用成本和速率限制。
- 本轮指定 Review 方：
  - UI（界面设计师）：Review Figma 原型落地范围、新闻卡片、抽屉四段式展示、页面信息层级和空间图标上传体验。
  - Architect（架构师）：Review L0/L1 数据模型、Worker 链路、库内检索、搜索/链接读取边界、图片存储和评分计算方式。
  - Developer（开发工程师）：Review 前后端契约、实现成本、现有 Vue 组件迁移、AI JSON 输出和重试工程方案。
  - Tester（测试工程师）：Review L0/L1 状态机、失败重试、搜索降级、卡片/抽屉展示和 UI 重构验收可测性。
  - DevOps（运维/部署工程师）：Review 外部搜索/链接读取/LLM 调用的环境依赖、成本、超时、部署与回滚风险。
- 未指定角色及原因：
  - 无。本次变更同时覆盖 UI、架构、实现、测试和部署风险，五方均需 Review。
- 少于 2 个 Review 方时的用户确认：不适用
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| UI | Figma 原型落地、新闻流、抽屉、管理页、空间图标上传体验 | 需修改 | R1 |
| Architect | L0/L1 数据模型、检索/搜索/链接读取、评分、重试、图片存储 | 需修改 | R1 |
| Developer | 前后端契约、Vue 重构、Worker/AI 链路、重试实现成本 | 需修改 | R1 |
| Tester | L0/L1 状态机、失败降级、展示规则、全页面联调验收 | 待 Review | R1 |
| DevOps | 外部依赖、LLM/搜索成本、超时、上传存储、部署风险 | 待 Review | R1 |

## 1. 迭代目标

v0.6 聚焦三件事：

1. 基于 `/root/news-aggregation-platform` Figma 导出原型重构现有 Vue 前端，并接入真实后端数据。
2. 围绕 X Source 建立 L0/L1 分层处理链路，把原始推文加工为可读、可理解、可展示的高价值新闻。
3. 支持空间图标上传，使空间入口具备更清晰的视觉识别能力。

一句话目标：

> 基于新 UI 原型重构信息消费界面，并围绕 X Source 建立 L0/L1 分层处理链路，让平台能够自动过滤明显无效推文，将有价值推文翻译、补全、分析、打标签并按时间流展示给用户。

## 2. 用户故事

### 2.1 高价值信息消费

作为 Owner，我想在新闻流中按时间顺序看到已经处理好的 X 高价值信息，以便快速浏览最新值得关注的内容。

验收要点：
- 新闻流展示 L1 处理完成的信息。
- 默认按原始发布时间倒序排列。
- 每条卡片展示标题、来源、发布时间、综合价值分、摘要和标签。
- 综合价值分用于辅助阅读优先级，不决定是否展示。

### 2.2 L1 四段式理解

作为 Owner，我想点击新闻卡片后在侧边抽屉中看到完整处理结果，以便理解原始推文到底在说什么、为什么重要。

验收要点：
- 点击新闻卡片打开右侧抽屉。
- 抽屉完整展示四段内容：
  1. 原文中文翻译
  2. 相关背景与补全
  3. AI 分析与评价
  4. AI 标签
- 本期不做独立全屏详情页。
- 本期不做反馈按钮和反馈记录。

### 2.3 X 原始信息分层处理

作为 Owner，我想让所有抓取到的 X 原始信息先入库，再由系统进行 L0/L1 处理，以便既不丢原始数据，又能只展示处理完成的有价值内容。

验收要点：
- 所有抓取到的原始信息必须先入库。
- L0 只过滤明显无效内容，不做过度价值判断。
- L0 通过的信息进入 L1。
- L1 完成后进入新闻流展示。
- L0 skipped、L0/L1 failed、L1 processing 的信息不进入前台新闻流。

### 2.4 L1 背景补全

作为 Owner，我想让系统对 X 推文进行必要的背景补全，以便短推文、暗语、项目代号或链接内容能被理解。

验收要点：
- 每条 L1 信息都进行库内相关新闻检索。
- 推文包含链接时，系统尝试读取链接内容。
- 当 `needs_context=true` 时，系统可以触发 X 搜索和 Web 搜索。
- 本期不读取评论作为 L1 必需补全来源。
- 背景补全必须展示来源标签。

### 2.5 L1 价值评分

作为 Owner，我想看到每条 L1 信息的综合价值分和评分依据，以便判断阅读优先级。

验收要点：
- AI 输出四个维度分和理由：
  - 时效性
  - 影响力
  - 置信度
  - 可理解度
- 系统按权重计算综合价值分，不由 AI 直接拍脑袋输出。
- 初版权重：
  - 时效性：25%
  - 影响力：35%
  - 置信度：25%
  - 可理解度：15%
- 卡片展示 `1-10` 综合价值分。
- 抽屉中展示维度分和理由。

### 2.6 Figma 原型前端重构

作为 Owner，我想让当前 Vue 前端按 `/root/news-aggregation-platform` 原型重构，以便页面结构、信息密度和交互符合新的使用方式。

验收要点：
- 不直接迁入 React 原型。
- 在现有 Vue3 + TS + Vite 前端中实现。
- 页面结构和视觉参考 `/root/news-aggregation-platform`。
- 需要接真实后端 API，不保留 mock 数据作为正式路径。
- 覆盖页面：
  - 全局导航和布局
  - 新闻浏览页
  - 管理页
  - Source 详情页
  - 告警/日志监控页
  - 添加信息源抽屉
  - 编辑/删除弹窗
  - 统计卡片
  - 空间/频道筛选
  - 信息源库表格

### 2.7 空间图标上传

作为 Owner，我想为空间设置图片图标，以便不同空间在新闻浏览和管理界面中更容易识别。

验收要点：
- 空间支持上传图片图标。
- 保留现有 emoji 或文本图标能力。
- 支持替换、移除图片图标。
- 限制文件类型和大小。
- 上传失败时给出明确错误提示。
- 空间入口和管理页展示上传后的图标。

## 3. 验收标准

### 3.1 L0 入库和状态

AC-01：当系统抓取到任意 X 原始信息时，应该先保存原始记录，不因 L0 判断而物理丢弃。

AC-02：当原始信息进入 L0 时，系统应该记录 L0 处理状态、L0 标签、处理时间和失败原因（如有）。

AC-03：当 L0 判断信息为空文本、乱码、纯 emoji、纯转发无新增文本、明显重复、抽奖/招聘/无关广告、与 Source 主题明显无关或已处理过同 URL / 同 tweet id 时，应该标记为 skipped，不进入 L1。

AC-04：当 L0 通过时，系统应该将信息送入 L1，并保留 L0 标签，例如 normal_candidate、high_priority_candidate、needs_context_candidate。

### 3.2 L1 处理和重试

AC-05：当信息进入 L1 时，系统应该生成中文标题、摘要、原文中文翻译、背景补全、AI 分析评价、AI 标签、四维评分和综合价值分。

AC-06：当 L1 处理完成时，系统应该将该信息展示在新闻流中，并按原始发布时间倒序排序。

AC-07：当 L1 处理中发生临时错误（LLM 超时、模型服务异常、JSON 格式错误、搜索服务异常、链接读取超时、速率限制、临时网络错误）时，系统应该记录失败原因、重试次数和下次重试时间。

AC-08：当 L0/L1 处理失败且属于可重试错误时，系统应该自动重试最多 3 次，退避间隔为 1 分钟、5 分钟、15 分钟。

AC-09：当自动重试超过最大次数后，系统应该标记为最终失败，不进入新闻流。

AC-10：当 L1 失败信息后续重试成功时，系统应该进入新闻流，但排序仍按原始发布时间计算。

### 3.3 L1 补全来源

AC-11：每条 L1 信息都应该执行库内相关新闻检索，并将相关结果作为背景补全输入。

AC-12：当推文包含链接时，系统应该尝试读取链接内容；读取失败时记录失败原因，但不阻塞 L1。

AC-13：当 L1 判断 `needs_context=true` 时，系统应该可以触发 X 搜索和 Web 搜索；不是每条 L1 信息都必须外部搜索。

AC-14：当背景补全使用 Source 信息、链接内容、库内相关新闻、X 搜索或 Web 搜索时，抽屉应该展示对应来源标签。

AC-15：当补全内容包含推断或不确定信息时，系统应该在背景补全中区分已确认事实、合理推断和不确定点。

### 3.4 评分和标签

AC-16：当 L1 生成评分时，AI 应输出时效性、影响力、置信度、可理解度四个 `1-5` 维度分和理由。

AC-17：系统应该按 25% / 35% / 25% / 15% 权重计算 `1-10` 综合价值分。

AC-18：新闻卡片应该展示综合价值分，抽屉应该展示维度分和理由。

AC-19：AI 标签应该按分类输出，不混成一个大数组：
  - 领域标签
  - 实体标签
  - 事件标签
  - 内容类型
  - 处理标签

### 3.5 前端展示

AC-20：新闻流应该展示 L1 completed 信息，不展示 L0 skipped、L1 processing、L1 failed 信息。

AC-21：新闻卡片应该包含标题、来源、发布时间、综合价值分、摘要和标签。

AC-22：点击新闻卡片时，应该打开右侧抽屉，抽屉展示四段式 L1 内容。

AC-23：本期不应出现独立全屏详情页入口。

AC-24：本期不应出现反馈按钮、反馈记录或评论式交互。

AC-25：前端重构页面必须接真实后端 API，不得用 mock 数据作为正式展示路径。

### 3.6 空间图标上传

AC-26：空间编辑入口应该支持上传图片图标，同时保留 emoji 或文本图标能力。

AC-27：当上传的文件类型或大小不符合限制时，系统应该给出明确错误提示。

AC-28：当空间图标上传、替换或移除成功后，新闻浏览页空间入口和管理页应该展示最新图标。

## 4. 范围边界

### 4.1 本迭代做

- 基于 `/root/news-aggregation-platform` 原型重构现有 Vue 前端对应页面。
- 接入真实后端 API，清理正式路径中的 mock 数据。
- X 信息 L0/L1 分层处理。
- 所有原始信息入库。
- L0 状态、L0 标签、L1 状态、L1 标签、失败原因、重试次数、下次重试时间。
- L1 四段式内容生成和抽屉展示。
- 库内相关新闻检索。
- 链接读取。
- 按 `needs_context` 触发 X 搜索和 Web 搜索。
- 四维评分和综合价值分计算。
- 五类 AI 标签。
- 空间图标上传、替换、移除。

### 4.2 本迭代不做

- 不做 L2 事件追踪。
- 不做时间线分析。
- 不做深度搜索和深度分析工作流。
- 不做公众号文章或系列文章生成。
- 不做反馈按钮、反馈记录或评论能力。
- 不做独立全屏新闻详情页。
- 不读取评论作为 L1 必需补全来源。
- 不把 React 原型直接迁入生产代码。
- 不做前端可配置的信息处理工作流。
- 不在前端展示分流策略。
- 不提供处理工作流配置接口。
- 不做前端 Source 级代理配置。
- 不做自定义代理池、自动代理切换或代理健康检查平台。
- 不做完整领域标签管理后台；L1 AI 标签按本迭代输出结构生成和展示。
- 不对所有历史新闻强制重算。

### 4.3 Source 信息使用边界

L1 处理时，Source 的账号身份、领域标签、备注和关注方向可以作为 AI 理解原始推文的上下文输入，用于辅助判断内容含义、相关性和价值。

Source 信息不得机械决定评分结果：
- 核心账号不等于自动高分。
- 官方账号不等于自动置信度 5。
- 非核心账号不等于自动低分。

最终评分必须基于推文本身、补全结果、来源证据和 AI 理由共同生成。

## 5. 前置依赖

- `/root/news-aggregation-platform` 原型项目可作为 UI 输入材料读取。
- 现有 Vue 前端继续作为生产前端基础。
- X Source 已有抓取、Stream 或补偿抓取能力可复用。
- 需要可用 LLM 服务。
- 需要确定 Web 搜索和 X 搜索的可用实现方式、成本、速率限制和失败降级。
- 需要 Architect 设计 L0/L1 数据模型、状态机、重试机制和图片存储方案。
- 需要 DevOps 确认外部搜索、链接读取、图片上传存储和 LLM 调用在部署环境中的网络与成本约束。

## 6. 风险和开放问题

### 6.1 风险

- L1 链路依赖 LLM、搜索和链接读取，失败面比 v0.5 更大。
- 外部搜索和链接读取可能带来成本、延迟、速率限制和不稳定性。
- AI JSON 输出可能不稳定，需要强校验和重试。
- 库内相关新闻检索如果设计过重，可能拖慢 L1 处理。
- Figma 原型是 React 项目，迁移到 Vue 时需要避免机械照搬组件结构。
- 空间图标上传涉及文件存储、访问路径、缓存和部署回滚，需要设计清楚。

### 6.2 开放问题

- Web 搜索与 X 搜索具体使用哪个服务或接口，由 Architect / Developer / DevOps 在设计阶段确认。
- 库内相关新闻检索的匹配算法、召回数量和排序规则由 Architect 在设计阶段确认。
- 图片上传存储位置（本地静态目录、数据库引用、对象存储或其他方案）由 Architect / DevOps 在设计阶段确认。
- L1 处理失败后的手动重试入口本期是否需要页面化，由设计阶段结合实现成本确认；PRD 只要求后端具备状态和自动重试能力。

## Review 记录

### Architect R1（2026-06-09）

**结论**：❌ 需修改。共 11 条意见（3 高 / 5 中 / 3 低）。

整体评价：v0.6 三条主线方向正确。前端重构沿用现有 Vue3 栈不直接迁 React 原型的判断稳妥（§4.2/§2.6/§6.1）；L0/L1 分层和"原始信息先入库"的原则与现有 `raw_items / processed_news / tasks` 架构方向一致，可在 v0.5 基线上做加法实现。但本轮 PRD 在**状态机定义、外部依赖收敛、评分数据形态、补全来源标签结构、空间图标上传策略**这五块仍有结构性缺口，进入设计阶段前必须修正，否则 Architect 阶段会被迫越界做产品决策。

#### 高严重度

**#A1（高）L0/L1 状态机未在 PRD 定稿，AC-07/AC-08/AC-09 不可测、不可设计**

- 关联：AC-03、AC-04、AC-07、AC-08、AC-09、AC-10、AC-20、§3.2 整段
- 问题：PRD 中散落出现多种状态词："L0 skipped/L0 通过"、"L0/L1 failed"、"L1 processing/completed"、"最终失败"、"L1 失败信息后续重试成功"。但缺少一张明确的状态枚举表 + 合法转移图：
  - L0 是否有 `pending` / `running` / `skipped` / `passed` / `failed` 五态，还是只有终态？
  - L1 是否有 `pending` / `running` / `completed` / `failed` / `permanently_failed`？
  - "最终失败"（AC-09）与"L1 failed"（AC-07）是不同状态还是同一终态？
  - AC-10「失败后重试成功进入新闻流」意味着允许从 `failed` → `completed` 的回流转移，需明确这个回流路径以及是否清空之前的 `last_error`。
  - L0/L1 是落在同一个状态字段还是两个状态字段？建议两个（分层独立），但 PRD 没说。
- 风险：架构师无法画状态机，开发不知道枚举值，测试不知道断言什么，DevOps 不知道告警挂在哪一态。
- 修改建议：PRD §3.2 新增一节明确：
  1. L0 状态枚举 + L1 状态枚举（各列举 4-5 个值 + 一句话语义）。
  2. 合法转移图（含 AC-10 回流路径）。
  3. 终态定义清单（哪些状态属于"不再处理"）。

**#A2（高）"可重试错误"和"不可重试错误"未划分清单，AC-07/AC-08 落不到代码**

- 关联：AC-07、AC-08、AC-09
- 问题：AC-07 列举了 7 种"临时错误"，但没说反面——什么算"永久错误"？例如：
  - LLM 返回 JSON 格式错误：算可重试还是不可重试？AC-07 把它列入临时错误，但实际上同一份输入再调一次大概率仍是同样错误（除非加随机种子或换 prompt）。
  - 链接读取 404：算"链接读取超时"吗？404 是永久错误。
  - LLM 速率限制：可重试，但退避策略应区别于网络超时。
  - 搜索服务认证失败 401：不可重试，但 AC-07 笼统写"搜索服务异常"。
- 风险：开发会自己拍脑袋分类，测试无法对齐验收，最终行为不可预期。AC-08「3 次 / 1min / 5min / 15min」退避对所有错误一刀切，对 429 限速可能过激（触发更严重限速），对 LLM JSON 错误可能浪费 token。
- 修改建议：PRD §3.2 给出两张表：
  1. **可重试错误清单**：网络超时 / 5xx / 429 / 连接重置 / LLM 服务 503 等，每条标注是否需要特殊退避（如 429 用 `Retry-After`）。
  2. **不可重试错误清单**：4xx 非 429 / LLM 解析错误（建议归入此类，由 prompt 自身问题排查）/ 链接 SSRF 拒绝 / 内容超长等。

**#A3（高）四类外部依赖（LLM / X 搜索 / Web 搜索 / 链接读取）的"具体使用什么"全部推迟到设计阶段，PRD 无法定稿**

- 关联：§5 前置依赖、§6.2 开放问题第 1/3 条、AC-12、AC-13
- 问题：PRD §6.2 把"Web 搜索、X 搜索具体用哪个服务"、"图片存储用什么方案"都标为"开放问题，由 Architect / Developer / DevOps 在设计阶段确认"。这违反基线规则：**PRD 阶段必须把前置依赖收敛清楚，设计阶段只做实现选型，不做范围决策**。当前写法实质上要架构师代 PM 做产品决策：
  - 是用 X 官方 Recent Search API（需 Basic 套餐起步）还是抓取式搜索？这两者成本差 1-2 个数量级。
  - 是用 Tavily / Serper / Google Custom Search 还是自建 SearXNG？这两者部署模式完全不同（外部 SaaS vs 自托管）。
  - 是直接 fetch + cheerio 还是用 Jina Reader / Firecrawl？前者免费但容易触发反爬，后者收费但稳定。
- 风险：架构师设计阶段做选型，会被开发/DevOps 推回"这是产品决策"；多走一轮 Review。
- 修改建议：PRD 应至少在 §5 前置依赖给出**候选清单 + 倾向选择 + 选择不确定时的兜底策略**。例如：
  - LLM：复用现有 OpenRouter / Claude（明确是哪个 endpoint）。
  - Web 搜索：候选 A、B、C，倾向 X；如 X 不可用降级为 Y。
  - X 搜索：候选 A、B，倾向 X。
  - 链接读取：候选 A、B，倾向 X；本期是否做 SSRF 防护。
  - 不要求一次锁定，但必须把候选范围 + 决策维度（成本/速率/部署复杂度）写明。

#### 中严重度

**#A4（中）评分四维输出口径与综合分公式潜在矛盾**

- 关联：AC-16、AC-17
- 问题：AC-16 要求 AI 输出 4 个 `1-5` 维度分；AC-17 要求按 `25%/35%/25%/15%` 权重计算 `1-10` 综合分。但：
  - 4 个 1-5 分加权计算后最大值是 `1×5×100%=5`，怎么映射到 `1-10`？是 `(weighted)/5 × 10` 还是 `weighted × 2`？
  - 这两个公式在边界值不一样：当所有维度分=1 时，前者=2 后者=2；当所有维度分=5 时，前者=10 后者=10；线性映射时一致。但 PRD 未明确，开发会拍脑袋。
  - 综合价值分是否四舍五入到整数？前端 AC-21 说"展示综合价值分"但未说精度。
- 修改建议：明确写出公式（如 `综合分 = round((d1×0.25 + d2×0.35 + d3×0.25 + d4×0.15) × 2, 1)`，结果保留 1 位小数 `1.0-10.0`），以及边界保护（如 AI 返回非数字时的默认值）。

**#A5（中）AC-19 五类标签的"标签来源、可枚举性、归一化"未定义**

- 关联：AC-19、§2.4、AC-14
- 问题：AC-19 要求 5 类标签分别输出但没说：
  - 是否有枚举闭集？例如"内容类型"是 `news/opinion/data/announcement` 这类闭集，还是 AI 自由生成？
  - "处理标签"是什么？是 L0 标签（`normal_candidate / high_priority_candidate / needs_context_candidate`）的延续，还是独立的 L1 处理结果标签？AC-04 用了同一词"标签"指 L0 输出，容易混淆。
  - "领域标签"和 INDEX 跨任务待办里 v0.5 的"领域标签管理后台"是否同一套？PRD §4.2「不做完整领域标签管理后台；L1 AI 标签按本迭代输出结构生成和展示」暗示本期 AI 自由生成，但前端如何对未知标签做颜色/分组？
- 修改建议：PRD §3.4 加一段定义五类标签：
  - 是闭集还是开集；
  - 与 L0 标签的关系；
  - 与现有 `sources.domain_tags / content_topics / source_role` 字段的关系（是否共享、是否互推、是否冲突时谁优先）。

**#A6（中）背景补全的"来源标签"展示规则与底层数据结构脱节**

- 关联：AC-14、AC-15、§2.4
- 问题：AC-14 列举 5 种补全来源（Source 信息 / 链接内容 / 库内相关新闻 / X 搜索 / Web 搜索），要求"抽屉展示对应来源标签"。但：
  - 是按段落标注（每句话挂一个来源）还是按章节标注（背景补全段落标一个混合来源列表）？前者要求 LLM 输出结构化引用（类似 RAG citation），实现成本极高；后者实现简单但用户无法精确追溯。
  - AC-15 又要求"区分已确认事实、合理推断、不确定点"，这要求 LLM 自分类置信度。和来源标签是正交两套元数据，还是合并成一个？
- 修改建议：PRD §3.3 明确：
  - 来源标签的粒度（句子级 / 段落级 / 章节级），优先推荐章节级以降低 LLM 输出结构复杂度。
  - AC-15 的"事实/推断/不确定"标签是用哪种载体表达（图标 / 文字前缀 / 段落标签），与 AC-14 来源标签共存的呈现方式。

**#A7（中）"库内相关新闻检索"算法策略未定义**

- 关联：AC-11、§6.2 开放问题第 2 条
- 问题：AC-11 强制"每条 L1 都执行库内检索"，但检索方式（关键词 ILIKE / pgvector 向量 / Elasticsearch 全文 / OpenAI embedding）会带来**架构层差异巨大**的方案：
  - 向量检索需要新增表存 embedding + 选 embedding 模型 + 决定何时更新。
  - 全文检索需要新建 `tsvector` 列 + GIN 索引或引入新服务。
  - ILIKE 简单但召回质量低，且 PG 性能不行。
- 这同样不是"设计阶段实现细节"，而是会决定**是否新增基础设施**和**是否额外烧 LLM token 做 embedding**。
- 修改建议：PRD 在 §5 前置依赖给出候选清单 + 倾向方案；如真要留到设计阶段，至少把"是否引入新基础设施（embedding 服务 / 全文索引）"的决策标准写明（如：日处理量 X 条以下用 ILIKE，以上才上向量）。

**#A8（中）空间图标上传的存储位置、文件名规范、清理策略未在 PRD 收敛**

- 关联：AC-26、AC-27、AC-28、§6.2 开放问题第 3 条
- 问题：§6.2 把"图片上传存储位置（本地静态目录、数据库引用、对象存储或其他）"标为"由 Architect / DevOps 在设计阶段确认"。但这同样是产品/部署决策不是架构选型：
  - 本地静态目录 → 部署回滚时文件可能丢失（v0.5 已踩过部署架构坑，参考 [`full-stack-deploy-handbook.md`](../knowledge/devops/full-stack-deploy-handbook.md) 软链接模式）。
  - 数据库 bytea → 拖慢 SELECT。
  - 对象存储 → 新增成本/账号/部署密钥。
- AC-27 要求"文件类型和大小限制"，但 PRD 没给具体阈值（MIME 白名单 / 最大 KB / 分辨率上限）。
- AC-28 说"上传/替换/移除后展示最新图标"，没说替换/移除是否要清理旧文件（避免长期占用磁盘）。
- 修改建议：PRD §3.6 补充：
  - 限制：MIME 白名单（如 `image/png, image/jpeg, image/webp`）+ 最大文件大小（建议 ≤ 500KB）+ 最大分辨率（建议 256×256，因为图标场景）。
  - 存储位置候选 + 倾向方案 + 兜底；明确替换/移除是否要清理旧文件。

#### 低严重度

**#A9（低）AC-25「不得用 mock 数据作为正式展示路径」与开发期 mock 兼容性未说清**

- 关联：AC-25
- 问题：禁止"正式展示路径"用 mock，但开发期、Storybook、Tester 联调时是否允许？建议措辞改为"生产环境构建不得包含 mock 数据"或"路由/页面在 prod 模式下不应回退到 mock"，避免开发期被卡。

**#A10（低）AC-23「不应出现独立全屏详情页入口」与未来可扩展性的关系**

- 关联：AC-23、§4.2
- 问题：本期不做详情页 OK，但 PRD 没说**抽屉是否预留"扩展到详情页"的入口**。例如抽屉头部是否需要一个分享/收藏/展开按钮的空槽位，避免后续迭代时再改路由结构。建议补一句"本期不做，但抽屉结构需为后续详情页扩展预留扩展位"或明确"本期保持最简结构，未来再说"。

**#A11（低）§2.6 覆盖页面清单与现有路由表对齐缺失**

- 关联：§2.6
- 问题：列举了 10 个覆盖页面（"全局导航和布局 / 新闻浏览页 / 管理页 / Source 详情页 / 告警/日志监控页 / 添加信息源抽屉 / 编辑/删除弹窗 / 统计卡片 / 空间/频道筛选 / 信息源库表格"），但现有 Vue 前端实际路由是 `/news`、`/admin`、`/admin/logs`、`/admin/sources/:id`、`/admin/alerts` 等（命名可能有出入）。PRD 没说每个页面对应当前哪条路由，UI 阶段会反复对齐。建议 PM 在 §2.6 加一张映射表「原型页面 → 当前 Vue 路由 → 重构后路由」，便于 UI 阶段直接接入。

#### 不阻塞的观察

- v0.5 已落地 `tasks` 表带 `max_attempts / attempt / run_after / last_error`，本期 L0/L1 重试机制可直接复用，AC-07/AC-08 在数据层很大程度上已经支持，是好基线。
- `raw_items.content` 已是 jsonb，存 X 原始推文 + L0 标签兼容（建议设计阶段考虑是否新增 `l0_meta jsonb` 字段还是塞进 content）。
- `processed_news.tags / entities / source_refs` 已是 jsonb 三件套，AC-19 五类标签和 AC-14 来源标签都能在不改表的情况下落（但建议设计阶段评估是否拆出 `processed_news_l1_meta` 单表以避免 jsonb 越塞越大）。
- ADR-001（drizzle generate + journal）已落地，本期任何 schema 变更必须走 `db:generate` 增量迁移、不能复制粘贴 baseline。

#### 总结

- 阻断项（#A1-#A3）属于"PRD 阶段必须收敛"的内容，设计阶段无法替 PM 决策。
- 中严重度（#A4-#A8）属于"PRD 应给方向、设计阶段做选型"，目前完全甩给设计阶段会导致 Architect / DevOps 在设计阶段被迫做产品决策。
- 低严重度（#A9-#A11）是表述完善和未来扩展位预留。
- 三条主线技术可行性 OK，没有架构红线问题；现有 v0.5 schema 和 worker 框架可作为良好基线。
- 等待 PM 汇总全部 5 方 R1 Review 后提交 R2；届时 Architect 复审。

### UI R1（2026-06-09）

**结论**：❌ 需修改。共 12 条意见（4 高 / 5 中 / 3 低）。

整体评价：v0.6 把"Figma 原型重构"和"L0/L1 链路前端展示"绑在一个 PRD 里，方向 OK 也确实必要。但本轮 PRD 直接把 `/root/news-aggregation-platform` 当成"成品 UI 蓝本"看待，而原型实际上存在**关键交付物缺画、与现有 Vue 路由结构不一致、与 PRD 用户故事冲突**的硬伤——这些是产品级 UI 决策，不能甩到 UI 方案阶段才发现。Architect 已经在 #A1-#A8 把数据/状态/外部依赖侧捋了一遍；UI 这边重点是**前端"看得到的东西"应该长成什么样**：抽屉 vs 常驻面板、5 类标签的视觉层级、4 维评分的展示密度、空间图标上传交互（原型完全没画）、L0/L1 处理状态前台是否可见。这些 PM 不在 PRD 阶段收敛，UI 方案阶段会被迫越界做产品决策（与 #A3 同构问题）。

#### 高严重度

**#U1（高）"原型新闻详情=常驻右侧分栏" 与 AC-22「点击卡片打开右侧抽屉」直接冲突，PRD 未做选择**

- 关联：AC-22、§2.2、§2.6
- 问题：`/root/news-aggregation-platform/src/app/pages/NewsPage.tsx:87,202-272` 把详情区做成了**常驻右侧分栏**（`detailPanelOpen` 写死 true、宽度 `w-96`、左右并列），而 PRD AC-22 写"点击新闻卡片时打开右侧抽屉"。这两种是完全不同的交互模型：
  - 常驻面板：列表+详情同屏、详情默认空态、占用宽度永久减一栏、不适合移动端。
  - 抽屉（Drawer）：默认隐藏、点击卡片覆盖出现、关闭后列表恢复满宽、与 v0.5 `SlidePanel` 一致。
- 同时原型详情区只画了占位（"选择一条新闻查看详细信息"），AC-22 要求的「四段式：原文中文翻译 / 背景补全 / AI 分析 / AI 标签」一段都没出现在原型里——也就是说 v0.6 的核心交付物在原型中**完全没设计**。
- 风险：UI 方案阶段会面临"按原型抄"和"按 PRD 抄"二选一；如果走原型，AC-22 就要改；如果走 PRD，原型整套布局（4 列统计卡 + 列表 + 常驻面板）的宽度分配要重算（v0.5 现状是无右栏满宽 + SlidePanel 覆盖式抽屉）。
- 修改建议：PRD §2.2 / AC-22 二选一并写明：
  - 方案 A（推荐）：维持"点击卡片打开覆盖式抽屉"，明确写"不采用原型的常驻右侧详情面板，原型该区域作废，重构时不照搬"。继承 v0.5 `SlidePanel` 体系。
  - 方案 B：改为"列表+常驻右侧详情面板"，并补充无选中态、列表占比、移动端降级方案。
- 不论选哪一个，都需要在 §2.6 覆盖页面清单中把"原型未画但 PRD 要求的四段式抽屉"列为必须在 UI 方案阶段产出的页面元素，不能默认"原型已经画了"。

**#U2（高）现有 Vue 路由（5 路由）与原型路由（4 路由）不一致，PRD 未声明取舍**

- 关联：§2.6
- 问题：
  - 现有 Vue `frontend/src/router.ts` 路由：`/news` `/admin` `/logs` `/alerts` `/sources/:id` —— 5 条，alerts 和 logs 各一独立页。
  - 原型 `/root/news-aggregation-platform/src/app/routes.tsx` 路由：`/news` `/admin` `/monitoring` `/sources/:id` —— 4 条，`MonitoringPage` 内部用 Tab 合并告警/日志（见 `MonitoringPage.tsx:117,158-180`）。
  - 原型 `AlertsPage.tsx` 文件存在但**没有挂载到路由**（routes.tsx 里没引）——属于"原型作者自己也没决定要不要独立页"。
- PRD §2.6 模糊写"告警/日志监控页"，既可解读为"两页保留"也可解读为"合并为监控页"。这是路由级决策，会影响：
  - URL 兼容性（外链、书签、`/alerts` 是否 301 到 `/monitoring?tab=alerts`）
  - 顶部导航项（`App.vue` TopNav 现 4 项 vs 原型 3 项 + 监控带未处理告警角标）
  - 现有 `AlertsPage.vue / LogsPage.vue` 是否合并、合并后代码归属
- 风险：UI 方案阶段如果决定合并，会触发"为什么不在 PRD 说"的反复；如果决定不合并，原型的 Tab 结构和顶部导航全部要改。
- 修改建议：PRD §2.6 加一段"路由迁移决策"明确：
  - 是否合并 `/alerts` + `/logs` → `/monitoring`（推荐合并，与原型一致 + 顶部导航少一项）。
  - 顶部导航最终项数（3 项还是 4 项）以及"监控"是否带未处理告警角标（原型 `RootLayout.tsx:7,61-65` 已画）。
  - 旧 URL 是否做兼容跳转。

**#U3（高）原型未画"空间图标上传" UI，§2.7 三条 AC 在 UI 阶段无从对照**

- 关联：AC-26、AC-27、AC-28、§2.7
- 问题：PRD §2.7 是 v0.6 三大主线之一，但 `/root/news-aggregation-platform/src/app/components/admin/SpaceEditDialog.tsx:11-67` 完整看了一遍，**只支持 emoji 文本输入**（`<input value={icon}` 单行文本框），没有上传按钮、没有图片预览、没有替换/移除按钮、没有大小/类型提示。也就是说"空间图标上传"在原型里**完全没画**，与 #A8 同构（Architect 提出的存储/限制缺失是后端侧，UI 这边是前端交互形态缺失）。
- 同时 §2.7 AC-26 写"保留现有 emoji 或文本图标能力" + "支持替换、移除图片图标"，混合 emoji 和图片两种模式时的优先级、互斥关系、默认展示形态都需要 UI 决策：
  - 编辑表单是 emoji + 上传两块并存，还是用 Tab 切换两种模式？
  - 上传图片后空间卡片是显示图片还是 emoji？是否允许同时存在？
  - 移除图片后回退到什么（默认 emoji？空？）？
- 风险：UI 方案阶段会被迫做"是否新增 Tab 切换两种模式"这类产品决策，触发与 PM 反复。
- 修改建议：PRD §2.7 补充：
  - emoji 与图片图标的关系（互斥 / 并存 / 优先级），明确编辑表单的形态选项；
  - 列举必备交互：上传按钮位置、上传中态、预览尺寸、替换按钮、移除按钮、移除后回退方案；
  - 列举必备视觉态：空间卡片在"无图标 / emoji / 已上传图片 / 图片加载失败" 4 态下的展示；
  - 错误提示位置（行内 / Toast / Dialog 内嵌）。
- 不要求 PRD 出像素稿，但要 PRD 把"需要 UI 在 UI spec 中产出 X / Y / Z 这些状态"列清。

**#U4（高）4 维评分 + 5 类标签的"视觉层级"在 PRD 没收敛，UI 阶段会面临空间挤压抉择**

- 关联：AC-16、AC-17、AC-18、AC-19、AC-21、AC-22
- 问题：
  - AC-21 卡片要展示：标题、来源、发布时间、综合价值分、摘要、标签——但 AC-19 标签有 5 类（领域/实体/事件/内容类型/处理）；卡片是否 5 类全展示？还是只展示 1-2 类（如领域 + 实体），其余在抽屉展示？原型 NewsPage 卡片只有平铺的 `tags + entities`（`NewsPage.tsx:243-253`），没区分类。
  - AC-18 抽屉要展示"维度分和理由"——4 个维度 × `1-5` 分 × 各自理由文本。如果每条理由是 1-2 句，4 维理由会占抽屉相当宽的篇幅，与四段式（翻译/补全/分析/标签）并存时层级如何排？
  - 综合价值分 `1-10`（AC-17）在卡片以什么形态展示（数字徽章 / 进度条 / 星级 / 颜色块）？原型用"颜色徽章 + 数字"（`NewsPage.tsx:212-218`），但 ≥8 绿 / ≥6 蓝 / 其余灰这套阈值 PRD 没定。
  - 五类标签是否每类一个颜色？AC-19 没说，原型也只有两个颜色（secondary / accent）。
- 风险：UI 方案阶段会被迫做"卡片只显示综合分还是 4 维都显示"、"5 类标签卡片显示几类"这种产品级密度决策。
- 修改建议：PRD §3.5 加一段"信息密度规则"：
  - 卡片必须展示哪几项、可选哪几项、5 类标签卡片显示哪几类（推荐：领域 + 实体，事件/内容类型/处理放抽屉）；
  - 综合价值分的视觉形态优先级（推荐数字徽章 + 颜色映射），如做颜色映射则给出阈值；
  - 抽屉里 4 维分与四段式的排版优先级（哪个在上 / 是否折叠默认收起）；
  - 5 类标签是否需要分类颜色（推荐：5 色一一对应，由 UI 在 UI spec 中定）。

#### 中严重度

**#U5（中）"来源标签"展示形态与 AC-15「事实/推断/不确定」 的视觉关系未在 PRD 收敛**

- 关联：AC-14、AC-15、§2.4
- 问题：与 Architect #A6 同构但侧重点不同。Architect 关心"数据粒度"，UI 关心"视觉如何不打架"。AC-14 要求展示 5 种补全来源（Source 信息 / 链接内容 / 库内相关新闻 / X 搜索 / Web 搜索），AC-15 要求区分 3 类置信度（已确认事实 / 合理推断 / 不确定点）。两套元数据正交叠加在一段"背景补全"文本上，前端有几种通用呈现：
  - 5 类来源用 5 种小图标 + 3 类置信度用前缀文字（"✓ 已确认 / ? 推断"）
  - 来源在段落末尾用 footnote 角标，置信度用整段背景色
  - 来源放章节标题旁，置信度内嵌每句前缀
- 不同方案对 LLM 输出结构要求差异巨大（见 #A6），但视觉密度差异更大（5 图标 + 3 置信度 + 主体文字最少 8 个视觉信号同屏）。
- 修改建议：PRD §3.3 在 #A6 基础上补一句"前端展示推荐方案"，例如：来源用段落末尾 footnote 链式标签（章节级粒度），置信度用每段前的小徽章（已确认/推断/不确定 3 色），让 UI 方案阶段直接照该方案出图。

**#U6（中）AC-25「不得用 mock 数据」与"前端重构迁移期"的过渡形态未说清**

- 关联：AC-25、§2.6
- 问题：与 Architect #A9 同向，UI 这边更具体：原型 6 个页面**全部**用了 mock data（`NewsPage.tsx:45-79` mockNews、`SourceDetailPage.tsx:15-67` source/recentNews/placements、`MonitoringPage.tsx:31-77` mockLogs、`SpacesManagement.tsx` mockSpaces 等）。"接入真实后端 API"意味着重构期间每个页面都要先按 mock 跑通视觉、再换 API。AC-25 用"不得用 mock 作为正式展示路径"一刀切，缺少：
  - 开发期是否允许保留 mock fixture？
  - Tester 联调时空数据/异常态怎么造（这一点 v0.5 走的是"后台 DB 直造数据"路径，本期是否延续）？
  - 后端某些字段尚未实现时（如 4 维评分理由），前端如何降级（隐藏 / 占位 / Toast 提示）？
- 修改建议：AC-25 改写为"生产构建（prod build）的页面渲染数据必须来自真实 API；开发期 mock fixture 不受限但需在 PR 中删除"。同时在 §3.5 加一句"接口未就绪时降级规则"：字段缺失走"隐藏该元素"而非"显示 N/A"。

**#U7（中）L0/L1 处理状态前台是否可见，PRD 没决策（与 §6.2 第 4 项重叠但范围更大）**

- 关联：AC-20、AC-22、§6.2 第 4 项
- 问题：§6.2 第 4 项只说"L1 失败后的手动重试入口本期是否页面化"留到设计阶段。但从 UI 视角看影响面更大：
  - AC-20 要求新闻流"不展示 L0 skipped / L1 processing / L1 failed"——也就是说处理中和失败的内容**前台用户完全看不到**。
  - 那么 Owner 想看"这条推文为什么没出现 / 卡在哪一步 / 失败原因是什么"，本期是否需要一个入口？
  - 候选有：管理页加"处理队列" Tab / Source 详情页加"该源最近处理失败列表" / 监控页加"L0L1 失败" 维度 / 完全不暴露只看后端日志。
  - 原型完全没画 L0/L1 处理状态可视化（MonitoringPage 只有 fetch/worker 日志，没有 L0/L1 任务态）。
- 风险：如果本期一概不暴露，Owner 试用阶段必然反馈"为什么我抓到的推文没出现"，倒逼后续临时加页面。
- 修改建议：PRD §4.1 或 §4.2 明确"本期是否提供 L0/L1 处理状态可视化"：
  - 推荐方案：本期最小化——在 Source 详情页 §247 "最近一周新闻"基础上**新增**一个"最近 L0/L1 处理状态计数"区块（成功/skipped/失败 三个数 + 跳转监控页 Source 维度筛选），不做单条详情，不做手动重试入口；
  - 或明确不做、只在监控页用日志侧面承载。
- 不论选哪个，PRD 必须给出决策，不要甩给设计阶段。

**#U8（中）"信息源使用位置"与"添加信息源" 在原型/v0.5 间存在两套交互，PRD 未指定**

- 关联：§2.6 覆盖页面清单（添加信息源抽屉 / 编辑/删除弹窗）
- 问题：v0.5 是"信息源库表格 + 行内添加到位置"+"空间管理 Tab 行内添加 Source"两条路径并存（详见 `v0.5-ui-spec.md:64-89`）。原型保留了 `AddSourceDrawer.tsx`、`SourceEditDrawer.tsx`，但同时给 `SourceDetailPage` 增加了"展示位置 + 添加位置"右栏（`SourceDetailPage.tsx:299-335`），与"在空间管理页加源" 形成第三条路径。PRD §2.6 只列了"添加信息源抽屉、编辑/删除弹窗"，没说三条路径是否都保留、入口在哪。
- 风险：UI 方案阶段无法判断是否保留 v0.5 "信息源库表格添加按钮"、"空间管理频道下添加按钮"、"Source 详情添加位置"三个并行入口；可能误删某个入口导致 Owner 用不爽。
- 修改建议：PRD §2.6 加一句"信息源新增/绑定入口保留范围"，列举本期保留哪些入口（推荐三条全保留 + 各自用场景），UI 方案再细化每个入口的弹窗 / 抽屉差异。

**#U9（中）"统计卡片"的取值口径与 L0/L1 引入后的兼容性未说明**

- 关联：§2.6 覆盖页面清单（统计卡片）、AC-20
- 问题：原型 `NewsPage.tsx:11-16` 4 张统计卡：今日新增 / 总新闻 / 启用信息源 / 频道数。v0.5 同形态。但本期 L0/L1 后，统计口径要重新定义：
  - "今日新增"是"今日抓取（含 raw_items 全部）"还是"今日 L1 completed"？前者会因 L0 skipped 暴涨，后者更符合用户感知但实现要 join。
  - "总新闻"是"raw_items 累计"还是"processed_news L1 completed 累计"？v0.5.1 后实际能看到的是后者，但原型沿用旧字段。
  - 是否新增"今日 L0 skipped / L1 失败"两个口径让 Owner 直观感受 L0/L1 过滤效果？
- 修改建议：PRD §3.5 或 §2.6 给出"统计卡片"4 张卡的口径定义 + 是否变更 + 是否新增 L0/L1 维度。

#### 低严重度

**#U10（低）"空间/频道筛选" 在浏览页的层级与管理页的层级差异未澄清**

- 关联：§2.6
- 问题：原型 `NewsPage.tsx:102-143` 用两层 Pill（空间行 + 频道行）做筛选，与 v0.5 "mini 模式" 一致。但原型管理页 `SpacesManagement.tsx:170-260` 把空间做成"180×80 卡片"（带描述 + 角标 + 双击编辑），同 Source 与浏览页 Pill 完全不同。PRD §2.6 只笼统列出"空间/频道筛选"。建议 §2.6 补一句"浏览页 Pill 模式 / 管理页卡片模式 由原型保留"或显式说"两套并存且不复用同一组件"。

**#U11（低）AC-23「不出现独立全屏详情页入口」与 Source 详情页的导航逻辑没冲突澄清**

- 关联：AC-23、§2.6（Source 详情页保留）
- 问题：AC-23 说"不应出现独立全屏详情页入口"，本意是"新闻详情用抽屉不开新页"。但 Source 详情页本身就是独立全屏页（`/sources/:id`），且新闻卡片中"来源链接"会跳转到 Source 详情页（`NewsPage.tsx:225-232`）。建议 AC-23 措辞改为"本期不出现**新闻**独立全屏详情页入口"，避免歧义。

**#U12（低）原型 `SourceDetailPage` "最近一周新闻" 时间轴样式与 v0.5 现有列表不一致，未在 PRD 说明取舍**

- 关联：§2.6
- 问题：`SourceDetailPage.tsx:247-271` 用"圆点 + 竖线时间轴"展示最近新闻，v0.5 现有是普通列表。这是视觉风格选择题，本身无优劣，但 PRD 没说取哪个，UI 方案阶段会问。建议 §2.6 加一句"Source 详情页最近新闻列表样式照原型"或显式说"沿用 v0.5 列表样式不照原型时间轴"。

#### 不阻塞的观察

- 原型 `RootLayout.tsx:21-23` 顶部 logo + "牛马程小报" 文字与 v0.5 一致，没有破坏品牌识别度。
- 原型 `SpaceEditDialog.tsx` 输入图标用了 `<input type="text">` 而非 emoji picker，本期接 emoji picker 与否本身是小决策（可在 UI spec 中决定），不阻塞 PRD。
- 原型用了 Tailwind + shadcn 的设计 token（`primary / secondary / accent / muted` 等），v0.5 现有 CSS 变量体系（`--primary / --text-secondary` 等）兼容但命名不一致，UI 方案阶段需要做映射表（不要求 PRD 解决）。
- 原型未画"创建/编辑空间"弹窗里 emoji 选择器（仅 1 行 input），但 v0.5 UI spec 也是文本输入。本期不引入 emoji picker 与否是 UI spec 阶段决策。
- 原型 `monitoring` 顶部导航带未处理告警角标（`RootLayout.tsx:61-65`），这一交互值得保留并写入 PRD §2.6（与 #U2 合并）。
- 与 #A11 同向，§2.6 缺"原型页面 ↔ 当前 Vue 路由 ↔ 重构后路由"映射表，强烈建议补上。

#### 总结

- 阻断项（#U1-#U4）属于"PRD 阶段必须收敛"的产品级 UI 决策：抽屉 vs 常驻面板、路由结构、空间图标上传交互形态、卡片/抽屉信息密度——这四项一旦留到 UI 方案阶段，UI 角色会被迫做产品决策，触发与 PM 反复（与 #A3 同构）。
- 中严重度（#U5-#U9）属于"PRD 应给视觉/交互方向，UI 方案做细化"，目前完全缺失会导致 UI 方案阶段返工。
- 低严重度（#U10-#U12）是措辞与边界澄清。
- v0.6 整体方向 OK，但 PRD 把 Figma 原型当成"已经成型的 UI 答案"看待是误判——原型在 PRD 核心交付物（四段式抽屉、空间图标上传、L0/L1 处理状态可视化）上**直接缺画**，PM 不能默认 UI 阶段照抄就行。
- 等待 PM 汇总全部 5 方 R1 Review 后提交 R2；届时 UI 复审。

### Developer R1（2026-06-09）

**结论**：❌ 需修改。共 13 条意见（4 高 / 6 中 / 3 低）。

整体评价：v0.6 三条主线落到现有 Node.js 全栈（`server/` + `frontend/`）整体可实现，没有架构红线。但本轮 PRD 在**前后端契约（API 字段、版本、URL）、Vue 重构工程量、AI JSON 输出契约、重试退避配置、tasks 表与 L0/L1 状态机的对接、空间图标上传接口**这几块从工程实现角度还存在结构性缺口。Architect #A1-#A8 已经覆盖了架构/数据/外部依赖侧，UI #U1-#U9 覆盖了视觉/交互侧；Developer 视角下另外几个具体硬伤是**「目前 PRD 还没法让开发拆任务」**：没法估工时（4 高严重度全部影响范围估算）、没法生成接口契约、没法写 TDD 测试断言。下面意见按"能不能直接动手写代码"为评判标准。

#### 高严重度

**#D1（高）AC-25「不得用 mock 数据作为正式展示路径」与 v0.6 渐进式迁移的实操矛盾，缺少"接口未就绪 → 前端如何渲染"的工程规则**

- 关联：AC-25、§2.6 覆盖页面清单
- 问题：与 Architect #A9 / UI #U6 同向，但 Developer 视角更具体——v0.6 是"先按原型重构页面 + 后端 L0/L1 后置实现"还是"接口先动，前端后接"？目前 PRD 没说，意味着实施时：
  - 如果先重构前端：必然先写 mock，违反 AC-25 字面要求。
  - 如果先做后端 L0/L1：前端要等 2-3 周才能动工，迭代节奏过慢。
  - 推荐做法是**并行**：前端按 mock fixture 跑视觉，后端做完后切换；mock fixture 只放在 `__fixtures__/` 或 dev 专用路径，prod build 不打包。但 AC-25 当前措辞会把这条路径堵死。
- 现有 v0.5 经验值：`AdminPage.vue` / `NewsPage.vue` 等改造时都先用真接口（v0.5 是后端先做），导致前端"等几天再开工"——本期主线包含前端重构，再走老路会拖死迭代。
- 风险：开发拆任务时无法确定前端能否立即动工；如果都按"等后端再写"，迭代周期翻倍。
- 修改建议：AC-25 改写为两条：
  - AC-25a：**生产构建（`npm run build` 的 dist）页面渲染数据必须来自真实 API**；mock fixture 文件不得被 prod build 引用（用 `import.meta.env.DEV` 或独立目录隔离）。
  - AC-25b：**开发期允许使用 mock fixture 跑视觉**；但每条 fixture 必须对应一个后端契约文档（路径 + 字段 + 类型），fixture 与契约不一致由前端开发人员负责对齐。
- 同时建议在 §4.1 加一句"实施顺序：后端 L0/L1 schema/路由优先（提供契约骨架）→ 前端按 mock fixture 并行重构 → 后端 L1 算法完成后切换真实数据"。

**#D2（高）"前后端契约清单"在 PRD 完全缺失，AC-05/AC-06/AC-17/AC-18/AC-26 等多条 AC 无法在前端落地**

- 关联：AC-05、AC-06、AC-16、AC-17、AC-18、AC-21、AC-22、AC-26、§2.6
- 问题：v0.5 PRD R2 §10 是"API 契约清单（6 组 20+ endpoint）"，是当时实现阶段顺利的关键基础。v0.6 PRD 全程没有出现过任何一条 API 路径或字段名，但本期：
  - 新闻流卡片要展示"综合价值分 / 5 类标签 / 4 维分理由"——`GET /v1/news` 当前返回 `tags: string[]`、`importance_score: number`，需要扩展为五类标签结构 + `score_dimensions` + `score_total`，**字段名 PRD 必须给**（决定数据库列、API 序列化、前端 TS 类型三处）。
  - 抽屉四段式（翻译/补全/分析/标签）—— 当前 `GET /v1/news/:id` 不存在（NewsPage 直接用列表数据），本期需要新增详情端点还是把字段塞进列表？字段名是什么？
  - 空间图标上传 —— 是 `PATCH /v1/spaces/:id` 带 base64 还是 `POST /v1/spaces/:id/icon` multipart？响应返回什么字段（URL? 文件名? data URL?）？
  - L0/L1 处理状态 —— Architect #A1 提到需要状态字段，那是落在 `raw_items.l0_status` 还是新表 `processing_states`？前端 #U7 提的"Source 详情页 L0/L1 计数区块"需要什么聚合接口？
- 风险：契约不在 PRD 出，会在设计阶段反复——开发提"我需要 X 字段" → Architect 评估 → PM 拍板 → 设计文档加字段 → 又发现少 Y 字段……v0.4/v0.5 都因此走过 2-3 轮契约修补。
- 修改建议：PRD §3 验收标准下追加一节 **§3.7 前后端契约清单（草案）**，列举本期新增/变更的端点和字段，包括但不限于：
  - `GET /v1/news` 列表新字段（综合分、5 类标签、l1_status 等）
  - `GET /v1/news/:id` 是否新增（抽屉 4 段式数据载体）
  - `POST/PATCH /v1/spaces/:id/icon` 图标上传接口（推荐 multipart + 服务端返回相对 URL）
  - `GET /v1/sources/:id` 是否新增 L0/L1 处理统计字段（如 #U7 推荐方案采纳）
  - 监控/告警接口是否变更（如 #U2 决定合并路由）
- 不要求 PRD 给完整 JSON Schema，但必须给出每个字段的**名称 + 类型 + 含义**，让 Architect 在设计阶段直接细化即可。

**#D3（高）AC-08「3 次 / 1min / 5min / 15min」退避与现有 `dispatcher.ts requeueTask` 的退避策略冲突，工程上无法直接复用 tasks 表**

- 关联：AC-07、AC-08、AC-09
- 问题：与 Architect #A2 互补但角度不同。现有 `server/src/worker/dispatcher.ts:68-76` 的 `requeueTask` 退避公式是 `delaySeconds = min(300, 10 * (tries + 1))`（线性退避，最大 5 分钟，5 次上限），而 AC-08 写的是 `[1min, 5min, 15min]`（3 次上限，指数退避）。两套机制如果共存：
  - 沿用 `tasks` 表 + 现有 dispatcher → 必须改 dispatcher 退避公式 + max_attempts，会影响 v0.5 抓取任务（fetch / process）的退避行为，**风险面外溢**。
  - 新建一套 L0/L1 任务表（如 `l1_tasks`）+ 独立 dispatcher → 重复实现，代码维护成本翻倍。
  - 在 `tasks.type` 上加分类（如 `l0_classify / l1_process`）+ 按 type 走不同退避配置 → 推荐路径，但需要 PRD 明确"L0/L1 复用现有 tasks 表，按 type 区分退避策略"。
- 风险：开发会自己决定（基于现有代码惯性沿用 tasks 表），但如果改了 `requeueTask` 没限定 type，老的 fetch/process 任务退避也变了，影响 v0.5 已稳定运行的 X Stream 补偿抓取（24h 间隔）。
- 修改建议：
  - PRD §3.2 明确"L0/L1 任务复用现有 `tasks` 表，新增 `tasks.type ∈ {l0_classify, l1_process}`"——这是后端实现路径决策，不是设计细节。
  - 退避配置写成**按 type 可配置的常量表**，避免硬编码：例如 `RETRY_POLICY[task_type] = { max_attempts, backoff_seconds }`。
  - 明确 AC-08 的 `[60, 300, 900]` 只对 L0/L1 生效，**不影响**现有 `fetch_source / process_raw_item` 类型任务。

**#D4（高）AC-26/AC-27 空间图标上传缺关键工程约束：上传字段名、存储路径、URL 拼接、并发覆盖语义未定**

- 关联：AC-26、AC-27、AC-28、§2.7
- 问题：与 Architect #A8（存储位置）/ UI #U3（前端形态）互补，Developer 视角的实施细节：
  - HTTP 上传字段名：用 `file` / `icon` / `image`？影响前端 FormData 和后端 multipart 解析。
  - 现有 Fastify 项目**没有装 multipart 中间件**（`@fastify/multipart`），需要引入新依赖——这是依赖增量，PRD 应在 §5 前置依赖列出。
  - 静态资源访问路径：`/static/icons/{space_id}.{ext}` 还是 `/uploads/{uuid}.{ext}`？这两条路径需要在 nginx / Fastify 静态服务里配置，DevOps 必须知道。
  - 文件名是否带 hash 防缓存？图标替换后旧 URL 怎么处理（CDN-style versioning vs 立即覆盖）？
  - 并发上传同一个空间图标的覆盖语义（last-write-wins?）—— 前端是否锁按钮？
  - emoji 与图片同一字段（`channel_spaces.icon TEXT`）还是新增列（`icon_url TEXT`）？两种方案数据迁移成本不同。
- 风险：开发自己拍脑袋会出现"图标上传后必须刷新页面"或"换图标后旧图标残留 30 天"这类 bug。
- 修改建议：PRD §3.6 加段"上传工程契约"：
  - 接口：`POST /v1/spaces/:id/icon` multipart, field name = `file`；响应：`{ icon: "/uploads/spaces/{id}/{hash}.{ext}", icon_type: "image" }`。
  - 删除接口：`DELETE /v1/spaces/:id/icon`，响应空间回退到默认 emoji 或空。
  - 数据库：`channel_spaces.icon` 字段同时承载 emoji 文本和图片相对 URL，前端用前缀判断（`/uploads/` 开头 → 图片，否则 → emoji/文本）；或者新增 `icon_type` 列显式区分（推荐）。
  - 文件名带 hash 防缓存，旧文件由后端在替换/删除时清理（避免 DevOps 后续打扫战场）。

#### 中严重度

**#D5（中）AC-05 要求 L1 输出"中文标题、摘要、原文翻译、背景补全、AI 分析评价、AI 标签、四维评分"，但 AI 单次调用是否能稳定输出这么大的 JSON 没评估**

- 关联：AC-05、AC-16、AC-19
- 问题：现有 `processLLM` 输出的 JSON 大约 5 个 top-level key，体量在 1-2KB；本期要求的 JSON 至少 11 个 top-level key（title/summary/translation/context/analysis/tags×5 类/dimensions/scores/source_refs），加上"补全来源标签"（#A6）和"事实/推断/不确定"分类（#A6），单次输出可能在 5-10KB。问题：
  - LLM 输出越长，JSON 解析失败率越高（v0.5 已经踩过 `extractFirstJsonObject` 不稳定的坑）。
  - 单次调用如果失败要全部重做，token 浪费大。
  - 是否拆为多次调用（L1a 翻译+摘要 / L1b 评分+标签 / L1c 背景补全）？这样可以局部重试，但 token 成本和延迟上升。
- 风险：开发实施时如果走单次调用 → L1 成功率可能只有 70-80%（v0.5 评估值），AC-08 的 3 次重试会被频繁触发，LLM 成本失控。
- 修改建议：PRD §3.2 或 §6.1 风险段加一段"LLM 调用策略"：
  - 单次 vs 多次调用方案的取舍由设计阶段决定，但 PRD 应给"上限约束"：例如单次调用输出不超过 4KB / token 不超过 N。
  - 如果走多次调用，PRD 应明确**部分成功是否进入新闻流**（如翻译+评分成功但背景补全失败 → 是 L1 completed 还是 L1 partial？）——这是产品决策。

**#D6（中）AC-19 五类标签如果 AI 自由生成（#A5），前端如何稳定渲染"未知标签 → 默认样式"PRD 没规则**

- 关联：AC-19、AC-21、AC-22
- 问题：与 Architect #A5 / UI #U4 同向，Developer 视角是：
  - 如果 5 类标签是闭集枚举（如领域 = AI/财经/科技/其他），前端可以用 TS 枚举 + 颜色字典渲染；
  - 如果 AI 自由生成，前端要做的是"按字符串 hash 分配颜色"（v0.5 的 `TagChip` 已经是这套），但 5 类同时存在时颜色冲突如何避免？
  - 数据形态：`tags: { domain: string[], entity: string[], event: string[], content_type: string[], processing: string[] }` —— PRD 没确认这是不是最终结构。如果设计阶段才定，前端无法预先建组件 props。
- 风险：前端组件无法在前期建好，等到设计阶段再返工，重构成本高。
- 修改建议：PRD §3.4 给出**标签数据结构草案**（即使开闭集还没定）：例如 `tags = { domain: TagItem[], entity: TagItem[], event: TagItem[], content_type: TagItem[], processing: TagItem[] }`，每个 `TagItem = { name: string, value?: string, confidence?: number }`。允许 AI 自由生成的字段也要先把"结构骨架"定下来。

**#D7（中）前端重构覆盖 10 个页面（§2.6），但实际工程量没估，"现有 Vue 组件迁移"边界不清**

- 关联：§2.6
- 问题：v0.5 留下了 21 个 Vue 组件（已 grep 确认 `frontend/src/components/` 下 22 个文件），原型有 30+ React 组件。PRD §2.6 列了 10 个页面，但：
  - 哪些 v0.5 组件可以直接复用（如 `StatusBadge`、`Pagination`、`SourceTable`）？
  - 哪些必须重写（如新闻卡片要展示 5 类标签 + 4 维分 + 综合分，原 `NewsListItem` 不够）？
  - 哪些可以删除（如原型不再需要的组件）？
- v0.5 经验值：实现阶段先做组件清单（保留/重写/删除）才能精准估工时，本期 PRD 不给的话设计阶段架构师又要做清单。
- 修改建议：PRD §2.6 加一句"现有 Vue 组件迁移策略由设计阶段产出清单"，**并把'重构最大工作量页面'按 P0/P1/P2 标出**——例如 P0 = NewsPage（含卡片 + 抽屉 + 筛选）、P1 = AdminPage、P2 = SourceDetailPage。这样实施时可按优先级分批合并。

**#D8（中）AC-15「区分已确认事实、合理推断、不确定点」前端如何承载，Developer 实现成本估算缺信息**

- 关联：AC-15、AC-14
- 问题：与 UI #U5 同向，Developer 看视觉层级 + 数据载体的协同：
  - 如果 AI 输出 `context: string`（一段纯文本，不带分类）→ 前端无法做置信度区分；
  - 如果 AI 输出 `context: Array<{ text, confidence, source }>`（分句结构）→ 前端可以做但 LLM 输出复杂度极高（参考 #D5 风险）；
  - 折中：AI 输出 `context: { confirmed: string, inferred: string, uncertain: string }`（按段落分类）→ 实现成本中等。
- 风险：开发选择不同载体，前端组件结构差异巨大，实施时无法快速重写。
- 修改建议：PRD §3.3 在 #A6/#U5 的"段落级"决定基础上，明确数据载体：例如 `context = { sections: [{ kind: 'confirmed'|'inferred'|'uncertain', source: 'source_info'|'link_content'|'kb_search'|'x_search'|'web_search', text: string }] }`。粒度可大可小，但**结构必须 PRD 定**，否则前端组件 props 无法稳定。

**#D9（中）AC-11「每条 L1 都执行库内检索」对 LLM 链路延迟和成本的影响未量化，开发无法判断是否同步阻塞**

- 关联：AC-11、§6.1
- 问题：与 Architect #A7 同向，Developer 关心**调用是同步还是异步**：
  - 同步检索（每条 L1 先 SQL 检索 → 拼上下文 → 调 LLM）→ 阻塞 L1 处理，但实现简单；
  - 异步检索（先发 L1 LLM 调用，检索结果作为补充材料二次调用）→ 复杂但延迟低。
  - 检索结果送入 LLM 上下文的体量上限是多少（5 条 / 10 条 / 20 条）？token 成本如何控制？
- 风险：开发拍脑袋实现，可能让 L1 平均处理时间从 5s 飙到 30s，影响实时性。
- 修改建议：PRD §3.3 加一句"库内检索策略"：候选条数上限 + 同步 vs 异步 + 召回失败是否阻塞 L1（推荐：异步、上限 5 条、召回失败不阻塞）。

**#D10（中）"前端真实数据联调" 的环境依赖未列：本地开发是否要后端先实现 L0/L1？没有 mock 路径如何前后端并行？**

- 关联：AC-25、§5、§6.2
- 问题：与 #D1 互补，开发实操层面：
  - 前端开发员本地起 `npm run dev` 时，后端是否已经完整运行（含 L0/L1 worker）？v0.5 经验是经常本地后端 + 远程 DB；本期 L0/L1 还要依赖 LLM/搜索 API，本地是否需要这些 key？
  - Tester 联调本地是否能用？联调测试库是否要有"已 L1 completed 的样例数据"？
  - CI 自动化测试如何 mock LLM（v0.5 测试已经禁用，恢复测试时本期依赖如何打桩）？
- 风险：实施时本地开发环境差异大，前端工程师可能没法稳定跑前端联调。
- 修改建议：PRD §5 前置依赖 加一段"开发联调环境"：本地是否需要全套 LLM/搜索 key、是否提供 mock LLM 模式、Tester 联调样例数据如何造（推荐沿用 v0.5 "后台 DB 直造" 路径）。

#### 低严重度

**#D11（低）AC-25 / AC-23 / AC-24 三条"不应"措辞过于 absolute，缺"特殊场景例外"边界**

- 关联：AC-23、AC-24、AC-25
- 问题：AC-23「不应出现独立全屏详情页入口」与 UI #U11 同向（Source 详情页是全屏，建议加"新闻"修饰词）；AC-24「不出现反馈按钮」如果出现在产品/工程内部调试页是否算违反？建议措辞改为"v0.6 用户可见路径不引入..."。

**#D12（低）§5 前置依赖未列 v0.6 引入的 npm 依赖估算，DevOps R1 难以评估部署变更**

- 关联：§5、§6.1
- 问题：本期可能引入新依赖：`@fastify/multipart`（图标上传）、可能的搜索 SDK / embedding 库（库内检索如走向量）、可能的 link reader 库（如 cheerio / @mozilla/readability）。这些虽然由设计阶段确定具体选型，但 PRD 应在 §5 前置依赖列出"已知会新增依赖的能力点"，DevOps 才能评估部署影响（如下载大小、安全审计、生产环境兼容）。
- 修改建议：§5 加一行"已知新增能力点对应的依赖类型清单"，与 #A3 同向但更工程化。

**#D13（低）"四维分理由"的字段长度上限未给，前端布局难收敛**

- 关联：AC-16、AC-18
- 问题：AC-16 要求 4 维分各带"理由"，但理由是 1 句话还是 1 段？前端抽屉布局完全不同（1 句 → 行内展示；1 段 → 折叠卡片）。建议 PRD §3.4 给字符上限（如每维理由 ≤ 80 字）。

#### 不阻塞的观察

- 现有 `dispatcher.ts:62-76` `requeueTask` 已经支持 attempt + max_attempts + 退避，加 type 维度后可以平滑承载 L0/L1，schema 不用大改。
- `processed_news` 表的 `tags / entities / source_refs` 三个 jsonb 字段确实能装下 5 类标签 + 4 维分 + 5 来源（与 Architect 观察一致），但建议设计阶段评估是否拆 `processed_news_l1_meta` 单表，避免 jsonb 越塞越大影响 SELECT。
- `frontend/src/components/NewsDetailPanel.vue` 已经是抽屉形态（与 `SlidePanel.vue` 一致），UI #U1 推荐方案 A（沿用抽屉）在 Developer 视角下代码改造量最小——只需改抽屉内容结构，外壳全复用。
- `frontend/src/router.ts` 现有 5 路由，UI #U2 推荐合并 `/alerts` + `/logs` → `/monitoring` 的工程量在前端是新建 `MonitoringPage.vue` + 配 2 个 Tab 容器 + 旧路由做 301 redirect，约 1 天工作量。
- 原型项目 `/root/news-aggregation-platform` 是 React + shadcn + Tailwind，Vue 这边继续用现有 CSS 变量体系（`--primary` / `--text` / `--card`）；原型的设计 token 命名（`primary / secondary / accent / muted`）需要在 UI 方案阶段做映射表，Developer 不需要重新搭样式系统。
- v0.5 测试当前禁用（生产 DB 误删事故，参考 `developer.md` 2026-06-08），本期实施前必须先恢复独立 test DB + `.env.test`，否则 AC-08 的重试退避、AC-05 的 LLM 输出契约都无法用单元测试覆盖。这是实施前置工作，建议列入设计阶段或部署前置。

#### 总结

- 阻断项（#D1-#D4）属于"PRD 阶段必须收敛"的实施细节：mock 工程规则、API 契约清单、tasks 表退避策略、图标上传字段——这四项一旦留到实现阶段，会触发"开发提需求 → 反复修 PRD" 多轮。
- 中严重度（#D5-#D10）属于"PRD 给方向，设计阶段细化"，目前缺失会让设计阶段架构师被迫做产品 + 工程双重决策。
- 低严重度（#D11-#D13）是措辞与边界澄清。
- 三条主线落到现有 Node.js + Vue 栈技术可行，没有要砍重写或换栈的硬伤；`dispatcher.ts` 重试机制 / `processed_news` schema / `SlidePanel` 抽屉组件等 v0.5 基线都能复用作为良好起点。
- 与 Architect #A1-#A8、UI #U1-#U9 的判断一致：本轮 PRD 把太多产品决策甩给设计阶段，导致 5 方 R1 一致 ❌需修改。
- 等待 PM 汇总全部 5 方 R1 Review 后提交 R2；届时 Developer 复审。

