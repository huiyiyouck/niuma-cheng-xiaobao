# 牛马程小报 v0.2 详细设计文档

## 文档状态
- 整体状态：待Review
- Review 轮次：第 R1 轮
- 版本：v0.2
- 日期：2026-05-24
- 关联 PRD：[v0.2-prd.md](v0.2-prd.md)（已定稿）

### 本轮 Review 状态
| Review 方 | 状态 | 轮次 |
|-----------|------|------|
| PM        | ⏳待审 | R1 |
| 全栈开发  | ⏳待审 | R1 |

---

## 1. 概述

本文档是 v0.2 迭代（Source 重构 + 前端体验 + 日志系统）的详细设计，基于 [v0.2-prd.md](v0.2-prd.md) PRD R2 定稿。v0.2 的范围：

- A. Source 重构：清空旧数据，删除除 X/Twitter 外所有抓取器，新增 Source 独立管理 API、验证机制、类型自动识别
- B. 前端体验：侧边栏详情面板、子频道筛选/管理 UI、评分排序、标签/实体展示、统计卡片真实数据
- C. 日志系统：API 层结构化日志、前端日志查看器

---

## 2. 数据库变更

### 2.1 ALTER TABLE：sources

```sql
-- 重命名 name → display_name
ALTER TABLE sources RENAME COLUMN name TO display_name;

-- 新增 Source 管理字段
ALTER TABLE sources
  ADD COLUMN IF NOT EXISTS status VARCHAR(20) NOT NULL DEFAULT 'unverified',
  ADD COLUMN IF NOT EXISTS source_url TEXT,
  ADD COLUMN IF NOT EXISTS last_verified_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS verify_error TEXT;

-- 移除旧约束（用户自主创建 Source 模型下不合理）
ALTER TABLE sources DROP CONSTRAINT IF EXISTS uq_sources_type_name;
```

### 2.2 级联行为验证

删除 Source 前需确认以下 FK 的 ON DELETE 行为（当前 schema 状态）：

| FK 关系 | 当前定义 | 期望 |
|---------|---------|------|
| `channel_sources.source_id → sources.id` | `ON DELETE CASCADE` | ✅ 无需变更 |
| `raw_items.source_id → sources.id` | `ON DELETE CASCADE` | ✅ 无需变更 |
| `raw_items.channel_space_id → channel_spaces.id` | `ON DELETE CASCADE` | ✅ 无需变更 |
| `processed_news.raw_item_id → raw_items.id` | `ON DELETE CASCADE` | ✅ 无需变更 |
| `source_states.channel_source_id → channel_sources.id` | `ON DELETE CASCADE` | ✅ 无需变更 |
| `tasks.channel_source_id → channel_sources.id` | `ON DELETE CASCADE` | ✅ 无需变更 |

上述 FK 在开发实现前需通过 `SELECT` 查询 `information_schema` 确认实际数据库中的状态，设计文档中标记期望值。若实际与期望不符，迁移脚本中需补充 ALTER 修正。

### 2.3 数据清理

```sql
-- 清空 Source 和新闻数据，保留 channel_spaces 和 sub_channels
DELETE FROM processed_news;
DELETE FROM raw_items;
DELETE FROM tasks;
DELETE FROM source_states;
DELETE FROM channel_sources;
DELETE FROM sources;
-- channel_spaces 和 sub_channels 保留
```

### 2.4 迁移与回滚

迁移脚本：`db/migrations/v0.2.sql`

回滚脚本：`db/migrations/v0.2_rollback.sql`

```sql
-- 回滚
ALTER TABLE sources RENAME COLUMN display_name TO name;
ALTER TABLE sources
  DROP COLUMN IF EXISTS status,
  DROP COLUMN IF EXISTS source_url,
  DROP COLUMN IF EXISTS last_verified_at,
  DROP COLUMN IF EXISTS verify_error;
-- 重新添加旧约束（注意：若已有同名 Source 数据，此约束可能失败）
ALTER TABLE sources ADD CONSTRAINT uq_sources_type_name UNIQUE (type, name);
```

同时更新 `db/schema.sql` 全量 schema。

---

## 3. Model 与 Schema 变更

### 3.1 `app/models.py` — Source 类重构

```python
class Source(Base):
    __tablename__ = "sources"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    type: Mapped[str] = mapped_column(String(50), nullable=False)
    display_name: Mapped[str] = mapped_column(String(200), nullable=False)
    source_url: Mapped[Optional[str]] = mapped_column(Text(), nullable=True)
    status: Mapped[str] = mapped_column(String(20), nullable=False, default="unverified")
    config: Mapped[dict] = mapped_column(JSON, default=dict, nullable=False)
    last_verified_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True)
    verify_error: Mapped[Optional[str]] = mapped_column(Text(), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, nullable=False)
    # 移除 __table_args__ 中的 UniqueConstraint("type", "name")
```

### 3.2 `app/schemas.py` — SourceType 枚举

在 `app/schemas.py` 顶部新增，供 API、Worker、前端共用：

```python
from enum import Enum

class SourceType(str, Enum):
    x_twitter = "x_twitter"
    rss = "rss"
    github_trending = "github_trending"
    hf_daily_papers = "hf_daily_papers"
    hacker_news = "hacker_news"
    semantic_scholar = "semantic_scholar"
    unknown = "unknown"
```

Worker 可直接 `from app.schemas import SourceType`，无循环依赖风险（schemas 是纯数据定义层，不 import 业务模块）。

### 3.3 `app/schemas.py` — Source Schema 重构

```python
class SourceCreate(BaseModel):
    display_name: str = Field(min_length=1, max_length=200)
    source_url: Optional[str] = None
    type: Optional[str] = None  # null → 自动识别
    config: dict = Field(default_factory=dict)

class SourceUpdate(BaseModel):
    display_name: Optional[str] = Field(default=None, min_length=1, max_length=200)
    source_url: Optional[str] = None
    type: Optional[str] = None
    config: Optional[dict] = None

class SourceOut(BaseModel):
    id: uuid.UUID
    type: str
    display_name: str
    source_url: Optional[str] = None
    status: str
    config: dict
    last_verified_at: Optional[datetime] = None
    verify_error: Optional[str] = None
    created_at: datetime

class SourceWithBindings(SourceOut):
    """GET /v1/sources 列表项，含频道空间绑定信息"""
    channel_spaces: list[SourceBindingInfo] = []

class SourceBindingInfo(BaseModel):
    channel_space_id: uuid.UUID
    channel_space_name: str
    sub_channel_id: Optional[uuid.UUID] = None
    sub_channel_name: Optional[str] = None
    enabled: bool
```

### 3.4 新增 Verify/MarkVerified Schema

```python
class SourceVerifyResponse(BaseModel):
    status: str  # "ok" | "error"
    items: list[VerifyItem] = []
    total_fetched: int = 0
    error: Optional[str] = None

class VerifyItem(BaseModel):
    source_item_id: str
    source_item_url: Optional[str] = None
    title: Optional[str] = None
    content_preview: Optional[str] = None
    published_at: Optional[str] = None
```

### 3.5 新增 Stats Schema

```python
class ChannelStats(BaseModel):
    total_news: int
    today_new: int
    active_sources: int
    sub_channel_count: int
```

### 3.6 新增 Admin Log Schema

```python
class LogEntry(BaseModel):
    timestamp: str
    level: str
    logger: str
    message: str
    extra: Optional[dict] = None

class LogQueryResponse(BaseModel):
    entries: list[LogEntry]
    total: int
    has_more: bool
```

### 3.7 新增 News 排序参数

在 `list_news` 查询参数中新增：

```python
sort: Optional[str] = Query(default="published_desc")
# 取值：published_desc(默认) | score_desc | score_asc
```

现有 `ProcessedNewsOut` 无需变更——排序只影响查询 ORDER BY，不影响响应结构。

---

## 4. API 变更

### 4.1 Source 类型自动识别 — `app/source_detector.py`

新建独立模块，纯函数无副作用，API 和 Worker 均可导入：

```python
from app.schemas import SourceType

# URL 域名/路径匹配表
_DOMAIN_RULES: list[tuple[str, SourceType]] = [
    ("x.com", SourceType.x_twitter),
    ("twitter.com", SourceType.x_twitter),
    ("github.com", SourceType.github_trending),
    ("huggingface.co/papers", SourceType.hf_daily_papers),
    ("news.ycombinator.com", SourceType.hacker_news),
    ("semanticscholar.org", SourceType.semantic_scholar),
]

# URL 路径模式（用于 RSS 识别）
_RSS_PATH_PATTERNS = ["/feed/", "/rss/", "/feeds/posts/default"]
_RSS_SUFFIXES = [".xml", ".rss", ".atom"]
_RSS_CONTENT_TYPES = ["application/rss+xml", "application/atom+xml"]


def detect_type_from_url(source_url: str) -> SourceType:
    """基于 URL 特征识别 Source 类型（不含 HTTP 请求）。"""
    from urllib.parse import urlparse
    parsed = urlparse(source_url)
    host = (parsed.hostname or "").lower()
    path = parsed.path.lower()

    for pattern, st in _DOMAIN_RULES:
        if pattern in host:
            return st

    for pat in _RSS_PATH_PATTERNS:
        if pat in path:
            return SourceType.rss

    for suf in _RSS_SUFFIXES:
        if path.endswith(suf):
            return SourceType.rss

    return SourceType.unknown


async def detect_type_with_http(source_url: str, timeout: float = 3.0) -> SourceType:
    """当 URL 特征无法判定时，发起 HEAD 请求检查 Content-Type。"""
    base = detect_type_from_url(source_url)
    if base != SourceType.unknown:
        return base

    import httpx
    from app.settings import settings
    proxy = settings.https_proxy or settings.http_proxy
    try:
        async with httpx.AsyncClient(timeout=timeout, proxy=proxy, follow_redirects=True) as client:
            resp = await client.head(source_url)
            ct = resp.headers.get("content-type", "")
            for rss_ct in _RSS_CONTENT_TYPES:
                if rss_ct in ct:
                    return SourceType.rss
    except Exception:
        pass

    return SourceType.unknown
```

`detect_type_from_url` 在创建 Source 时同步调用（不阻塞）；仅当结果 `unknown` 时触发 `detect_type_with_http`（可能有 3s 延迟）。

### 4.2 Source CRUD 路由

所有 Source CRUD 端点在 `app/routes.py` 中新增。

#### POST /v1/sources — 创建 Source

```python
@router.post("/v1/sources", response_model=SourceOut)
async def create_source_v2(payload: SourceCreate, session: AsyncSession = Depends(get_session)):
    # 类型识别
    if payload.type:
        source_type = payload.type
    elif payload.source_url:
        source_type = await detect_type_with_http(payload.source_url)
    else:
        source_type = SourceType.unknown

    obj = Source(
        type=source_type,
        display_name=payload.display_name,
        source_url=payload.source_url,
        config=payload.config,
        status="unverified",
    )
    session.add(obj)
    await session.commit()
    await session.refresh(obj)
    return _source_out_v2(obj)
```

错误响应：
- display_name 为空 → 422
- source_url 和 type 同时为空 → 创建成功（type=unknown，用户后续编辑）

#### GET /v1/sources — 列出所有 Source

```python
@router.get("/v1/sources", response_model=list[SourceWithBindings])
async def list_sources_v2(
    status: Optional[str] = Query(default=None),
    type: Optional[str] = Query(default=None),
    session: AsyncSession = Depends(get_session),
):
    stmt = select(Source).order_by(Source.created_at.desc())
    if status:
        stmt = stmt.where(Source.status == status)
    if type:
        stmt = stmt.where(Source.type == type)
    rows = (await session.execute(stmt)).scalars().all()

    # 批量查询绑定关系
    source_ids = [r.id for r in rows]
    bindings = {}
    if source_ids:
        cs_rows = (await session.execute(
            select(ChannelSource, ChannelSpace.name, SubChannel.name)
            .join(ChannelSpace, ChannelSpace.id == ChannelSource.channel_space_id)
            .outerjoin(SubChannel, SubChannel.id == ChannelSource.sub_channel_id)
            .where(ChannelSource.source_id.in_(source_ids))
        )).all()
        for cs, space_name, sc_name in cs_rows:
            bindings.setdefault(cs.source_id, []).append(
                SourceBindingInfo(
                    channel_space_id=cs.channel_space_id,
                    channel_space_name=space_name,
                    sub_channel_id=cs.sub_channel_id,
                    sub_channel_name=sc_name,
                    enabled=cs.enabled,
                )
            )

    return [
        SourceWithBindings(
            **(_source_out_v2(r).model_dump()),
            channel_spaces=bindings.get(r.id, []),
        )
        for r in rows
    ]
```

#### GET /v1/sources/{id} — 获取 Source 详情

```python
@router.get("/v1/sources/{source_id}", response_model=SourceWithBindings)
async def get_source_v2(source_id: uuid.UUID, session: AsyncSession = Depends(get_session)):
    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="source not found")
    # 查询绑定关系（同 list_sources_v2 逻辑）
    ...
    return SourceWithBindings(...)
```

#### PUT /v1/sources/{id} — 更新 Source

```python
@router.put("/v1/sources/{source_id}", response_model=SourceOut)
async def update_source_v2(
    source_id: uuid.UUID,
    payload: SourceUpdate,
    session: AsyncSession = Depends(get_session),
):
    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="source not found")

    values = {}
    if payload.display_name is not None:
        values["display_name"] = payload.display_name
    if payload.source_url is not None:
        values["source_url"] = payload.source_url
    if payload.type is not None:
        values["type"] = payload.type
    if payload.config is not None:
        values["config"] = payload.config

    # 修改 source_url 或 type → 重置验证状态
    if payload.source_url is not None or payload.type is not None:
        values["status"] = "unverified"
        values["last_verified_at"] = None
        values["verify_error"] = None

    if values:
        await session.execute(
            update(Source).where(Source.id == source_id).values(**values)
        )
        await session.commit()

    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one()
    return _source_out_v2(row)
```

注意：`display_name` 修改不重置状态（PRD 3.1.4）。

#### DELETE /v1/sources/{id} — 删除 Source

```python
@router.delete("/v1/sources/{source_id}", status_code=204)
async def delete_source_v2(source_id: uuid.UUID, session: AsyncSession = Depends(get_session)):
    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="source not found")
    await session.delete(row)
    await session.commit()
```

级联行为：依赖现有 FK 的 `ON DELETE CASCADE` 自动完成（见 2.2 节），无需手动逐表清理。

### 4.3 Source 验证与标记

#### POST /v1/sources/{source_id}/verify — 测试抓取

```python
@router.post("/v1/sources/{source_id}/verify", response_model=SourceVerifyResponse)
async def verify_source(source_id: uuid.UUID, session: AsyncSession = Depends(get_session)):
    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="source not found")

    source_type = row.type
    config = _as_dict(row.config)

    # 根据 Source type 调用对应的抓取函数
    try:
        items = await asyncio.wait_for(
            _execute_verify_fetch(source_type, config),
            timeout=15.0,
        )
    except asyncio.TimeoutError:
        await session.execute(
            update(Source).where(Source.id == source_id).values(verify_error="外部服务超时（15s）")
        )
        await session.commit()
        return SourceVerifyResponse(status="error", error="外部服务超时（15s）")

    except NonRetryableError as e:
        await session.execute(
            update(Source).where(Source.id == source_id).values(verify_error=str(e))
        )
        await session.commit()
        return SourceVerifyResponse(status="error", error=str(e))

    except Exception as e:
        await session.execute(
            update(Source).where(Source.id == source_id).values(verify_error=str(e))
        )
        await session.commit()
        return SourceVerifyResponse(status="error", error=str(e))

    # 验证成功
    verify_items = [
        VerifyItem(
            source_item_id=it["source_item_id"],
            source_item_url=it.get("url"),
            title=it.get("content", {}).get("title") or it.get("title"),
            content_preview=_truncate_preview(it, 200),
            published_at=str(it.get("published_at")) if it.get("published_at") else None,
        )
        for it in items[:5]
    ]

    await session.execute(
        update(Source).where(Source.id == source_id).values(
            last_verified_at=datetime.now(tz=UTC),
            verify_error=None,
        )
    )
    await session.commit()
    return SourceVerifyResponse(status="ok", items=verify_items, total_fetched=len(items))
```

`_execute_verify_fetch` 是分发函数：

```python
async def _execute_verify_fetch(source_type: str, config: dict) -> list[dict]:
    """验证抓取分发——仅调用抓取函数，不入库。"""
    if source_type == "x_twitter":
        from worker.fetch_x_twitter import parse_x_twitter
        items, _ = await parse_x_twitter(config=config, cursor={}, max_items=5)
        return items
    # 其他类型暂未实现抓取器
    raise NonRetryableError(f"该 Source 类型的抓取器尚未实现：{source_type}")
```

**关键设计决策**：API 直接导入 `worker/fetch_x_twitter.parse_x_twitter`。该函数本身就是纯抓取逻辑（HTTP 调用 + 解析），不依赖 Worker 的数据库连接，从 API 调用是安全的。后续迭代新增抓取器时，只需在 `_execute_verify_fetch` 中新增分支。

`_truncate_preview` 辅助函数：

```python
def _truncate_preview(item: dict, max_chars: int) -> str:
    content = item.get("content", {})
    text = content.get("text") or content.get("description") or ""
    if len(text) <= max_chars:
        return text
    return text[:max_chars]
```

#### POST /v1/sources/{source_id}/mark-verified — 标记已验证

```python
@router.post("/v1/sources/{source_id}/mark-verified")
async def mark_source_verified(source_id: uuid.UUID, session: AsyncSession = Depends(get_session)):
    row = (await session.execute(select(Source).where(Source.id == source_id))).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="source not found")

    if row.status in ("verified", "active"):
        return {"status": "ok"}  # 幂等

    if row.status == "error":
        raise HTTPException(status_code=409, detail="不能将 error 状态的 Source 直接标记为 verified")

    if row.status != "unverified":
        raise HTTPException(status_code=409, detail=f"当前状态 {row.status} 不允许此操作")

    await session.execute(
        update(Source).where(Source.id == source_id).values(status="verified")
    )
    await session.commit()
    return {"status": "ok"}
```

### 4.4 统计端点

#### GET /v1/channel-spaces/{channel_space_id}/stats

```python
@router.get("/v1/channel-spaces/{channel_space_id}/stats", response_model=ChannelStats)
async def get_channel_stats(channel_space_id: uuid.UUID, session: AsyncSession = Depends(get_session)):
    today = datetime.now(tz=UTC).date()
    total = (await session.execute(
        select(func.count()).select_from(ProcessedNews).where(ProcessedNews.channel_space_id == channel_space_id)
    )).scalar()
    today_new = (await session.execute(
        select(func.count()).select_from(ProcessedNews).where(
            ProcessedNews.channel_space_id == channel_space_id,
            ProcessedNews.created_at >= today,
        )
    )).scalar()
    active_sources = (await session.execute(
        select(func.count()).select_from(ChannelSource).where(
            ChannelSource.channel_space_id == channel_space_id,
            ChannelSource.enabled == True,
        )
    )).scalar()
    sub_count = (await session.execute(
        select(func.count()).select_from(SubChannel).where(SubChannel.channel_space_id == channel_space_id)
    )).scalar()
    return ChannelStats(total_news=total, today_new=today_new, active_sources=active_sources, sub_channel_count=sub_count)
```

#### GET /v1/stats — 全局统计

```python
@router.get("/v1/stats", response_model=ChannelStats)
async def get_global_stats(session: AsyncSession = Depends(get_session)):
    today = datetime.now(tz=UTC).date()
    total = (await session.execute(select(func.count()).select_from(ProcessedNews))).scalar()
    today_new = (await session.execute(
        select(func.count()).select_from(ProcessedNews).where(ProcessedNews.created_at >= today)
    )).scalar()
    active_sources = (await session.execute(
        select(func.count()).select_from(ChannelSource).where(ChannelSource.enabled == True)
    )).scalar()
    sub_count = (await session.execute(select(func.count()).select_from(SubChannel))).scalar()
    return ChannelStats(total_news=total, today_new=today_new, active_sources=active_sources, sub_channel_count=sub_count)
```

### 4.5 新闻列表排序

在 `GET /v1/channel-spaces/{id}/news` 新增 `sort` 参数：

```python
@router.get("/channel-spaces/{channel_space_id}/news", response_model=list[ProcessedNewsOut])
async def list_news(
    channel_space_id: uuid.UUID,
    limit: int = Query(default=20, ge=1, le=100),
    offset: int = Query(default=0, ge=0),
    sub_channel_id: Optional[uuid.UUID] = Query(default=None),
    sort: Optional[str] = Query(default="published_desc"),
    session: AsyncSession = Depends(get_session),
):
    # 排序逻辑
    if sort == "score_desc":
        order_col = ProcessedNews.importance_score.desc().nullslast()
    elif sort == "score_asc":
        order_col = ProcessedNews.importance_score.asc().nullslast()
    else:  # published_desc（默认）
        order_col = ProcessedNews.published_at.desc().nullslast()

    stmt = (
        select(ProcessedNews)
        .where(ProcessedNews.channel_space_id == channel_space_id)
        .order_by(order_col, ProcessedNews.created_at.desc())
        .limit(limit).offset(offset)
    )
    if sub_channel_id is not None:
        stmt = stmt.where(ProcessedNews.sub_channel_id == sub_channel_id)
    rows = (await session.execute(stmt)).scalars().all()
    return [_processed_news_out(r) for r in rows]
```

### 4.6 日志查询端点

#### GET /v1/admin/logs

文件读取策略：
- 源文件：`logs/api.log`、`logs/worker.log`（通过 `source` 参数区分，默认同时读取两者并合并排序）
- 读取方式：从文件末尾反向读取（tail 语义），`offset` 为"距末尾的行数"，offset=0 返回最新 N 行
- 行数上限：单次最多 1000 行

```python
LOG_DIR = Path(__file__).resolve().parent.parent / "logs"
LOG_FILES = {"api": LOG_DIR / "api.log", "worker": LOG_DIR / "worker.log"}


def _read_log_lines(source: Optional[str], level: Optional[str], keyword: Optional[str],
                    limit: int, offset: int) -> tuple[list[dict], int]:
    """从磁盘日志文件读取并过滤，返回 (entries, total_matched)。"""
    import json

    files_to_read = []
    if source:
        if source in LOG_FILES:
            files_to_read.append(LOG_FILES[source])
    else:
        files_to_read = list(LOG_FILES.values())

    all_lines: list[dict] = []
    for fp in files_to_read:
        if not fp.exists():
            continue
        with open(fp, "r", encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if not line:
                    continue
                try:
                    entry = json.loads(line)
                except json.JSONDecodeError:
                    continue
                all_lines.append(entry)

    # 过滤
    if level:
        all_lines = [e for e in all_lines if e.get("level", "").upper() == level.upper()]
    if keyword:
        kw = keyword.lower()
        all_lines = [e for e in all_lines if kw in e.get("message", "").lower()]

    # 按时间戳倒序
    all_lines.sort(key=lambda e: e.get("timestamp", ""), reverse=True)

    total = len(all_lines)
    # offset 为距末尾行数：offset=0 → 最新 [0:limit]
    entries = all_lines[offset:offset + limit]
    return entries, total


@router.get("/v1/admin/logs", response_model=LogQueryResponse)
async def query_logs(
    level: Optional[str] = Query(default=None),
    source: Optional[str] = Query(default=None),
    keyword: Optional[str] = Query(default=None),
    limit: int = Query(default=100, ge=1, le=1000),
    offset: int = Query(default=0, ge=0),
):
    entries, total = _read_log_lines(source, level, keyword, limit, offset)
    return LogQueryResponse(
        entries=[LogEntry(**e) for e in entries],
        total=total,
        has_more=(offset + limit) < total,
    )
```

#### GET /v1/admin/logs/config

```python
@router.get("/v1/admin/logs/config")
async def get_logs_config():
    return {
        "levels": ["DEBUG", "INFO", "WARNING", "ERROR"],
        "sources": ["api", "worker"],
        "log_files": {k: str(v) for k, v in LOG_FILES.items()},
    }
```

### 4.7 转换函数更新

`_source_out` → `_source_out_v2`：

```python
def _source_out_v2(r: Source) -> SourceOut:
    return SourceOut.model_validate({
        "id": r.id, "type": r.type, "display_name": r.display_name,
        "source_url": r.source_url, "status": r.status,
        "config": _as_dict(r.config),
        "last_verified_at": r.last_verified_at,
        "verify_error": r.verify_error,
        "created_at": r.created_at,
    })
```

保留旧 `_source_out` 用于 channel_sources 绑定等向后兼容场景——`ChannelSourceWithSource` 中的 `source: SourceOut` 暂时使用转换逻辑适配（display_name → name fallback）。

---

## 5. Worker 变更

### 5.1 代码清理

#### 5.1.1 删除文件

| 文件 | 说明 |
|------|------|
| `worker/fetch_rss.py` (实际文件名为 `fetch_arxiv.py`) | RSS/Arxiv 抓取器 |
| `worker/fetch_hf.py` | HuggingFace Daily Papers 抓取器 |
| `worker/fetch_hackernews.py` | HackerNews 抓取器 |
| `worker/fetch_github_trending.py` | GitHub Trending 抓取器 |
| `worker/fetch_semantic_scholar.py` | Semantic Scholar 抓取器 |

#### 5.1.2 `worker/main.py` — import 清理

**移除的 import：**
```python
from worker.fetch_arxiv import parse_rss as parse_rss_items, render_text_for_llm as arxiv_text
from worker.fetch_hf import parse_daily_papers, render_text_for_llm as hf_text
from worker.fetch_github_trending import parse_github_trending, render_text_for_llm as gt_text
from worker.fetch_hackernews import parse_hackernews, render_text_for_llm as hn_text
from worker.fetch_semantic_scholar import parse_semantic_scholar, render_text_for_llm as ss_text
```

**保留的 import：**
```python
from worker.fetch_x_twitter import parse_x_twitter, render_text_for_llm as xt_text
from worker.errors import NonRetryableError
```

#### 5.1.3 `worker/main.py` — `fetch_and_ingest` 分支清理

移除所有非 Twitter 的 `if/elif` 分支，保留：

```python
async def fetch_and_ingest(conn, task: dict) -> None:
    ...
    async with httpx.AsyncClient(...) as client:
        if source_type == "x_twitter":
            items, cursor_updates = await parse_x_twitter(source_config, cursor, max_items)
        else:
            raise NonRetryableError(f"该 Source 类型的抓取器尚未实现：{source_type}")
    ...
```

默认分支抛 `NonRetryableError`（非 RuntimeError），避免无意义重试，并生成 alert。

#### 5.1.4 `worker/main.py` — `process_one` 分支清理

移除所有非 Twitter 的 `elif` 分支，保留：

```python
if row["source_type"] == "x_twitter":
    text = xt_text(content)
else:
    # 未实现 render 的 Source type：用原始 JSON 字符串
    text = json.dumps(content, ensure_ascii=False)
```

### 5.2 Scheduler 过滤

`scheduler_loop` 在创建 fetch 任务时，JOIN `sources` 并过滤 `status='active'`：

```python
rows = await conn.fetch(
    """
    select
      cs.id as channel_source_id,
      cs.channel_space_id,
      cs.fetch_policy,
      ss.id as state_id,
      ss.next_fetch_at
    from channel_sources cs
    join sources s on s.id = cs.source_id
    left join source_states ss on ss.channel_source_id = cs.id
    where cs.enabled = true
      and s.status = 'active'
    """
)
```

### 5.3 抓取函数复用

API 验证端点直接导入 `parse_x_twitter`，该函数已满足复用条件：
- 独立创建 `httpx.AsyncClient`（不依赖外部连接）
- 通过参数接收 config、cursor、max_items
- 返回 `(items, cursor_updates)` 元组

**无需重构**。后续新 Source 类型的抓取器实现也应遵循相同接口约定。

### 5.4 Worker 日志补充

在 `worker/main.py` 的 `process_one` 中，补充 LLM 调用耗时日志：

```python
t0 = datetime.now(tz=UTC)
result = await translate_and_summarize(text=text, source_url=row["source_item_url"])
elapsed = (datetime.now(tz=UTC) - t0).total_seconds()
log.info("LLM CALL source_type=%s duration=%.2fs", row["source_type"], elapsed)
```

Source 验证流程日志在 API 层记录（`POST /v1/sources/{id}/verify`），Worker 不直接参与验证。

### 5.5 日志格式升级为 JSON

将 `worker/logger.py` 中的文件输出格式从纯文本升级为 JSON 行：

```python
import json
from datetime import datetime, timezone

class JsonFormatter(logging.Formatter):
    def format(self, record):
        entry = {
            "timestamp": datetime.now(tz=timezone.utc).isoformat(),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
        }
        # 特殊字段附着在 record 上时进入 extra
        if hasattr(record, "extra_data"):
            entry["extra"] = record.extra_data
        return json.dumps(entry, ensure_ascii=False)

# 文件 handler 使用 JsonFormatter
fh.setFormatter(JsonFormatter())
```

控制台 handler 保留原可读格式不变。

---

## 6. API 层日志系统

### 6.1 `app/logger.py`

与 `worker/logger.py` 结构一致，日志文件为 `logs/api.log`：

```python
import logging
import json
import sys
from datetime import datetime, timezone
from pathlib import Path
from typing import Optional

LOG_DIR = Path(__file__).resolve().parent.parent / "logs"
LOG_FILE = LOG_DIR / "api.log"

_logger: Optional[logging.Logger] = None


class JsonFormatter(logging.Formatter):
    """JSON 行格式化器——与 Worker 完全一致。"""
    def format(self, record):
        entry = {
            "timestamp": datetime.now(tz=timezone.utc).isoformat(),
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
        }
        if hasattr(record, "extra_data"):
            entry["extra"] = record.extra_data
        return json.dumps(entry, ensure_ascii=False)


def get_logger(name: str = "api") -> logging.Logger:
    global _logger
    if _logger is not None:
        return _logger

    LOG_DIR.mkdir(parents=True, exist_ok=True)

    # 文件输出：JSON 格式，所有级别
    json_fmt = JsonFormatter()
    fh = logging.FileHandler(str(LOG_FILE), encoding="utf-8")
    fh.setLevel(logging.DEBUG)
    fh.setFormatter(json_fmt)

    # 控制台输出：可读格式，INFO 及以上
    console_fmt = logging.Formatter(
        "%(asctime)s [%(levelname)-5s] %(name)s %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S",
    )
    ch = logging.StreamHandler(sys.stderr)
    ch.setLevel(logging.INFO)
    ch.setFormatter(console_fmt)

    _logger = logging.getLogger(name)
    _logger.setLevel(logging.DEBUG)
    _logger.addHandler(fh)
    _logger.addHandler(ch)

    return _logger
```

### 6.2 HTTP 请求日志中间件

在 `app/routes.py` 或独立的 `app/middleware.py` 中新增：

```python
from fastapi import Request
from app.logger import get_logger
import time

log = get_logger()

# 作为中间件注册到 FastAPI app
async def log_requests(request: Request, call_next):
    t0 = time.monotonic()
    response = await call_next(request)
    duration_ms = round((time.monotonic() - t0) * 1000)
    log.info(
        "HTTP %s %s → %d (%.0fms)",
        request.method, request.url.path, response.status_code, duration_ms,
    )
    return response
```

注册方式（在 `app/main.py` 或 `app/__init__.py` 的 app 创建处）：

```python
app.middleware("http")(log_requests)
```

敏感信息脱敏：middleware 不记录请求头和 body，不涉及 Authorization/API Key。

---

## 7. 前端数据层适配

### 7.1 `frontend/src/lib/types.ts`

| 类型 | 变更 | 操作 |
|------|------|------|
| `Source` | `name` → `display_name`；新增 `source_url`、`status`、`last_verified_at`、`verify_error` | 重写 |
| `SourceWithBindings` | 新增类型 | 新增 |
| `SourceBindingInfo` | 新增类型 | 新增 |
| `VerifyItem` | 新增类型 | 新增 |
| `ChannelStats` | 新增类型（替换现有 `StatsOverview` 或并行） | 新增 |
| `LogEntry` | 新增类型 | 新增 |
| `NewsSort` | `"published_desc" \| "score_desc" \| "score_asc"` | 新增 |

### 7.2 `frontend/src/lib/api.ts`

| 函数 | 变更 |
|------|------|
| `listSources()` | 返回类型改为 `SourceWithBindings[]` |
| `createSource()` | payload 改为 `{ display_name, source_url?, type?, config? }` |
| `getSource(id)` | 新增函数 |
| `updateSource(id, payload)` | 新增函数 |
| `deleteSource(id)` | 新增函数 |
| `verifySource(id)` | 新增函数，返回 `SourceVerifyResponse` |
| `markVerified(id)` | 新增函数 |
| `listNews()` | opts 新增 `sort?` 参数 |
| `getChannelStats(spaceId)` | 新增函数 |
| `getGlobalStats()` | 新增函数 |
| `queryLogs(params)` | 新增函数 |
| `getLogsConfig()` | 新增函数 |

### 7.3 前端 UI 组件（设计层面声明，不含实现细节）

本设计文档不展开 UI 组件的具体实现（属前端开发范畴），仅列出需要修改/新增的 Vue 组件和关键数据流：

| 文件 | 操作 | 说明 |
|------|------|------|
| `views/NewsPage.vue` | 修改 | 接入排序参数、子频道筛选、统计卡片真实数据、详情面板 |
| `views/AdminPage.vue` | 修改 | 新增 Source 管理 Tab、子频道管理区域、日志查看器 Tab |
| 新增组件 | 新增 | `NewsDetailPanel.vue`、`SourceManager.vue`、`SubChannelManager.vue`、`LogViewer.vue`、`VerifyDialog.vue`、`TagChip.vue`、`EntityBadge.vue` |

---

## 8. 文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `app/models.py` | Source 类重构（字段重命名/新增） |
| 修改 | `app/schemas.py` | 新增 SourceType 枚举、Source/Verify/Stats/Log Schema |
| 新增 | `app/source_detector.py` | Source 类型自动识别 |
| 新增 | `app/logger.py` | API 层结构化日志 |
| 修改 | `app/main.py`（或 `__init__.py`） | 注册 HTTP 请求日志中间件 |
| 修改 | `app/routes.py` | 新增 Source CRUD/Verify/Mark/Stats/Admin 路由；新闻列表新增 sort 参数；更新转换函数 |
| 修改 | `app/settings.py` | 新增 log_dir、log_file 配置项 |
| 修改 | `worker/main.py` | 删除旧 import 和分支；scheduler JOIN + status 过滤；默认分支 NonRetryableError；LLM 耗时日志 |
| 修改 | `worker/logger.py` | 文件输出格式升级为 JSON 行 |
| 删除 | `worker/fetch_arxiv.py` | 旧 RSS 抓取器 |
| 删除 | `worker/fetch_hf.py` | 旧 HF 抓取器 |
| 删除 | `worker/fetch_hackernews.py` | 旧 HN 抓取器 |
| 删除 | `worker/fetch_github_trending.py` | 旧 GitHub 抓取器 |
| 删除 | `worker/fetch_semantic_scholar.py` | 旧 SS 抓取器 |
| 新增 | `db/migrations/v0.2.sql` | v0.2 迁移脚本 |
| 新增 | `db/migrations/v0.2_rollback.sql` | 回滚脚本 |
| 修改 | `db/schema.sql` | 更新全量 schema |
| 修改 | `frontend/src/lib/types.ts` | 所有类型变更 |
| 修改 | `frontend/src/lib/api.ts` | 所有 API 函数变更 |
| 修改 | `frontend/src/views/NewsPage.vue` | 排序/筛选/统计/详情面板接入 |
| 修改 | `frontend/src/views/AdminPage.vue` | Source 管理/子频道管理/日志查看器接入 |
| 新增 | 多个 `.vue` 组件 | 见 7.3 节 |

---

## 9. 技术约束

### 9.1 部署顺序

1. 执行 `db/migrations/v0.2.sql`（重命名列 + 新增列 + 移除约束）
2. 执行数据清理脚本（3.2.3 节 SQL）
3. 部署新代码（API + Worker + 前端）
4. 前端首次加载处理空状态（引导提示）

### 9.2 API 导入 Worker 模块

API 层 `POST /v1/sources/{id}/verify` 导入 `worker/fetch_x_twitter.parse_x_twitter`。该函数内部创建自己的 `httpx.AsyncClient`，不依赖 Worker 数据库连接，交叉导入是安全的。代码路径：`app/routes.py` → `worker/fetch_x_twitter.py`（纯抓取逻辑）→ 返回结果。

### 9.3 日志格式统一

API 和 Worker 的文件日志均使用 JSON 行格式（通过 `JsonFormatter`）。控制台输出保持人类可读格式。`GET /v1/admin/logs` 依赖 JSON 行格式做反序列化和过滤。若日志文件包含非 JSON 行（如启动横幅），读取时静默跳过。

### 9.4 日志轮转

v0.2 不实现自动日志轮转。实现方式参考：使用 `logging.handlers.TimedRotatingFileHandler`（按天轮转，保留 7 天）。若开发排期紧张，轮转可降级为手动实现或推迟到 v0.3。

### 9.5 Source 删除性能

DELETE Source 的级联范围覆盖 5 张表（channel_sources → tasks/raw_items/source_states + raw_items → processed_news）。对于一个活跃 Source（关联数百条 raw_items 和 processed_news），级联删除需要数秒。当前项目体量下（单用户、少量 Source）不会成为瓶颈，但前端应展示 loading 状态。

---

## 10. 验证方案

### 10.1 数据库迁移
- 执行 `db/migrations/v0.2.sql`，确认无报错
- 验证 `sources.display_name` 列存在，`name` 列不存在
- 验证 `uq_sources_type_name` 约束已移除
- 验证 status、source_url、last_verified_at、verify_error 列存在

### 10.2 Source CRUD
- 创建 Source（自动识别 type） → 验证 200、type 正确
- 创建 Source（手动指定 type） → 验证手动值优先
- 创建 Source（无 URL） → 验证 type=unknown
- 列出 Source → 验证 channel_spaces 绑定信息正确
- 更新 Source → 验证 source_url/type 修改后 status 重置
- 删除 Source → 验证关联数据级联清理

### 10.3 Source 验证
- 验证 X/Twitter Source → 返回限量抓取预览（≤5 条）
- 验证超时 → 返回 error + 超时信息
- mark-verified（unverified） → status→verified
- mark-verified（verified） → 幂等返回 200
- mark-verified（error） → 返回 409

### 10.4 Worker 清理
- Worker 启动 → 确认无 import 错误
- 创建 non-x_twitter Source → Worker 抛 NonRetryableError，任务不重试
- 创建 active X/Twitter Source → Worker 正常抓取
- scheduler 扫描 → 确认 unverified/verified/error Source 不被调度

### 10.5 统计端点
- 验证 channel stats → 四字段数值正确
- 验证 global stats → 全量数据正确

### 10.6 日志系统
- API 请求产生 JSON 行日志 → `logs/api.log` 文件可读
- `GET /v1/admin/logs` → 返回 JSON 格式日志条目
- 按 level/source 筛选 → 结果正确

### 10.7 前端
- TypeScript 编译通过（`npx vue-tsc --noEmit`）
- 排序参数传入 `listNews` → 前端正确展示
