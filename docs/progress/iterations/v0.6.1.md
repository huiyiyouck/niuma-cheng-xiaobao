# v0.6.1 迭代记录

## 概览
- 目标：AI 中枢接入 + 两类新闻产品更正 + 新闻相关性展示
- 开始时间：2026-07-04
- 关闭时间：
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：实现 R4 复核通过（PM ✅ / Architect ✅，2026-07-26）— 实现阶段收口；待 DevOps 生产部署（含 #A-R4-1/2 部署前核实）→ PM 迭代关闭检查
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | ✅ PM 已产出 `v0.6.1-prd.md`（2026-07-12）— 数据库契约边界解耦 + 两层入库架构 + 5 态状态机 + 处理类型区分 + 前端三层展示 + 管理侧 AI 统计 + 灰度开关 | Architect / Developer / DevOps | Architect ❌需修改（5条：2高/2中/1低）/ Developer ⚠️有条件通过 / DevOps ❌需修改（10条：3高/4中/3低） | R1 Review完成 |
| R2 | ✅ PM 已修改全部 21 条意见（7高/8中/6低）— 新增 5 条架构决策（AD-01~05）+ 部署协调 + 环境变量增量 + 回滚边界 + 运维事项 + 前端改动清单补全 + 直显类 language | Architect / Developer / DevOps | Architect ✅通过 / Developer ✅通过 / DevOps ✅通过 | PRD R2 已定稿 |

### UI 阶段
- UI 说明：本次 UI 变更较轻（仅展示分层，无新页面），界面要点已在 PRD §5 覆盖，由 PM 自审，不再单独产出 UI spec。跳过独立 UI 阶段。

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | ✅ Architect 已产出 `v0.6.1-design.md`（2026-07-12）— 5 条 ADR（ADR-006~010）+ raw_items process_type 扩展 + ai_worker 数据库角色与列级 GRANT + 触发器自动关联 news_positions + 内建 L1 兜底保留 + 零停机迁移 + 可回滚 | PM / Developer / DevOps | PM 🟡有意见（7条：1高/3中/3低）/ Developer ❌需修改（14条：6高/6中/2低）/ DevOps ❌需修改（10条：3高/5中/2低） | R1 Review 完成 |
| R2 | ✅ Architect 已修改全部 28 条意见（8高/13中/7低）— SECURITY DEFINER 触发器 + GRANT 修正（updated_at / raw_items.language / 序列）+ 占位 processed_news 模式 + AI_INTEGRATION_MODE 对齐 PRD + 人工 SQL 迁移 + CTE 批量 UPDATE + 运维告警章节 + dry-check 框架 + 部署验证清单 | PM / Developer / DevOps | PM ✅通过（7/7 条全部关闭）/ Developer ✅通过 / DevOps ✅通过 | 设计 R2 已定稿 |

### PRD 讨论记录（R1 前）

**2026-07-04 第一轮范围讨论（Owner + PM）**

Owner 提出的核心方向：
- 新闻抓取后先过 L0 过滤，过滤后的全部入库（标记待处理状态）
- AI 侧直接从库里拿待处理数据，做翻译 / 识别 / 评分等一系列流程
- 处理完写回库（更新状态）
- xiaobao 前端根据状态从库拿数据展示
- 核心变化：xiaobao 与 AI 中枢不再通过 HTTP 调用耦合，而是以数据库为契约边界

PM 提出的 5 个碰撞点（待 Owner 确认后再出 R1）：
1. **数据库共享 vs 各有各的**：PM 倾向共享库（两服务规模小、数据本就是 xiaobao 业务数据）
2. **L0 过滤谁来做**：PM 倾向 xiaobao 做 L0（规则简单确定性高、减少 AI 无效处理量、xiaobao 更懂 Source 上下文）
3. **状态机设计**：PM 初步设计 `pending_l0 → l0_passed → pending_ai → ai_processing → ai_completed`，失败分支待细化；第一类新闻定位待确认
4. **AI 侧拿数据方式**：PM 倾向轮询 poll（量小够用、不需新基础设施、与 xiaobao worker claim 模式一致）
5. **前端展示区分度**：PM 倾向 v0.6.1 先只展示 AI 处理后的作为最小闭环，后续再加筛选

待讨论事项：
- 第一类新闻（不经 AI 直接展示）在新方案中的定位
- 状态机完整定义（含失败重试、最终失败等）
- 共享库的 schema 变更协作机制
- `processed_news` 与 `news_positions` 的写回职责

**2026-07-05 Owner 确认意见（第二轮范围讨论）**

**大方向确认**：从 HTTP 改数据库边界方向正确——解决单条 74~79 秒同步调用的异步、重试、超时解耦问题，是用真实联调经验修正架构。

**5 个碰撞点 Owner 确认口径**：

1. **共享库 vs 各有各的** → ✅ 同意共享库，加边界纪律：schema 归 xiaobao 所有，ai 只准读指定待处理表、写回指定字段，ai 不得建表改表。
2. **L0 谁做** → ✅ 同意归 xiaobao，无争议。L0 是规则过滤、确定性高，xiaobao 懂 Source 上下文，且现状 L0 本来就在 xiaobao。
3. **状态机** → ✅ 同意方向，三条硬要求：
   - 状态数最少化：`l0_passed` 和 `pending_ai` 若语义重合则合并，说不清就合并
   - 失败分支必须有：处理失败（带重试计数）、最终失败、`ai_processing` 卡死回收（超时释放回队列）
   - 第一类新闻（直显类）不塞进 AI 状态机：用独立的「处理类型」字段（direct / ai）区分，状态机只管 ai 类型生命周期
4. **AI 拿数据方式** → ✅ 同意轮询 + claim 模式（与 xiaobao worker 一致）。PRD 定轮询间隔和单次取数上限。
5. **前端展示** → ❌ 不同意"只展示 AI 处理后的"。改为**按状态区分展示**：AI 处理完的富展示（评分/摘要/翻译），未处理/直显类的基础展示（与现生产行为一致：X 直显、AI 默认关）。避免页面内容积压骤减，最小闭环照样成立。

**Owner 补充的 2 条生态层提醒（跨项目边界）**：

1. **契约变更要走契约程序**：`contracts/news-l1.md` v1 是 HTTP 模式，"数据库为契约边界"等于换掉集成模式。按规则**先改 contracts（新建 DB 边界契约：表、字段、状态枚举、读写权属），再改两边代码**，CHANGELOG 记一行。ai 侧改造（HTTP 服务 → 轮询 worker）是 ai 项目的迭代，得走公告板提 REQ 让 ai 承接——xiaobao PRD 单方面定不了 ai 怎么干活。
2. **共享库不能推翻 ai 生态定位**：decisions/0002（06-29）刚把 ai 定位升级为"生态通用 AI 中枢、多调用方预留"；ai 直连 xiaobao 库读 schema 会把 ai 焊死在 xiaobao 上。要求：**ai 侧把"从 xiaobao 库取数"封装成数据源适配层，处理核心不感知取数方式**——成本是代码分层，不是新基础设施，但保住多调用方定位。

**讨论结论**：5 个碰撞点全部有结论，生态层 2 条约束纳入前置依赖。PM 出 PRD R1。

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R1 | ✅ Developer 已完成全部代码实现 — schema.ts process_type 扩展 + config.ts AI_INTEGRATION_MODE + processor.ts 占位 processed_news + dispatcher.ts 分流逻辑 + l0-classifier.ts l1_ai_process + news.ts API 扩展 + level-status.ts 统计接口 + reclaim.ts 回收同步 + 2 个 SQL 迁移文件 | 4dbbddb | 469cd9f | PM / Architect / DevOps | PM ❌需修改（1高/4低） / Architect ❌需修改（1高/3低） / DevOps ❌需修改（2高/4中/2低，共8条，其中 #DO-2 部署阻塞） | 测试环境验证通过（55/55 单元测试 + API 健康检查） | 实现 R1 Review 完成（3/3方） |
| R2 | ✅ Developer R2 修复 — 占位 processed_news 移至 l0-classifier + 移除 dead code + 默认值对齐 + deploy.sh 迁移步骤 + .env.example 同步 + systemd 模板修正 + ddl_only 用途说明 | 469cd9f | 12e2d46 | PM / Architect / DevOps | PM ✅通过 / Architect ✅通过 / DevOps ✅通过（高严重度全关闭） | 测试/生产环境部署完成 / 迁移执行成功 | 实现 R2 已定稿 |
| R3 | ✅ Developer 补实现前端展示层（迭代关闭检查遗留，PRD R2 §5.10）— api.ts 契约类型 + AI 统计调用 / NewsPage 卡片状态徽章与抽屉分层（状态条 + 四维评分 + AI 分析 + 背景补全 + 失败原因）/ MonitoringPage「AI 处理概览」Tab / AppSidebar AI 指标 / 后端 news.ts 详情补 process_type + 列表详情补 l1_error | 12e2d46 | 0c733c5 | PM / Architect / DevOps（Owner 订正：与 R1/R2 一致走三方 Review，不走 PM 单方验收） | DevOps ✅通过（无部署阻塞）/ Architect ⚠️有条件通过（9条：1高/4中/4低，#A-R3-1 部署前必修）/ PM ⚠️有条件通过（1低 #PM-R3-1 + 裁定 Architect 提请 4 项 + 同批修复清单 6 项） | server tsc 0 错误 + frontend build 通过 + 本地 dev 代理测试环境完成 6 种展示态可视化自测（含真实数据回归） | R3 Review 完成（3/3方），不定稿 → 进入 R4 修复轮 |
| R4 | ✅ Developer 完成三方 R3 同批修复清单 6 项 — #A-R3-1 l1_error 公开接口归一化为分类文案（原文仅 admin 返回，新增纯函数单测，红灯抓到 ETIMEDOUT 不含 timeout 子串的分类漏洞）/ #A-R3-2 retryable_failed 归入「解析中」（PM 方案①，卡片/抽屉不再出现失败字样）/ #A-R3-3 level-status 新增 `ai_completed`/`ai_retryable_failed`/`ai_final_failed` AI 口径字段（保留原全量字段兼容 SourceDetail），监控 AI Tab 改用 + 口径说明更新 / #A-R3-5 详情接口补返回 `raw_items.content` 提取原文 + `source_item_url`，抽屉「正文/查看原文」落地（占位 summary 与原文相同时不重复渲染）/ #A-R3-6/7 删 `pending` 幽灵分支 + `displayState` 白名单兜底（仅 completed→富展示，未知→待解析；无状态字段旧数据保持富展示）/ #A-R3-8 注释按 PM 裁定修正。**#PM-R3-1 经核实不成立**（api.ts 类型注释本为 AD-05 四维 `timeliness/impact/confidence/clarity`，不存在 `importance/relevance/credibility` key），按意见意图改为显式 `ScoreDimensions` 接口使契约 key 类型可见 | 0c733c5 | 5ab883f | PM + Architect 复核（DevOps 零部署阻塞维持，R4 未涉部署面变更） | Architect ✅通过（R3 六项全关闭；新增 2中3低不阻塞，#A-R4-1 缓存头 / #A-R4-2 ADMIN_REQUIRE_BOTH 列为部署前核实）/ PM ✅通过（2026-07-26 逐项 diff+单测复跑+测试环境实测：6 项全关闭；#PM-R3-1 确认误报公开撤回并记入 pm-corrections） | server tsc 0 错误 + 新增单测 news-public-error 6/6 通过 + frontend build 通过 + 本地 dev 模拟自测（retryable→解析中 / final_failed 失败态 / 未知状态白名单兜底）+ 真实数据回归正常（23 卡片） | R4 复核完成（PM ✅ / Architect ✅）→ 待 DevOps 生产部署 |

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|
| ✅ 测试环境部署完成 | test.huiyiyou.cloud (:8001) | API 健康检查 200 / 全局统计接口返回正确数据 / 新闻列表含 process_type/l1_status | 无 |
| ✅ 生产环境部署完成 | news.huiyiyou.cloud (:8000) | API 健康检查 200 / 全局统计接口返回正确数据 / 数据库迁移完成（637 条 ai 类型） | 无 |
| ✅ 测试环境 R4 部署完成（2026-07-26，Owner 要求 Developer 部署供验收） | test.huiyiyou.cloud (:8001) | `deploy.sh test` 执行成功（构建源拉至 `5ab883f` 之后 HEAD）/ `news-api-test` active / health 200 / `/global-level-status-counts` 返回 `ai_*` 三字段 / 详情返回 `content` + `original_url` / 浏览器实测：AI 空间富展示态（状态条+正文+查看原文）、财经空间 5 条真实「待解析」卡片 + 解析中抽屉（原文+提示框）均正常 | 无。生产环境待 PM/Architect 复核 R4 后由 DevOps 部署 |
| ✅ 生产环境 R4 部署完成（2026-07-27，DevOps） | news.huiyiyou.cloud (:8000) | **部署前 Architect R4 三项核实全过**：#A-R4-1 生产 nginx 无 `proxy_cache`（`/v1/` 无中间缓存，方案①关闭，无需加 Cache-Control）/ #A-R4-2 生产 `.env` `ADMIN_REQUIRE_BOTH=false`（非旁路，不升必修）/ #A-R4-4 verify SQL **0 行**（120 条 `ai/not_started` 全未进列表 processed_news=0、前端不展示，无永久待解析）。`deploy.sh both` 构建源拉至 `2c20da6`、新 bundle `index-BFek7phs.js`、迁移 skip（列已在）、`news-api` active + health 200；公网首页/bundle 200、`/v1/news` 返回 `process_type`/`l1_status`/`l1_error`、隔离（WD=/srv/prod）与迁移数据（2 空间 / 637 新闻）完好 | 无。遗留（低，不阻塞，Developer 后续）：#A-R4-3 外链协议白名单 / #A-R4-5 `??` 兜底 / #A-R4-2 抽公共 `isAdminAuthenticated`；生产库 `ai_worker` GRANT 已于 2026-07-27 执行（角色 cluster 级已建 + 补生产 news 库级列级 GRANT + 触发器；ai_worker 端到端连生产 news OK，757 条可读；test+prod 双库就绪）|

## 实现阶段遗留：前端展示层未实现（2026-07-25 迭代关闭检查发现）

> **执行迭代关闭检查时叫停。** 后端契约解耦 + 异步链路（schema / dispatcher / l0-classifier / news.ts / level-status / reclaim / SQL 迁移 + 触发器 + 列级权限）已完整实现、测试/生产部署、迁移成功；但 **PRD R2 §5.10「前端三层展示」在前端主浏览页完全未实现**，实现阶段 PM/Architect/DevOps 三方 Review 意见全部集中在后端，漏审前端展示层（PRD 当时定「UI 变更轻，并入 PM 自审」，PM 自审时亦未核查前端实现落地）。

**代码事实核查（2026-07-25）**：

| PRD R2 要求 | `NewsPage.tsx` 现状 | `api.ts` 类型层 |
|---|---|---|
| 卡片/抽屉 **处理状态徽章**（待AI/处理中/可重试失败/最终失败） | ❌ 无 | ❌ 无 `l1_status` |
| **直显类(direct) vs AI 类(ai)** 分层展示 | ❌ 无 | ❌ 无 `process_type` |
| **四维评分** 展示 | ❌ 仅单一 `score` | ❌ 无 `score_dimensions` |
| **AI 分析 / 上下文** 展示 | ❌ 无 | ❌ 无 `analysis` / `context` |

后端 `news.ts` 列表 & 详情查询已 SELECT 返回 `ri.l1_status / process_type`（实现 R1 #DD8 确认），前端未接收、未展示。

**前端会话（Developer）待办清单**：

1. `frontend/src/app/lib/api.ts`：新闻数据类型补字段 `l1_status` / `process_type` / `score_dimensions` / `analysis` / `context`（后端已返回，前端类型层缺）。
2. `frontend/src/app/pages/NewsPage.tsx` 卡片：处理状态徽章（待AI处理 / 处理中 / 可重试失败 / 最终失败）+ 直显类与 AI 类视觉区分。
3. `frontend/src/app/pages/NewsPage.tsx` 抽屉：四维评分（`score_dimensions`，现仅总分 `score`）+ AI 分析（`analysis`）+ 上下文（`context`）；AI 未完成时的基础展示态 + 处理中/失败状态条。
4. `frontend/src/app/pages/MonitoringPage.tsx`：AI 处理统计展示（后端 `/global-level-status-counts` 已扩展 `pending / processing / total_ai`，前端未调用/未展示）。
5. `frontend/src/app/components/AppSidebar.tsx`：AI 指标补充（按 PRD §5.10 前端改动清单核对）。

**收口路径**：Developer 实现上述前端展示层 → PM 对照 PRD R2 §5.10 验收 → 前端展示完整后**重新执行迭代关闭检查**，届时再收尾归档。
（Owner 2026-07-25 订正：验收不走 PM 单方，改为 PM / Architect / DevOps 三方 Review → DevOps 部署 → PM 迭代关闭检查。）

**进展（2026-07-25 Developer 实现 R3，commit `0c733c5`）**：待办清单 5 项全部实现：
1. ✅ `api.ts` — 新增 `NewsOut`/`NewsDetailOut` 契约类型（`l1_status`/`process_type`/`l1_error`/`score_dimensions`/`analysis`/`context`）+ `getGlobalLevelStatusCounts()`
2. ✅ `NewsPage.tsx` 卡片 — 「待解析」「解析中（脉冲）」角标；失败态底部灰色小字 hover 看原因；基础展示态不展示评分与标签
3. ✅ `NewsPage.tsx` 抽屉 — 状态条（✓ L0 通过 → ✓/⏳/✕）、四维评分条形图、AI 分析、背景补全、解析中提示、失败原因摘要；直显类无状态条
4. ✅ `MonitoringPage.tsx` — 新增「AI 处理概览」Tab（待处理/处理中/已完成/可重试失败/最终失败/AI 类总数六卡片 + 失败重试态）
5. ✅ `AppSidebar.tsx` — 底部统计补「AI 待处理 / AI 处理中」（统计接口失败时静默降级不影响基础四项）

附带后端小补（PRD §5.5 依赖）：`news.ts` 详情查询补返回 `process_type`（抽屉直显/AI 分层判断必需）；列表/详情补返回 `l1_error`（失败原因展示）。

**自测证据**：`server` tsc 0 错误；`frontend` vite build 通过；本地 dev（`VITE_API_PROXY_TARGET=https://test.huiyiyou.cloud`）连测试环境完成可视化自测——富展示（四维评分/AI 分析/背景补全/状态条）、待解析、解析中、可重试失败、最终失败、直显 6 种展示态截图核对，监控页 AI Tab 显示测试环境真实统计（completed=154/total_ai=154），侧栏 AI 指标正常；移除临时状态模拟代码后真实数据回归正常（23 卡片全富展示态、无残留）。

**已知偏差（提请三方 Review 时裁定）**：
- PRD §5.7 要求「今日成功数/今日失败数」，但后端 `/global-level-status-counts` 为全量累计口径（设计/实现 R1/R2 均按此定稿），前端如实标注「累计」。如需「今日」口径需后端扩展，建议留后续迭代。
- PRD §5.11「原文」语言标签依赖后端 language 检测（#PM-IMPL-3 已降级为后续迭代项），前端本轮未实现该标签。
- 待解析态卡片角标用「待解析」（PRD §5.4 表仅列「解析中」，AC-04 要求区分待解析/解析中，按 AC-04 从严实现）。

## Review 记录

### PM R1 Review（实现阶段，2026-07-14）

**结论**：❌ **需修改**（1 个高严重度阻塞 + 4 个低严重度）

**角色**：PM（产品经理）
**Review 范围**：实现代码对齐 PRD R2 定稿版和设计 R2 定稿版、AC 验收标准符合性

---

#### 🔴 高严重度（阻塞，必须修改）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #PM-IMPL-1 | **占位 `processed_news` 创建逻辑放错位置导致 dead code**。Developer 把占位创建逻辑放在 `processor.ts` L37-61 的 `processOne` 函数中，但 `processOne` 只处理 `process` task，而 AI 类 + database 模式下 `dispatcher.ts` 返回 `l0_classify`（不是 `process`），导致该分支永远不触发。正确位置应在 `l0-classifier.ts` L155-174（L0 通过后创建 `l1_ai_process` task 时同时创建占位）。 | `processor.ts` L37-61（dead code）+ `l0-classifier.ts` L155-174（缺失） | **违反 AC-01（L0 过滤后立即展示基础展示态）和 AC-06（AI 服务故障不影响基础链路）**。AI 类 database 模式下 L0 通过后的新闻无 `processed_news` 记录，`news.ts` 列表查询通过 JOIN `processed_news` + `news_positions` 会排除这些新闻，前端不可见，直到 ai_worker 完成处理。 | 在 `l0-classifier.ts` `classifyL0` 函数中，L0 通过后创建 `l1_ai_process` task 之前（或同时），插入占位 `processed_news` 记录（标题/摘要从 `raw_items.content` 提取原文），移到正确位置后删除 `processor.ts` L37-61 的 dead code。 |

---

#### 🟢 低严重度（优化建议，不阻塞但应修复）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #PM-IMPL-2 | **`.env.example` 未同步更新**。PRD §6.5 和设计 §6.3 明确要求 `AI_INTEGRATION_MODE` / `AI_STALE_TIMEOUT_MS` / `AI_MAX_RETRIES` 在 `.env` / `.env.example` / systemd EnvironmentFile 三处同步，但 `server/.env.example` 未包含任何 v0.6.1 新增变量。 | `server/.env.example` | 补充 3 个新增环境变量到 `.env.example`，含注释说明默认值和可选值。 |
| #PM-IMPL-3 | **`language` 字段硬编码 `"zh"`**。PRD §5.11 / #D6 修复要求直显类 `language` 检测原文语言写入，但 `processor.ts` L49/L111 和 `l1-processor.ts` L196 都硬编码 `"zh"`。 | `processor.ts` L49/L111、`l1-processor.ts` L196 | 直显类改为检测原文语言写入（X/Twitter 内容检测语言）；AI 类占位可暂用原文语言标识，ai_worker 处理后更新为输出语言。如本期工作量过大可降级为后续迭代项，但需明确标注。 |
| #PM-IMPL-4 | **`l1-processor.ts` 仍保留手动 `news_positions` fan-out 代码**。设计 §2.3 #DD10 修复要求移除手动创建代码（触发器已覆盖），`processor.ts` 已移除 ✅，但 `l1-processor.ts` L223-238 未移除。 | `l1-processor.ts` L223-238 | 移除手动 `INSERT INTO news_positions` 循环代码，触发器 `trg_processed_news_auto_link` 已覆盖此场景。 |
| #PM-IMPL-5 | **`config.ts` 默认值与 PRD 不一致**。PRD §6.5 要求 `AI_INTEGRATION_MODE` 默认 `database`，但 `config.ts` L92 默认 `"http"`。 | `server/src/shared/config.ts` L92 | 如果是有意保守默认（需显式启用 database 模式），在设计文档中补充说明并同步 PRD 勘误；否则改为默认 `"database"` 对齐 PRD。 |

---

#### ✅ 已正确实现的部分

| PRD AC / 设计要求 | 实现文件 | 验证结果 |
|-------------------|----------|----------|
| ADR-007 `process_type` 字段 | `schema.ts` L179 | ✅ varchar(20) NOT NULL DEFAULT 'ai' |
| 索引 ix_raw_items_ai_queue + ix_raw_items_direct_published | `schema.ts` L188-191 | ✅ 部分索引正确 |
| AI_INTEGRATION_MODE 配置项 | `config.ts` L92 | ✅ 已新增（默认值待确认 #PM-IMPL-5） |
| ENABLE_AI_PROCESSING 废弃但兼容读取 | `config.ts` L90 | ✅ 逻辑正确 |
| dispatcher 分流逻辑（direct/ai） | `dispatcher.ts` L32-46 | ✅ 正确 |
| l1_ai_process task type + BACKOFF_CONFIG | `dispatcher.ts` L22 | ✅ maxAttempts=3, backoff=[60,300,900] |
| max_attempts 显式设置 | `dispatcher.ts` L200-225 | ✅ #DD14 修复 |
| l0-classifier L0 通过后 l1_status='queued' | `l0-classifier.ts` L163-168 | ✅ #DD12 修复 |
| l1_ai_process task 创建 | `l0-classifier.ts` L170-174 | ✅ |
| news.ts 列表查询扩展 ri.l1_status/ri.process_type | `news.ts` L64-70 | ✅ #DD8 修复 |
| news.ts 详情查询扩展 | `news.ts` L88-92 | ✅ |
| /global-level-status-counts 扩展 pending/processing/total_ai | `level-status.ts` L44-57 | ✅ #PM-3 修复 |
| reclaim 卡死回收 + 同步 raw_items.l1_status | `reclaim.ts` L6-35 | ✅ |
| SQL 迁移 DDL + 索引 + 触发器 + GRANT + 数据回填 | `v0.6.1_ai_contract.sql` | ✅ 完整 |
| SECURITY DEFINER + search_path 锁定 | `v0.6.1_ai_contract.sql` L31-54 | ✅ #D1 修复 |
| REVOKE CREATE ON SCHEMA public | `v0.6.1_ai_contract.sql` L97 | ✅ #D8 修复 |
| 列级 GRANT（不含 raw_items.language） | `v0.6.1_ai_contract.sql` L101-104 | ✅ #DD5 修复 |
| 无序列权限（uuid 不需要） | `v0.6.1_ai_contract.sql` | ✅ #DD6 修复 |
| 回滚脚本（注释化） | `v0.6.1_ai_contract.sql` L129-135 | ✅ |

### Architect R1 Review（实现阶段，2026-07-12）

**结论**：❌ **需修改**（1 个高严重度阻塞 + 1 个新发现低严重度 + 确认 PM 的 4 个低严重度）

**角色**：Architect（架构师）
**Review 范围**：实现代码对齐设计文档 R2 定稿版的架构边界、数据流正确性、ADR 落地完整性

---

#### 🔴 高严重度（阻塞，必须修改）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #A1 | **占位 `processed_news` 创建位置错误，是 dead code**（与 PM #PM-IMPL-1 同一问题，架构师独立确认）。数据流追踪：AI+database 模式下 `dispatcher.ts` 入库时 task type = `l0_classify`（非 `process`）→ L0 通过后 `classifyL0` 创建 `l1_ai_process` task + `l1_status='queued'` → task 等待外部 ai_worker claim。整条链路不经过 `processOne` 函数。但占位创建代码位于 `processor.ts` L34-61 的 `processOne` 内，该函数只处理 `process` task。**三路数据流均不触发该代码**：direct 类走 direct 分支 / AI+database 走 l0_classify→l1_ai_process / AI+http 走 l0_classify→l1_process。 | `processor.ts` L34-61（dead code）+ `l0-classifier.ts` L155-174（缺失占位创建） | **违反 ADR-010 / AC-01 / AC-06**。AI 类 database 模式下 L0 通过后无 `processed_news` 记录 → `news.ts` L62-76 查询通过 `JOIN processed_news pn JOIN news_positions np` 排除这些新闻 → 前端不可见 → 直至 ai_worker 完成处理才有记录。 | 在 `l0-classifier.ts` `classifyL0` 函数中，L0 通过后创建 `l1_ai_process` task 的同一事务内插入占位 `processed_news`（标题/摘要从 `raw_items.content` 提取原文），随后删除 `processor.ts` L34-61 的 dead code。 |

---

#### 🟢 低严重度（优化建议，不阻塞但应修复）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #A2 | **`l1-processor.ts` 仍保留手动 `news_positions` fan-out**（与 PM #PM-IMPL-4 同一问题，架构师独立确认）。`processor.ts` 已正确移除（L99 注释"触发器自动创建"），但 `l1-processor.ts` L228-235 仍手动 `INSERT INTO news_positions` 循环。触发器 `trg_processed_news_auto_link` 已覆盖所有 INSERT 场景，手动代码冗余且可能导致双重写入（触发器 + 手动）。 | `l1-processor.ts` L223-238 | 移除手动 `news_positions` 创建循环（`SELECT display_positions` + `for pos of positions` 循环），触发器已覆盖。 |
| #A3 | **`reclaim.ts` 回收同步 SQL 可优化精确度**。当前逻辑先用第一个 UPDATE 把 `l1_ai_process` task 从 running→queued，再用第三个 UPDATE 同步 `raw_items.l1_status` 从 processing→queued，但第三个 UPDATE 查找所有 `status='queued'` 的 l1_ai_process task。虽然 `AND l1_status = 'processing'` 条件能正确过滤刚创建的 task，但逻辑不够精确，且在极端并发场景下（多个 reclaim tick 重叠）可能误更新。 | `reclaim.ts` L25-35 | 建议用 `UPDATE ... RETURNING raw_item_id` 从第一个 UPDATE 获取刚回收的 task ID，再用这些 ID 精确更新 `raw_items`。当前实现可工作，属优化项。 |
| #A4 | **`config.ts` 默认值 `"http"` 与 PRD §6.5 不一致**（与 PM #PM-IMPL-5 同一问题，架构师独立确认）。PRD 要求默认 `database`，代码默认 `http`。 | `server/src/shared/config.ts` L92 | 对齐 PRD §6.5 改为默认 `database`；如有保守默认意图需在设计文档补充说明并同步 PRD 勘误。 |

---

#### ✅ PM 意见核验

| PM 编号 | 架构师确认 | 说明 |
|---------|-----------|------|
| #PM-IMPL-1（高） | ✅ 确认成立 | 数据流追踪确认 dead code，见 #A1 |
| #PM-IMPL-2（低） | ✅ 确认成立 | `.env.example` 确实未同步 |
| #PM-IMPL-3（低） | ✅ 确认成立 | `language` 硬编码 `"zh"` 确实存在 |
| #PM-IMPL-4（低） | ✅ 确认成立 | `l1-processor.ts` 手动 fan-out 确实未移除，见 #A2 |
| #PM-IMPL-5（低） | ✅ 确认成立 | `config.ts` 默认值确实不一致，见 #A4 |

---

#### ✅ 已正确实现的部分（架构层面确认）

| ADR / 设计要求 | 实现文件 | 验证结果 |
|----------------|----------|----------|
| ADR-007 `process_type` 字段 + 部分索引 | `schema.ts` L179/188-191 | ✅ varchar(20) NOT NULL DEFAULT 'ai' + 2 个部分索引 |
| ADR-008 `ai_worker` 角色 + 列级 GRANT | `v0.6.1_ai_contract.sql` L97-114 | ✅ REVOKE CREATE ON SCHEMA + 列级 GRANT 不含 language/sequence |
| ADR-008 SECURITY DEFINER 触发器 | `v0.6.1_ai_contract.sql` L31-54 | ✅ `SECURITY DEFINER` + `SET search_path = public, pg_temp` |
| ADR-006 dispatcher 分流（direct/ai） | `dispatcher.ts` L32-46 | ✅ `determineProcessType` 正确 |
| ADR-009 `l1_ai_process` task type + max_attempts=3 | `dispatcher.ts` L22/L200-225 | ✅ BACKOFF_CONFIG + 显式 max_attempts |
| ADR-009 reclaim 卡死回收 + aiStaleTimeoutMs | `reclaim.ts` L6-35 | ✅ 使用 `config.aiStaleTimeoutMs` |
| ADR-010 news.ts SELECT 扩展 | `news.ts` L64-70 | ✅ 追加 `ri.l1_status, ri.process_type` |
| ADR-010 /global-level-status-counts 扩展 | `level-status.ts` L44-57 | ✅ pending/processing/total_ai 统计 |
| l0-classifier L0 通过后 l1_status 同步 | `l0-classifier.ts` L163-168 | ✅ #DD12 修复正确 |
| processor.ts shouldDirectDisplay 改读 process_type | `processor.ts` L127-129 | ✅ #DD9 修复正确 |
| processor.ts 移除手动 news_positions fan-out | `processor.ts` L99 | ✅ #DD10 部分修复（processor.ts 已移除，l1-processor.ts 未移除见 #A2） |
| SQL 迁移完整（DDL + 索引 + 触发器 + GRANT + REVOKE + 数据回填 + 回滚） | `v0.6.1_ai_contract.sql` | ✅ 完整，含回滚脚本 |
| 数据回填（现有数据 process_type 修正） | `v0.6.1_ai_contract.sql` L72-82 | ✅ CTE 包装批量 UPDATE（#DD13 修复） |

---

#### 📋 Review 说明

- **与 PM Review 的关系**：本 Review 独立完成，与 PM #PM-IMPL-1 高严重度问题系同一根因的独立确认（非重复提报）。新增 #A3（reclaim.ts 优化）为架构师独立发现。
- **未覆盖**：DevOps 角色尚未完成 R1 Review，建议 DevOps 侧重点关注：数据库角色实际创建 / GRANT 执行 / env 变量三处同步 / 灰度切换路径 / 连接池配置 / 告警扩展。
- **阻塞项**：#A1 为阻塞项，必须修复后才能进入 R2。#A2/#A3/#A4 与 PM 的 4 个低严重度项一并在 R2 修复。

### DevOps R1 Review（实现阶段，2026-07-14）

**结论**：❌ **需修改**（2 个高严重度 + 4 个中严重度 + 2 个低严重度，共 8 条）

**角色**：DevOps（运维/部署工程师）
**Review 范围**：数据库迁移 SQL、环境变量配置、systemd 部署、连接池配置、告警扩展、部署脚本、灰度切换路径、回滚路径

---

#### 🔴 高严重度（阻塞，必须修改）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #DO-1 | **`.env.example` 未同步 v0.6.1 新增环境变量**（与 PM #PM-IMPL-2 同一问题，DevOps 独立确认，从部署视角升级为高严重度）。PRD §6.5 明确要求三处同步（.env / .env.example / systemd EnvironmentFile），但 `server/.env.example` 不含任何 v0.6.1 新增变量：`AI_INTEGRATION_MODE` / `AI_STALE_TIMEOUT_MS` / `AI_MAX_RETRIES`。新环境部署时不知道需要这些变量，会用默认值运行（`AI_INTEGRATION_MODE="http"`，与 PRD 默认 `database` 不一致，导致 AI 处理不生效且无明显报错 | `server/.env.example` 全缺 | 补充 3 个新增环境变量到 `.env.example`，含注释说明默认值和可选值。Systemd 模板也需同步检查。 |
| #DO-2 | **部署脚本 `deploy.sh` 无数据库迁移执行步骤（部署阻塞项）。v0.6.1 有 SQL 迁移脚本（`v0.6.1_ai_contract.sql`），但 `deploy.sh` 只有 rsync + systemctl restart，没有执行 SQL 迁移的步骤。v0.6 部署经验明确记载「drizzle 迁移元数据已脱轨，迁移走人工 SQL」，但 deploy.sh 完全没提及。生产部署如果漏执行迁移，新功能不生效（process_type 列不存在会导致 SQL 报错） | `deploy/deploy.sh` 无迁移步骤 | 在 deploy.sh 增加迁移执行步骤：部署前检查迁移脚本是否已执行（或在部署脚本中增加执行 SQL 迁移的步骤，明确由谁执行、怎么验证）。建议：①新增 `migrate_db()` 函数，用 psql 执行 `server/db/scripts/v0.6.1_ai_contract.sql`（幂等，有 IF NOT EXISTS 保护）；②部署前 dry-check 验证迁移结果。 |

---

#### 🟡 中严重度（建议修改，不阻塞但应修复）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #DO-3 | **systemd unit 模板仍有 `ExecStartPre=/usr/bin/npx drizzle-kit migrate`**。v0.6 部署时两 unit 都已去 ExecStartPre migrate（drizzle 元数据脱轨），但 `deploy/systemd/news-api.service` 模板第 12 行还保留着。模板与实际生产状态不一致，新人按模板部署会执行 drizzle migrate 失败或误操作 | `deploy/systemd/news-api.service` L12 | 移除 `ExecStartPre=/usr/bin/npx drizzle-kit migrate` 或替换为人工 SQL 迁移注释说明，保持与生产现状一致 |
| #DO-4 | **`l1-monitor.ts` 告警未扩展 AI 相关告警类型**。设计 §7.3 要求 4 类告警（进程崩溃 / 队列积压 / 卡死回收 / 最终失败），但现有实现：①`checkTaskBacklog` 查的是 `l1_status = 'not_started'`（v0.6 L1 队列），不覆盖 AI 类 `queued/retryable_failed` 积压；②`checkFinalFailedSurge` 查所有 final_failed 不区分 AI 类；③**无卡死回收触发的告警**（reclaim 回收了多少条，运维侧不知道）；④无 ai_worker 进程崩溃告警（不在 xiaobao 侧，ai 侧系统管） | `server/src/worker/l1-monitor.ts` | 扩展 `checkTaskBacklog` 增加 AI 类队列检查（`process_type = 'ai' AND l1_status IN ('queued', 'retryable_failed')`）；② `checkFinalFailedSurge` 区分 AI 类统计；③新增 `checkReclaimEvents` 告警（最近 1 小时 reclaim 回收次数 > 阈值）；④ai_worker 进程崩溃告警归 ai 侧负责，xiaobao 侧只负责数据库层告警 |
| #DO-5 | **systemd 无 `OnFailure` 告警配置**。设计 §7.3 提到「systemd OnFailure → 邮件」，但 `news-api.service` 无 OnFailure 配置。进程崩溃只会静默重启，运维不知道。 | `deploy/systemd/news-api.service` | 增加 `OnFailure=` 配置或在部署 handbook 中明确告警渠道（本期不实现需标注清楚） |
| #DO-6 | **连接池配置与设计文档的关系未明确**。设计 §6.3 说 ai_worker 连接池大小 5（ai 侧），xiaobao 侧 `pool.ts` `max: 10` 是 v0.6 原值。本期 xiaobao 侧连接池需求是否变化未明确，是否需要调整或监控 | `server/src/db/pool.ts` L18 | 本期 xiaobao 侧连接池不变（max: 10），在设计文档或代码注释中标注。重点是部署前核验 PG max_connections 总量（news-api 10 + ai_worker 5 < max_connections * 0.8） |

---

#### 🟢 低严重度（优化建议，可不改）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #DO-7 | **`v0.6.1_ai_contract.sql` 回滚脚本是注释化的，未提供可执行回滚脚本**。L129-135 的回滚 SQL 全部被注释，实际回滚时需要手动取消注释，容易出错 | `server/db/scripts/v0.6.1_ai_contract.sql` L129-135 | 不改动格式（注释化是对的，避免误执行），但增加回滚步骤说明文档化（deploy handbook 中明确回滚操作顺序） |
| #DO-8 | **`v0.6.1_ddl_only.sql` 与 `v0.6.1_ai_contract.sql` 关系未说明**。两个脚本有什么区别、分别什么时候用、测试环境用哪个、生产环境用哪个，没有注释说明 | `server/db/scripts/v0.6.1_ddl_only.sql` | 在文件头加注释说明用途（ddl_only 用于测试环境（不需要 ai_worker 角色），ai_contract 用于生产（含角色+GRANT+REVOKE+数据回填） |

---

#### ✅ 已正确实现的部分（DevOps 视角确认）

| 设计要求 | 实现文件 | 验证结果 |
|--------|----------|----------|
| SECURITY DEFINER 触发器 + search_path | `v0.6.1_ai_contract.sql` L31-54 | ✅ 正确，#D1 修复到位 |
| REVOKE CREATE ON SCHEMA public | `v0.6.1_ai_contract.sql` L97 | ✅ #D8 修复到位 |
| 列级 GRANT（不含 raw_items.language） | `v0.6.1_ai_contract.sql` L101-104 | ✅ #DD5 修复到位 |
| tasks GRANT 含 updated_at | `v0.6.1_ai_contract.sql` L114 | ✅ #DD2 修复到位 |
| 无序列权限（uuid 不需要） | `v0.6.1_ai_contract.sql` | ✅ #DD6 修复到位 |
| AI_INTEGRATION_MODE 配置项 | `config.ts` L92 | ✅ 已新增（默认值待确认 #PM-IMPL-5） |
| ENABLE_AI_PROCESSING 兼容读取 | `config.ts` L90 | ✅ 逻辑正确 |
| AI_STALE_TIMEOUT_MS / AI_MAX_RETRIES | `config.ts` L94-96 | ✅ 已新增 |
| 数据回填 CTE 包装 | `v0.6.1_ai_contract.sql` L62-69 | ✅ #DD13 修复到位 |
| deploy.sh 去软链 + rsync 隔离 | `deploy/deploy.sh` | ✅ v0.6 架构延续，本期无变更 |

---

#### 📋 Review 说明

- **与 PM Review 的关系**：#DO-1（.env.example 未同步）与 PM #PM-IMPL-2 同一问题，DevOps 从部署视角独立确认，升级为高严重度（部署会直接导致新环境部署失败）。
- **新增发现**：#DO-2（deploy.sh 无迁移步骤）、#DO-3（systemd 模板与实际不一致）、#DO-4（告警未扩展 AI 类型）、#DO-5（OnFailure 缺失）、#DO-6（连接池关系未明确）、#DO-7（回滚脚本注释化）、#DO-8（ddl_only 用途未说明）为 DevOps 独立发现。
- **阻塞项**：#DO-2 为部署阻塞项（deploy.sh 漏迁移步骤，生产部署漏执行迁移会导致功能不生效）。#DO-1 也是高严重度（.env.example 缺变量）。
- **与 PM/Architect 阻塞项的关系**：PM #PM-IMPL-1 / Architect #A1（占位 processed_news 位置错误）是功能阻塞，DevOps #DO-2 是部署阻塞，两者独立，都需 R2 修复。

### Architect R2 Review（实现阶段，2026-07-15）

**结论**：✅ **通过**（架构师 R1 全部 1 条高严重度阻塞已修复；3 条低严重度中 2 条已修复，1 条保留为优化建议不阻塞）

**角色**：Architect（架构师）
**Review 范围**：R2 修复代码验证（`fa98b23` + `12e2d46`，R1 base: `469cd9f`），架构师 R1 4 条意见 + PM 5 条意见的关闭验证

---

#### 🔴 高严重度（R1 阻塞项关闭验证）

| R1 编号 | 问题 | 修复位置 | 验证结果 |
|---------|------|----------|----------|
| #A1 | 占位 `processed_news` 创建位置错误（dead code） | `l0-classifier.ts` L157-186（新增占位创建）+ `processor.ts` L34-61（删除 dead code） | ✅ **已关闭**。占位创建移至 `classifyL0` 函数 L0 通过后（database 模式），与 `l1_ai_process` task 创建在同一事务链路中；`processor.ts` 中 AI+database 的早期 return 分支已完全移除，dead code 清理干净。数据流验证：AI+database 模式下 `dispatcher` → `l0_classify`（L0 通过 → 创建占位 processed_news → 创建 l1_ai_process task + l1_status='queued'）→ ai_worker claim 处理。三路（direct/ai-database/ai-http）均有正确出口，无遗漏。 |

---

#### 🟢 低严重度（R1 优化项关闭验证）

| R1 编号 | 问题 | 修复位置 | 验证结果 |
|---------|------|----------|----------|
| #A2 | `l1-processor.ts` 仍保留手动 `news_positions` fan-out | `l1-processor.ts` L223-238（已删除） | ✅ **已关闭**。手动 `SELECT display_positions` + `for pos INSERT` 循环已完全移除，替换为一行注释说明触发器自动创建。触发器 `trg_processed_news_auto_link`（AFTER INSERT）已覆盖所有 INSERT 场景（直显类 + AI 类 + 内建 L1），不会双重写入。 |
| #A3 | `reclaim.ts` 回收同步 SQL 可优化精确度 | （未修改） | ⚠️ **保留为优化建议，不阻塞**。当前实现可工作（`l1_status = 'processing'` 条件能正确过滤），极端并发下理论上存在误更新风险但实际影响极小（同一 raw_item 不会同时有多个 l1_ai_process task）。建议后续迭代用 `UPDATE ... RETURNING raw_item_id` 优化，但本期不阻塞。 |
| #A4 | `config.ts` 默认值 `"http"` 与 PRD §6.5 不一致 | `server/src/shared/config.ts` L92 | ✅ **已关闭**。默认值从 `"http"` 改为 `"database"`，与 PRD §6.5 对齐。注释标注 `#PM-IMPL-5/#A4 修复`。 |

---

#### ✅ PM R1 意见关闭验证（架构师独立复核）

| PM 编号 | 问题 | 修复位置 | 验证结果 |
|---------|------|----------|----------|
| #PM-IMPL-1（高） | 占位 processed_news 创建位置错误（dead code） | `l0-classifier.ts` + `processor.ts` | ✅ 已关闭（同 #A1） |
| #PM-IMPL-2（低） | `.env.example` 未同步 | `server/.env.example` L44-50 | ✅ 已关闭。补充了 `AI_INTEGRATION_MODE` / `AI_STALE_TIMEOUT_MS` / `AI_MAX_RETRIES` 3 个变量，含注释说明。 |
| #PM-IMPL-3（低） | `language` 硬编码 `"zh"` | `processor.ts` L111 + `l0-classifier.ts` L237 | ✅ 已关闭。新增 `detectLanguage()` 函数（中文字符正则检测），直显类和 AI 占位类均使用检测结果写入 language 字段。 |
| #PM-IMPL-4（低） | `l1-processor.ts` 手动 fan-out 未移除 | `l1-processor.ts` L223-238 | ✅ 已关闭（同 #A2） |
| #PM-IMPL-5（低） | `config.ts` 默认值与 PRD 不一致 | `config.ts` L92 | ✅ 已关闭（同 #A4） |

---

#### 🆕 R2 新发现（架构师视角，非阻塞）

本次 R2 复审无新发现的高/中严重度问题。唯一观察：

- **`deploy.sh` 迁移脚本用 ddl_only 版而非 ai_contract 版**：`deploy.sh` L42 执行的是 `v0.6.1_ddl_only.sql`（不含 ai_worker 角色和 GRANT）。这是正确选择——deploy.sh 是 xiaobao 侧部署脚本，ai_worker 角色创建和 GRANT 应由 DBA 或运维手动执行（生产环境权限隔离要求），不应由应用部署脚本自动创建数据库角色。ddl_only 版含 DDL + 索引 + 触发器 + 数据回填，满足 xiaobao 侧部署需求。

---

#### 📋 Review 说明

- **架构师 R1 意见关闭率**：高严重度 1/1 关闭，低严重度 2/3 关闭（#A3 保留为优化建议不阻塞）。**无未关闭阻塞项**。
- **未覆盖**：DevOps 的 8 条 R1 意见（2高/4中/2低）由 DevOps 角色自行 R2 复审，架构师在此只交叉验证了与架构相关的部分（#DO-2 deploy.sh 迁移步骤 ✅ / #DO-3 systemd 模板 ✅ / #DO-8 ddl_only 用途说明 ✅）。
- **整体评估**：R2 修复质量高，核心架构问题（#A1 占位 dead code）已彻底解决，dead code 清理干净，数据流三路出口均验证正确。**架构师 R2 复审通过**。

### DevOps R2 复审（实现阶段，2026-07-14）

**结论**：✅ **通过**（高严重度全部关闭，中/低严重度部分关闭，无阻塞项）

**角色**：DevOps（运维/部署工程师）
**复审范围**：R2 修复覆盖的 DevOps R1 意见核验 + 未修复项影响评估

---

#### R1 意见核验（8 条）

| # | R1 严重度 | R2 关闭状态 | 验证位置 |
|---|---------|------------|----------|
| #DO-1 | 🔴 高 | ✅ 完全关闭 — `.env.example` 已补充 3 个新增变量（AI_INTEGRATION_MODE / AI_STALE_TIMEOUT_MS / AI_MAX_RETRIES） | `server/.env.example` L46-52 |
| #DO-2 | 🔴 高 | ✅ 完全关闭 — `deploy.sh` L40-57 新增迁移执行逻辑（检查列是否存在 + 幂等执行 + 错误处理） | `deploy/deploy.sh` L40-57 |
| #DO-3 | 🟡 中 | ✅ 完全关闭 — systemd unit 模板已移除 `ExecStartPre drizzle-kit migrate`，改为注释说明"迁移走人工 SQL" | `deploy/systemd/news-api.service` L12 |
| #DO-4 | 🟡 中 | ⚠️ 未修复（中严重度，不阻塞）— `l1-monitor.ts` 未扩展 AI 类告警。本期未实现，建议后续迭代补齐 | `server/src/worker/l1-monitor.ts` 无改动 |
| #DO-5 | 🟡 中 | ⚠️ 未修复（中严重度，不阻塞）— systemd 无 `OnFailure` 配置。本期未实现 | `deploy/systemd/news-api.service` 无 OnFailure |
| #DO-6 | 🟡 中 | ⚠️ 未修复（中严重度，不阻塞）— xiaobao 侧连接池未改（max: 10），本期无变化。部署前需核验 PG max_connections | `server/src/db/pool.ts` 无改动 |
| #DO-7 | 🟢 低 | ⚠️ 未修复（低严重度，不阻塞）— 回滚脚本仍注释化。建议 deploy handbook 文档化 | `v0.6.1_ai_contract.sql` L129-135 无改动 |
| #DO-8 | 🟢 低 | ✅ 完全关闭 — `v0.6.1_ddl_only.sql` 文件头已加用途说明（测试环境 vs 生产环境） | `v0.6.1_ddl_only.sql` L1-4 |

---

#### R2 新增部署侧改进确认

✅ **迁移脚本执行路径**：`deploy.sh` L43-57 实现在 server/ 目录下执行迁移脚本（解决 pg 模块查找问题），先加载 `.env` 的 `DATABASE_URL`，再检查列是否存在，幂等执行安全

✅ **默认值对齐**：`config.ts` L93 `AI_INTEGRATION_MODE` 默认值改为 `"database"`（与 PRD 一致），L92 兼容读取 `ENABLE_AI_PROCESSING`

✅ **迁移脚本说明**：`v0.6.1_ddl_only.sql` L1-4 明确区分测试环境（本文件）和生产环境（ai_contract.sql）

---

#### 未修复项（中/低严重度，不阻塞 R2 通过）

| # | 严重度 | 问题 | 建议 |
|---|--------|------|------|
| #DO-4 | 🟡 中 | 告警未扩展 AI 类型 | 后续迭代补齐：AI 类队列积压 / 最终失败区分 AI 类 / 卡死回收告警 |
| #DO-5 | 🟡 中 | systemd 无 OnFailure | 后续迭代补齐或在 deploy handbook 明确告警渠道 |
| #DO-6 | 🟡 中 | 连接池关系未明确 | 部署前 dry-check 核验 `SHOW max_connections;`（news-api 10 + ai_worker 5 < 0.8 * max） |
| #DO-7 | 🟢 低 | 回滚脚本注释化 | deploy handbook 文档化回滚操作步骤 |

---

#### 整体评价

- ✅ **2 个高严重度阻塞项全部关闭**（#DO-1 / #DO-2），部署路径已打通
- ✅ **2 个中/低严重度关闭**（#DO-3 / #DO-8），systemd 模板和 SQL 脚本说明已修复
- ⚠️ **4 个中/低严重度未修复**（#DO-4/5/6/7），不阻塞 R2 通过，建议后续迭代补齐
- ✅ **部署就绪检查已通过**（测试/生产环境部署完成，迁移执行成功）

---

#### 📋 Review 说明

- **阻塞项状态**：R1 的两个阻塞项（#DO-1 / #DO-2）已全部修复，无新增阻塞项
- **与 PM/Architect 的关系**：DevOps 关注的部署阻塞已解决，PM/Architect 关注的功能阻塞（#PM-IMPL-1/#A1）由 Developer 同次 R2 修复
- **下一步**：实现阶段可进入收尾归档，后续迭代补齐 #DO-4/5/6/7 告警和连接池监控

### DevOps R3 Review（实现阶段 · 前端展示层，2026-07-25）

**结论**：✅ **通过（DevOps 视角无部署阻塞）**

**角色**：DevOps（运维/部署工程师）
**Review 范围**：R3 前端展示层实现（commit `0c733c5`）在部署方式、环境变量、构建产物、发布风险、回滚层面的影响；不审前端功能/交互/UI（归 PM / Architect）。

---

#### 部署侧核验（6 项）

| # | 检查项 | 结论 |
|---|--------|------|
| 1 | 依赖增量 | ✅ 无 — 前后端 `package.json` 未变，部署无需新 `npm install` 包 |
| 2 | 环境变量 | ✅ 无新增 — 无新 `VITE_*` / 后端 env；前端走相对路径经 nginx 反代（既有架构） |
| 3 | Schema / 迁移 | ✅ 无新迁移 — R3 后端为纯查询字段增补（`news.ts` SELECT 补 `ri.l1_error` / `ri.process_type`）；依赖的 `raw_items.l1_error`（v0.6 遗留列）+ `process_type`（v0.6.1 已部署迁移）**生产 `news` + 测试 `news_test` 库均已存在**（独立 psql 核实），不会缺列 500 |
| 4 | 构建产物 | ✅ 独立复核通过 — `frontend` vite build ✓（1.29s，仅 chunk size warning 非错误）；`server` tsc `--noEmit` 0 错误 |
| 5 | 部署路径 | ✅ 走现有 `deploy.sh` 全隔离流程（前端 build→rsync `/var/www/{news,test}` + 后端 rsync `/srv/niuma-news/{prod,test}/server` + 重启），无新增部署步骤 |
| 6 | 回滚 | ✅ 安全 — 纯代码回滚（`git checkout` 旧 commit + 重新部署），R3 无数据/schema 变更，无不可逆操作 |

#### 3 条已知偏差 · DevOps 视角

均为产品/功能口径（§5.7 今日 vs 累计、§5.11 language 标签、待解析措辞），**无 DevOps 部署相关**，交 PM / Architect 裁定。

#### 沿用 R2 未修复项（不阻塞 R3，部署前提示）

- **#DO-6**（🟡 中）连接池 dry-check：部署前核验 `SHOW max_connections;`（news-api 10 + ai_worker 5 < 0.8×max）
- **#DO-4/5/7**（中/低）AI 类告警扩展 / systemd OnFailure / 回滚脚本文档化：R3 不新增此类，维持后续迭代补齐，不阻塞

#### 📋 Review 说明 / 部署建议

- **阻塞项**：无。R3 从 DevOps 视角零部署阻塞。
- **部署前须知**：服务器构建源当前 HEAD `12e2d46`，落后 R3（`0c733c5`）——部署前先 `git pull` 到含 R3 的 commit。
- **部署后 verify**：① 列表/详情 API 返回含 `process_type` / `l1_error` 字段；② 6 种展示态（富展示/待解析/解析中/可重试失败/最终失败/直显）；③ 监控页 AI Tab 统计；④ 侧栏 AI 指标；⑤ AI 统计接口失败时侧栏静默降级不影响基础四项（§5.10）。
- **待其余两方**：PM（PRD §5.10 功能验收）+ Architect（数据流/契约）Review 通过后，由 DevOps 执行 test→prod 部署。

### Architect R3 Review（实现阶段 · 前端展示层，2026-07-25）

**结论**：⚠️ **有条件通过**（1 高 / 4 中 / 4 低，共 9 条）

**角色**：Architect（架构师）
**Review 范围**：commit `0c733c5`（base `12e2d46`）前端展示分层实现 + 附带后端 `news.ts` 字段补返回，对齐 PRD R2 §5.3-§5.11 与设计 R2 §3.1/§3.2/§5.1 的契约边界、展示状态机、口径一致性
**独立验证**：`server` `npx tsc --noEmit` 0 错误 ✅；`frontend` `npm run build` 通过 ✅（注：`frontend` 无 `tsconfig.json`，build 脚本仅 `vite build`，esbuild 转译**不做类型检查**，见 #A-R3-9）

**通过条件**：#A-R3-1 部署前必修（与 DevOps R3「零部署阻塞」不冲突——DevOps 审的是部署方式，本条是接口暴露面）；#A-R3-2 / #A-R3-3 建议同批修（改动量均在 10 行内）；#A-R3-4 / #A-R3-5 提请 PM 裁定本轮补齐或登记延期；低严重度不阻塞。

---

#### 🔴 高严重度（本迭代修，建议列为部署门禁）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #A-R3-1 | **`l1_error` 内部异常原文经公开接口透传到前台**。`admin-guard.ts` L20-28 只对非 GET 写操作 + `/v1/admin`、`/v1/alerts` 做鉴权，`GET /v1/news`、`GET /v1/news/:id` 属**公开接口**（无 token 可访问）。本轮把 `ri.l1_error` 加入列表与详情 SELECT 并原样进 `newsToOut`/`newsDetailToOut`，前端卡片 `title` 属性 hover 全量展示、抽屉展示前 300 字。该字段写入源是 `dispatcher.ts` L160/L324/L326 的 `err.message`（LLM/HTTP/DB 原始异常），可能含内部 endpoint、主机名、模型名、驱动堆栈片段；database 模式下还由外部 ai_worker 写入，内容不由 xiaobao 侧控制。PRD §5.5 要求的是「失败原因**摘要**」，不是内部异常原文。 | `server/src/api/routes/news.ts` L64/L88/L122/L156；`frontend/src/app/pages/NewsPage.tsx` 卡片 `title={news.l1Error}` + 抽屉失败原因块 | 面向公网新增的信息暴露面，v0.6.1 R3 引入（v0.6 后端不外吐该字段）。 | 后端归一化：复用 `tasks.last_error_kind` 的错误分类映射为固定文案（如「AI 服务暂时不可用」/「解析超时」/「内容格式异常」），原始 `l1_error` 只对通过 `adminGuard` 的请求返回；最简做法——公开接口只返回错误分类枚举，原文留管理侧。 |

---

#### 🟡 中严重度（建议本轮修 / 需 PM 裁定）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #A-R3-2 | **`retryable_failed` 展示为「AI 解析失败」，与设计 R2 §5.1 定稿相反**。设计卡片层规格明确把 `l1_status IN ('queued','processing','retryable_failed')` 三态统一归入「处理中占位 → 解析中角标」，「失败降级」只对应 `final_failed`。实现将 `retryable_failed` 单列为 `failed_retry`，卡片显示「AI 解析失败，将自动重试」、抽屉状态条显示「✕ AI 解析失败（将自动重试）」。语义上该状态**仍在待重试队列内**（`schema.ts` L189 `ix_raw_items_ai_queue ... l1_status IN ('queued','retryable_failed')` 即待 claim 队列），对用户呈现「✕ 失败」而实际大概率数分钟后转富展示，构成误导。 | `NewsPage.tsx` `displayState()` / `isFailedState()` | 与定稿设计不一致的用户可见语义偏差；PRD §5.1「处理状态不喧宾夺主」亦倾向弱化。 | 二选一并登记：①按设计回归——`retryable_failed` 并入「解析中」（最小改动：`isFailedState` 只保留 `failed_final`）；②保留三态区分但文案改为「解析重试中」，并走 Change Note 修订设计 §5.1。架构师倾向①。 |
| #A-R3-3 | **监控页「AI 处理概览」六卡口径混用，`completed`/`retryable_failed`/`final_failed` 三卡实为全量口径（含直显类）**。`level-status.ts` L39-46 中只有 `pending`/`processing`/`total_ai` 带 `process_type = 'ai'` 过滤，其余三项是全表 `raw_items` 计数；而 `processor.ts` L100-108 对直显类显式写 `l1_status = 'completed'`，**直显类全部计入 `completed`**。前端把六个数字并列放进「AI 处理概览」Tab 并标注「累计」，运维会读成「AI 类已完成 N 条」。Developer 自测记录的 `completed=154 / total_ai=154` 恰好相等，属测试环境数据巧合，掩盖了该失真。 | `frontend/src/app/pages/MonitoringPage.tsx` AI Tab 卡片数组；根因在 `server/src/api/routes/level-status.ts` L39-46 | 监控口径失真，直接影响运维对 AI 链路健康度的判断（生产直显类占比不低）。 | 后端给三项加 `process_type = 'ai'` filter（新增 `ai_completed`/`ai_retryable_failed`/`ai_final_failed`，保留原字段兼容 SourceDetail 现有调用），前端 AI Tab 改用 AI 口径字段；若本轮不动后端，前端至少把三卡标签改为「全量已完成（含直显）」等明确措辞。 |
| #A-R3-4 | **设计 §5.1 前端改造清单第 3 条「失败降级 + 手动重试按钮（管理员可见）」未实现，且未登记为已知偏差**。设计卡片层规格「失败降级」行明确要求重试入口，后端 `POST /v1/l1-tasks/:task_id/retry`（设计 §3.3）已具备。本轮完全未实现，Developer 登记的 3 条已知偏差也未涵盖。 | `NewsPage.tsx` 失败态卡片 | 定稿设计明文要求项静默缺失——比缺失本身更需纠正的是未登记。注：前端当前只持有 `news_id`，调重试需后端支持按 `raw_item_id` 定位 task，工作量非零。 | 请 PM 裁定：本轮补齐（需后端加 `POST /v1/news/:id/retry-l1` 或按 raw_item_id 查 task）/ 登记为后续迭代项。无论哪种都须写进迭代记录已知偏差表。 |
| #A-R3-5 | **PRD §5.5 四种展示态均要求的「正文（原文）」与「查看原文外链按钮」在抽屉中恒不渲染**。`mapNews` 取 `n.content ?? n.full_content ?? n.body` 和 `n.url ?? n.original_url`，但后端 `newsToOut`/`newsDetailToOut`（`news.ts` L103-166）**从不返回这两类字段**，`processed_news` 表也无正文列，原文只存在于 `raw_items.content`。因此 `fullContent`/`originalUrl` 恒 `undefined`，抽屉「正文」区块与外链按钮永不出现。对富展示态影响有限（有 summary/analysis/context），**对基础展示态影响大——该态无 AI 摘要，占位 `summary` 是唯一实质内容**，PRD §5.5「待 AI / 解析中」「解析失败」两态要求的「正文（原文）」实际未落地。 | `frontend/src/app/pages/NewsPage.tsx` `mapNews`；根因在 `server/src/api/routes/news.ts` `newsDetailToOut` | AC-01「L0 过滤后立即展示（基础展示）」的内容完整度打折。属 v0.6 跨版本遗留（PRD §5.10 改动清单未列此项），非 R3 引入。 | 详情接口补返回 `ri.content` 提取的原文与 `ri.source_item_url`（详情查询已 JOIN `raw_items`，改动量小）；或由 PM 裁定登记为后续迭代项。 |

---

#### 🟢 低严重度（不阻塞）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #A-R3-6 | **`displayState()` 中 `case "pending"` 是不存在的状态值**。库内 `l1_status` 实际枚举为 `not_started / queued / processing / completed / retryable_failed / final_failed / skipped`（`schema.ts` L173 + worker 各写入点），无 `pending`；`api.ts` 新增的 `L1Status` 类型也未含它。冗余分支，无功能影响但误导后续维护者。 | `NewsPage.tsx` `displayState()` | 删除该 case，或统一以 `api.ts` 的 `L1Status` 为唯一枚举来源。 |
| #A-R3-7 | **`displayState()` 兜底为 `rich`（富展示）过于激进**。`default: return "rich"` 意味着任何未知/新增状态值、或 `l1_status` 为 null 的异常数据都会被按富展示渲染（评分取 `score_total ?? importance_score ?? 0`，可能显示「0」分徽章）。database 模式下 `l1_status` 由外部 ai_worker 写入，状态枚举扩展不由 xiaobao 单方控制。 | `NewsPage.tsx` `displayState()` | 反转为白名单：仅 `completed` → `rich`，其余（含未知）→ `pending`。失败方向从「误显富展示」变为「误显解析中」，后者无害得多。 |
| #A-R3-8 | **`isBasicState()` 不含 `direct`，直显类仍渲染标签/实体**。PRD §5.3 明确「基础展示态（待 AI / **直显类** / 处理失败）…不展示标签」，设计 §5.1「基础展示（直显）」规格亦无 tags 项；而 `buildDirectNews`（`processor.ts` L119-140）为直显类写入 `tags: ["金十快讯"] / ["X/Twitter"]` 与 account 实体，故直显卡片/抽屉实际会显示这些标签。同时与 `pending` 态表现不一致——占位记录（`l0-classifier.ts` L161-177）同样写入 tags 却被隐藏。代码注释「数据本身也为空」与实况不符。 | `NewsPage.tsx` `isBasicState()` + 卡片/抽屉标签块注释 | 架构师观点：保留直显来源标签更贴近 PRD §5.3 另一句「与当前生产 X 直显的展示形态一致」，实现选择可能是对的，但**属 PRD 内部口径冲突，须 PM 裁定并登记**，不宜靠注释默认。裁定后同步修正注释。 |
| #A-R3-9 | **新增契约类型缺乏可校验性**。`api.ts` 的 `NewsOut` 带 `[key: string]: unknown` 索引签名，任意属性访问均合法，类型约束基本失效；`NewsPage.mapNews(n: any)` 也未消费该类型；且 `frontend/` 无 `tsconfig.json`、`package.json` build 脚本仅 `vite build`（esbuild 转译，不做类型检查），新增类型在任何环节都不会被校验。**属既有基础设施状态、非 R3 引入**，登记为观察项。 | `frontend/src/app/lib/api.ts` L162-193；`frontend/package.json` L6-9 | 本轮不动。建议后续迭代补 `tsconfig.json` + `tsc --noEmit` 到 build 前置，届时再收紧 `NewsOut` 索引签名——否则「契约类型」只是注释。 |

---

#### 📋 Developer 登记的 3 条已知偏差 · 架构师裁定

| Developer 登记项 | 架构师意见 |
|------------------|-----------|
| PRD §5.7「今日成功/今日失败」→ 实现为全量累计口径，前端标注「累计」 | ✅ **同意留后续迭代**（设计 §3.2 SQL 定稿即全量口径，实现无偏差）。但请连同 #A-R3-3 一并处理——比「今日 vs 累计」更要紧的是这三项**混入了直显类**。 |
| PRD §5.11 language 语言标签未实现（依赖 #PM-IMPL-3 已降级的后端语言检测） | ✅ **同意**。前置依赖已在实现 R1 显式降级，前端无法单方落地，链路一致。 |
| 待解析态用「待解析」角标（PRD §5.4 表仅列「解析中」，按 AC-04 从严实现） | ✅ **同意，实现优于文档**。AC-04 要求区分待解析/解析中，§5.4 表格是遗漏；建议 PM 顺手把 §5.4 表格补一行，避免下次 Review 又被当作偏差提。 |

---

#### ✅ 已正确实现的部分（架构层面确认）

| PRD / 设计要求 | 实现位置 | 验证结果 |
|----------------|----------|----------|
| 设计 §5.1 卡片层：富展示态才显示评分徽章 | `NewsPage.tsx` `st === "rich"` 条件渲染 | ✅ 基础态/直显态均不显示评分 |
| PRD §5.4 卡片角标：解析中蓝色系 + 脉冲动画 | `NewsPage.tsx` `animate-pulse` + `bg-blue-100` | ✅ 符合视觉约束，未占用核心内容区 |
| PRD §5.4 失败态卡片底部低调小字 + hover 看原因 | `NewsPage.tsx` 失败态块 `title` 属性 | ✅ 形态符合（内容安全性见 #A-R3-1） |
| PRD §5.6 抽屉状态条 + 直显类不展示状态条 | `NewsPage.tsx` `selSt !== "direct"` 门控 | ✅ 三分支（✓/⏳/✕）齐全 |
| PRD §5.5 四维评分明细（AD-05 timeliness/impact/confidence/clarity） | `NewsPage.tsx` `DIMENSION_LABELS` + 条形图 | ✅ key 映射与 ai 侧契约一致；非法值 `Number.isFinite` 过滤，宽度 clamp 到 0-10 |
| PRD §5.5 AI 分析 / 背景补全，仅富展示态 | `NewsPage.tsx` `selSt === "rich"` 门控 | ✅ `context` jsonb 元素做了字符串/对象归一化，规避 React 渲染对象崩溃 |
| 设计 §3.2 统计接口复用（不新增 `/v1/admin/ai-stats`） | `api.ts` `getGlobalLevelStatusCounts()` → `/v1/global-level-status-counts` | ✅ 路径与 `level-status.ts` 注册一致，未新增接口 |
| PRD §5.7 监控页新增 Tab（不新增页面/路由） | `MonitoringPage.tsx` `mainTab` 扩展为三态 | ✅ 符合设计 §5.3「无新路由 / 无新页面」 |
| PRD §5.9 AI 统计不可用时的错误态 | `MonitoringPage.tsx` `aiError` 分支 + 重试按钮 | ✅ 有显式错误态与重试入口 |
| PRD §5.10 AppSidebar AI 指标 + 故障隔离 | `AppSidebar.tsx` `getGlobalLevelStatusCounts().catch(() => null)` | ✅ AI 统计失败静默降级，不影响基础四项（符合 AC-06 精神） |
| 详情异步回填不覆盖列表已知分层字段 | `NewsPage.tsx` `openNews()` 合并逻辑 | ✅ `processType/l1Status/l1Error` 做了 `?? news.*` 兜底，规避详情缺字段时展示态回退 |
| 后端 `newsDetailToOut` 补 `process_type` | `news.ts` L156 | ✅ 抽屉直显/AI 分层判断的必要补齐，改动最小且未动查询结构 |
| 设计 §3.1 方案①（占位 processed_news）未被本轮改动破坏 | `news.ts` L62-76 列表查询 | ✅ 仍为 `FROM processed_news JOIN news_positions`，未引入查询结构变更 |

---

#### 📋 Review 说明

- **本轮 Review 独立完成**。#A-R3-1 / #A-R3-3 / #A-R3-5 为架构师独立发现，均经代码事实核查（`admin-guard.ts` 鉴权范围、`processor.ts` 直显类写 `l1_status='completed'`、`newsDetailToOut` 返回字段清单），非文档推断。
- **与 DevOps R3 的关系**：DevOps ✅通过（部署方式/依赖/迁移/回滚零阻塞）与本轮 ⚠️有条件通过不冲突——DevOps 审「怎么发」，本轮审「发出去的接口暴露了什么、展示语义是否符合定稿」。#A-R3-1 建议纳入部署前 verify 清单。
- **未覆盖**：视觉细节与文案观感（PM 域）、dist 发布与 nginx（DevOps 域已覆盖）、Developer 浏览器自测截图的真实性（本轮未复现浏览器自测，只复跑 `tsc --noEmit` + `vite build`）。
- **给 PM 的提示**：需裁定 4 项——#A-R3-2 展示口径回归设计还是修订设计、#A-R3-4 重试按钮补齐还是延期、#A-R3-5 正文/外链补齐还是延期、#A-R3-8 直显类标签展示口径（PRD §5.3 内部两句自相冲突，须 PM 收口）。

### PM R3 Review（实现阶段 · 前端展示层，2026-07-25）

**结论**：⚠️ **有条件通过**（1 条 PM 独立发现低严重度 + 对 Architect 提请的 4 项裁定 + 同批修复清单）

**角色**：PM（产品经理）
**Review 范围**：R3 前端展示层实现（commit `0c733c5`）对齐 PRD R2 §5.3-§5.11 的产品验收；本地 dev（代理测试后端真实数据）可视化验收；不重复 Architect 已覆盖的架构/安全边界与 DevOps 已覆盖的部署面。

---

#### 可视化验收结果（本地 dev 代理测试后端，真实数据）

| PRD 要求 | 验收方式 | 结果 |
|----------|----------|------|
| §5.10 AppSidebar AI 指标 | 浏览器实测 | ✅ 侧栏出现「AI 待处理 0 / AI 处理中 0」，与 `/v1/global-level-status-counts` 返回一致 |
| §5.6 抽屉状态条 | 浏览器实测 | ✅ 富展示态显示「✓ L0 通过 → ✓ AI 解析完成」，直显类不展示（代码门控确认） |
| §5.7 监控页 AI 处理概览 Tab | 浏览器实测 | ✅ 六卡渲染（待处理 0/处理中 0/已完成 154/可重试失败 0/最终失败 0/AI 类总数 154）+ 口径说明文案（口径失真问题见 Architect #A-R3-3，PM 确认属实） |
| §5.5 四维评分/AI 分析/背景补全 | 浏览器实测 + API 数据核查 | ⚠️ **无法端到端验证** — 测试环境 23 条新闻全部 `l1_status=completed` 但 `score_dimensions/analysis/context` 全空（v0.6 HTTP 时代存量数据），字段要等 ai 侧 REQ-003 worker 上线才产出。代码实现经 Architect 确认正确（`DIMENSION_LABELS` 对齐 AD-05 + 条件渲染 + 归一化防御），条件渲染在无数据时正确隐藏区块 ✅ |
| §5.4 卡片状态徽章（待解析/解析中/失败态） | 代码核查 | ⚠️ 同上,真实数据无 pending/processing/failed 态可视验证;实现逻辑核查通过,依赖 Developer 自测声明 |
| 富展示评分徽章/摘要/标签 | 浏览器实测 | ✅ 正常（与现生产形态一致） |

#### 🟢 PM 独立发现（低严重度）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #PM-R3-1 | **`api.ts` 四维评分 TS 类型 key 名与 AD-05 契约不符**。类型注释/定义写 `importance / relevance / timeliness / credibility`，PRD AD-05 与后端实际为 `timeliness / impact / confidence / clarity`（4 个错 3 个）。运行时不受影响（`NewsPage` 用 `Object.entries` 遍历 + 自己的 `DIMENSION_LABELS` 正确映射），但类型契约错误会误导后续开发按错误 key 取值（配合 #A-R3-9「类型不被校验」，错误会静默）。 | `frontend/src/app/lib/api.ts` `score_dimensions` 类型定义 | 类型 key 改为 AD-05 四维；一行级修改，纳入同批修复。 |

#### 📋 对 Architect 提请 4 项的 PM 裁定

| 编号 | Architect 提请 | PM 裁定 | 理由 |
|------|----------------|---------|------|
| #A-R3-2 | `retryable_failed` 展示为「失败」vs 设计定稿归入「解析中」 | **采纳方案①：回归设计** — `retryable_failed` 并入「解析中」表现（`isFailedState` 仅保留 `final_failed`），文案不提「失败」 | 该状态仍在待 claim 队列,大概率数分钟后转富展示;对用户呈现「✕ 失败」构成误导,违背 PRD §5.1「处理状态不喧宾夺主」。用户无需感知重试机制。 |
| #A-R3-4 | 失败降级 + 手动重试按钮（管理员可见）未实现且未登记 | **登记为已知偏差,延期至 ai worker 上线后的迭代** | 需后端新增按 news_id 定位 task 的接口,工作量非零;且 ai 侧 REQ-003 worker 未上线,生产尚不存在失败态,重试按钮当前无使用场景。此偏差本表即为登记。 |
| #A-R3-5 | 抽屉「正文（原文）」与「查看原文外链」恒不渲染（后端不返回字段） | **纳入同批修复** — 详情接口补返回 `ri.content` 原文与源链接（详情查询已 JOIN `raw_items`,改动量小） | 基础展示态占位 summary 是唯一实质内容,正文/外链缺失让 AC-01 内容完整度打折;趁 #A-R3-1 必修同批处理,避免单独再开修复轮。 |
| #A-R3-8 | 直显类标签展示口径（PRD §5.3 两句自相冲突） | **收口：保留直显类来源标签** — §5.3「基础展示不展示标签」指 **AI 语义标签（tagsV2）**;直显类的「金十快讯 / X/Twitter」是**来源标识**,保留符合「与现生产直显形态一致」且有信息价值。实现选择正确,同步修正代码注释即可。PRD 措辞澄清延后走轻量勘误,本 Review 记录先行作准。 | 口径冲突由 PM 收口:两句分别约束不同对象,非真冲突。 |

#### 📋 Developer 3 条已知偏差 · PM 意见

与 Architect 裁定一致：①「累计口径」同意留后续迭代,但 #A-R3-3 直显类混入 AI 统计需修（见下方修复清单）;②language 依赖已降级,同意;③「待解析」角标实现优于文档,同意,PRD §5.4 表格补行列入 PRD 轻量勘误待办。

#### ✅ 通过条件（同批修复清单,部署前完成）

1. **#A-R3-1（高,Architect）**：`l1_error` 原文只对 admin 返回,公开接口改错误分类文案 — **部署前必修**
2. **#A-R3-2（中）**：`retryable_failed` 归入「解析中」表现（按 PM 裁定方案①,10 行内）
3. **#A-R3-3（中,Architect）**：监控 AI Tab 三卡加 `process_type='ai'` 过滤（或前端明确标注全量口径,Developer 择一,PM 倾向后端加 AI 口径字段）
4. **#A-R3-5（中）**：详情接口补正文/源链接,抽屉渲染
5. **#PM-R3-1（低）**：api.ts 四维类型 key 对齐 AD-05
6. **#A-R3-6/7（低,建议顺手）**：删除 `pending` 幽灵分支 + `displayState` 兜底反转为白名单

#### 📋 Review 说明

- 三方 R3 Review 已收齐：DevOps ✅通过 / Architect ⚠️有条件通过 / PM ⚠️有条件通过 → **R3 不定稿,进入 Developer 修复轮（R4）**,修复后 PM + Architect 复核（DevOps 零部署阻塞维持,如修复涉及部署面再邀）。
- 富展示完整效果（四维/分析/背景）的端到端验证依赖 ai 侧 REQ-003 worker 上线,**不构成 xiaobao 侧阻塞**,但迭代关闭检查时须在遗留项中写明。
- 本次 Review 期间创建了本地辅助文件 `frontend/.env.local`（代理目标,gitignore 内）与 `.claude/launch.json`（dev server 启动配置,未提交）,均不属产出物。

### Architect R4 复核（实现阶段 · R3 同批修复，2026-07-26）

**结论**：✅ **通过**（R3 六项修复全部关闭；新增 2 中 3 低，均不阻塞定稿，其中 2 项列为部署前核实）

**角色**：Architect（架构师）
**Review 范围**：commit `5ab883f`（base `0c733c5`）对三方 R3 同批修复清单的关闭验证 + 修复本身是否引入新的架构 / 安全 / 数据流问题
**独立验证**：`server npx tsc --noEmit` 0 错误 ✅ / `npx vitest run` **61/61 通过**（R2 时 55，本轮新增 6 条 `news-public-error`）✅ / `frontend npm run build` 通过 ✅

---

#### R3 意见关闭验证（6/6 关闭）

| 编号 | 严重度 | 关闭验证 |
|------|--------|----------|
| #A-R3-1 | 🔴 高 | ✅ **关闭**。`publicL1Error()` 四个分支**全部返回常量文案**，无任何位置把 `raw` 拼进返回值——这是本条的关键，逐分支核对通过。`isAdminReq()` 在 `config.adminToken` 未配置时返回 `false` → 降级为「全部归一化」，失败方向指向安全侧 ✅。单测含「归一化结果不包含原文内部信息」断言（对 IP / 主机名 / 路径三项显式 `not.toContain`），Developer 记录的红灯（`ETIMEDOUT` 不含 `timeout` 子串）是真实的分类漏洞，测试先抓到再补 `etimedout` 匹配，属有效 TDD 而非补写绿灯测试。 |
| #A-R3-2 | 🟡 中 | ✅ **关闭**（按 PM 裁定方案①）。`failed_retry` 状态整体删除，`retryable_failed` 并入 `processing`；卡片角标、卡片底部小字、抽屉状态条**三处**失败字样均已清理，无残留。 |
| #A-R3-3 | 🟡 中 | ✅ **关闭**。`level-status.ts` 新增 `ai_completed`/`ai_retryable_failed`/`ai_final_failed`（均带 `process_type='ai'` filter），**保留原全量字段**——`SourceDetail` 等既有调用不受影响，向后兼容处理正确。前端 AI Tab 改用 AI 口径 + 口径说明文案同步更新。 |
| #A-R3-5 | 🟡 中 | ✅ **关闭**。详情查询补 `ri.content AS raw_content` + `ri.source_item_url`（复用既有 JOIN，未改查询结构）；`rawContentText()` 字段优先级 `text→content→summary→introduction` 覆盖 x_twitter / rss / jin10 三类源。抽屉「正文」块与 Footer「查看原文」按钮现可渲染。**去重逻辑核对通过**：x_twitter 占位 `summary` 取 `content.text` 全文（`l0-classifier.ts` L205-214，不截断），与 `rawContentText` 同源同值 → `fullContent !== summary` 判定成立，不会出现两段重复正文；富展示态 summary 是 AI 中文摘要、正文是原文，两段并存符合 PRD §5.5。 |
| #A-R3-6/7 | 🟢 低 | ✅ **关闭**。`case "pending"` 幽灵分支已删；`displayState` 反转为白名单（仅 `completed` → `rich`），未知状态落 `pending`，旧数据兜底条件收敛为 `l1Status == null && processType == null`。 |
| #A-R3-8 | 🟢 低 | ✅ **关闭**。注释按 PM 裁定改为「基础展示态不展示 AI 语义标签；直显类保留来源标识」，与实况一致，原「数据本身也为空」的错误描述已消除。 |
| #PM-R3-1 | 🟢 低 | ✅ **独立核实：Developer 的「不成立」判断正确**。`git show 0c733c5:frontend/src/app/lib/api.ts` L188 原注释即 `timeliness/impact/confidence/clarity`，不存在 PM 所述的 `importance/relevance/credibility`，指控不成立。R4 按意见**意图**改为显式 `ScoreDimensions` 接口（四个 key 类型可见）是合理承接，比原样保留更好。 |

---

#### 🟡 中严重度（新增，不阻塞定稿，列为部署前核实）

| 编号 | 问题 | 位置 | 影响 | 修复方向 |
|------|------|------|------|----------|
| #A-R4-1 | **响应内容随请求头变化，但未设缓存控制头**。`GET /v1/news`(`:id`) 现在对 admin 与公开请求返回**不同的 `l1_error`**，而响应未带 `Cache-Control: private` / `no-store`，也未声明 `Vary: X-Admin-Token`。仓库内无 nginx 配置（服务器手工维护），无法静态确认是否启用 `proxy_cache`。**若链路上存在任何中间缓存，管理员请求的原文响应可能被缓存后投给公开用户，使 #A-R3-1 的修复整体失效**。 | `server/src/api/routes/news.ts` 两个 handler | 修复的有效性依赖部署环境事实，不能只看代码。 | 二选一：① DevOps 核实生产/测试 nginx 无 `proxy_cache`（确认后本条关闭，记录留痕）；② 保险做法——两个 handler 加 `reply.header("Cache-Control", "private, no-store")`（一行，无副作用）。架构师倾向②，成本比核实还低。 |
| #A-R4-2 | **`isAdminReq()` 与 `adminGuard` 的鉴权判定分叉**。`adminGuard`（`admin-guard.ts` L30-50）是 token +（可选）IP 白名单双因子，受 `ADMIN_REQUIRE_BOTH` 控制（`config.ts` L61，默认 `false`）；`isAdminReq()` **只验 token、不看 IP、不看 `adminRequireBoth`**。默认配置下两者等价，但**若生产启用了 `ADMIN_REQUIRE_BOTH=true`，`/v1/news` 的原文返回就是一条不受 IP 约束的旁路**。更长期的问题是同一套鉴权语义被两处独立实现，后续改一处就会漂移。 | `server/src/api/routes/news.ts` `isAdminReq()` | 安全判定重复实现；实际暴露程度取决于生产 env。 | 抽公共 `isAdminAuthenticated(req)` 放 `admin-guard.ts`，`adminGuard` 与 `news.ts` 共用；**并请 DevOps 核实生产 `.env` 的 `ADMIN_REQUIRE_BOTH` 实际取值**——若为 `true`，本条升级为部署前必修。 |

---

#### 🟢 低严重度（不阻塞，建议后续处理）

| 编号 | 问题 | 位置 | 修复方向 |
|------|------|------|----------|
| #A-R4-3 | **外链 `href` 未做协议白名单**。`original_url` 取自 `raw_items.source_item_url`，是**抓取来的第三方数据**，直接进 `<a href={selectedNews.originalUrl}>`。React 对 `javascript:` 协议 URL 只告警不阻断，被投毒的 RSS `link` 字段理论上可构成点击型 XSS。R3 时该字段恒 `undefined`，**本轮是这条渲染路径首次被真正激活**。 | `NewsPage.tsx` Footer 外链 | 渲染前校验 `/^https?:\/\//i`，不匹配则不渲染按钮。`target="_blank"` + `rel="noopener noreferrer"` 已正确设置 ✅。 |
| #A-R4-4 | **白名单兜底对存量数据的影响需一条 SQL 确认**。新兜底下 `process_type` 非 null 且 `l1_status ∉ {completed, final_failed, processing, retryable_failed, queued, not_started}`、或 `l1_status='skipped'` 的记录会落到「待解析」。v0.6.1 迁移已把存量回填为 `process_type='ai'`（生产 637 条），这些老数据**不会再被 `l0_classify` 调度**，若其中有已进列表（有 `processed_news` + `news_positions`）但 `l1_status ≠ 'completed'` 的，前端会显示**永不消失的「待解析」角标**。 | `NewsPage.tsx` `displayState()` 兜底分支 | 部署前跑一条 verify SQL 确认计数为 0：`SELECT ri.l1_status, count(*) FROM processed_news pn JOIN raw_items ri ON ri.id = pn.raw_item_id JOIN news_positions np ON np.news_id = pn.id WHERE ri.process_type = 'ai' AND ri.l1_status <> 'completed' GROUP BY 1;` 非 0 则需数据订正或调整兜底。 |
| #A-R4-5 | **`ai_* ?? 全量` 兜底与新文案组合会产生错误标注**。前端 `aiCounts.ai_completed ?? aiCounts.completed` 在后端未部署新版本（或后端回滚）时退回全量口径数字，但文案已固定写「不含直显类」→ **标注与数据不符，回到 #A-R3-3 的失真且更隐蔽**。`deploy.sh` 前后端同批发布，实际风险低。 | `MonitoringPage.tsx` AI Tab | 去掉 `??` 兜底（字段缺失显示 `—`），或让文案随兜底切换。二选一即可。 |

---

#### 📋 Review 说明

- **本轮为 R3「有条件通过」后的条件闭环复核，非第四轮评审博弈**。按 `standard-iteration-quick.md` §9-10「R3 仍未通过升级阻塞交用户决策,不进 R4」的字面口径，本轮 R4 已越线；实质上 R3 三方结论是 DevOps ✅ / Architect ⚠️ / PM ⚠️，修复清单是**已达成一致的确定项**而非未收敛的争议，故按修复轮处理。**提请 Owner 知悉**：若 R4 后仍需改动，建议走 Change Note，不再开 R5。
- **新增 5 条均为 R4 修复本身引入或首次激活的问题**，不是 R3 遗留：#A-R4-1/#A-R4-2 随 admin 差异化返回引入，#A-R4-3 随 `original_url` 首次有值而激活，#A-R4-4 随兜底反转引入，#A-R4-5 随 `??` 兼容写法引入。此为「修复带出新面」的正常现象，不影响 R3 六项关闭结论。
- **给 DevOps 的部署前核实清单（2 项，均为一条命令级）**：① nginx 是否启用 `proxy_cache`（#A-R4-1）② 生产 `.env` 的 `ADMIN_REQUIRE_BOTH` 取值（#A-R4-2，若 `true` 则 #A-R4-2 升级为必修）③ 附带跑 #A-R4-4 的 verify SQL。
- **未覆盖**：浏览器可视化复现（本轮只做代码核验 + `tsc`/`vitest`/`build` 复跑）；富展示完整效果的端到端验证依赖 ai 侧 REQ-003 worker 上线，PM R3 已记录为不构成 xiaobao 侧阻塞。

### PM R4 复核（实现阶段 · 三方 R3 同批修复，2026-07-26）

**结论**：✅ **通过**（同批修复清单 6 项全部核验关闭；#PM-R3-1 确认系 PM 误报，公开撤回）

**角色**：PM（产品经理）
**复核方式**：逐项 diff 核查（commit `5ab883f`）+ 复跑单测/tsc + 测试环境（已部署 R4）API 与浏览器实测（真实 pending 队列数据 5 条）

#### 逐项核验

| 编号 | 核验方式 | 结果 |
|------|----------|------|
| #A-R3-1（高） | diff：`isAdminReq` + `publicL1Error` 归一化，列表/详情双改；单测 6/6 复跑通过（含「归一化结果不含内部 IP/主机名」断言）；测试环境无 token 请求 `l1_error` 归一化正常 | ✅ 关闭 |
| #A-R3-2（中） | diff：`failed_retry` 态删除，`retryable_failed` → 「解析中」；浏览器实测全页无「解析失败」字样 | ✅ 关闭（按 PM 裁定方案①） |
| #A-R3-3（中） | diff：`level-status.ts` 新增 `ai_completed/ai_retryable_failed/ai_final_failed`（AI 口径 FILTER），保留旧字段兼容；测试环境 API 实测三字段返回正确（ai_completed 149 / pending 5 与造数一致） | ✅ 关闭 |
| #A-R3-5（中） | diff：详情 SELECT 补 `ri.content`/`ri.source_item_url`，`rawContentText` 提取器覆盖 x_twitter/rss/jin10_flash 字段优先级；API 实测 `content` 返回推文原文、`original_url` 为真实 x.com 链接；浏览器实测抽屉「正文」「查看原文」渲染 | ✅ 关闭 |
| #A-R3-6/7（低） | diff：`pending` 幽灵 case 删除；`displayState` 白名单兜底（仅 `completed`→富展示，未知→待解析），且对 `l1Status`/`processType` 双 null 的 v0.5/v0.6 存量保持富展示——边界处理比原建议更周到 | ✅ 关闭 |
| #A-R3-8 | 注释按 PM 裁定修正（直显类保留来源标识） | ✅ 关闭 |
| #PM-R3-1（低） | **PM 误报，撤回**：`git show 0c733c5:api.ts` 定案——R3 版本注释本为 AD-05 四维，从不存在错误 key。根因是 R3 会话一轮被污染的工具输出被 PM 残留引用（已记入 `pm-corrections.md`）。Developer 按意见意图将类型显式化为 `ScoreDimensions` 接口，属改进 | ✅ 撤回 + 改进采纳 |

#### 可视化验收（测试环境真实数据）

- 侧栏「AI 待处理 5」= R-4 造数真实队列 ✅
- 财经空间 5 条「待解析」卡片：角标 ✅ / 无评分徽章 ✅ / 无 AI 语义标签 ✅
- 待解析抽屉：状态条「✓ L0 通过」+ 待解析提示 + **正文（原文）** + **查看原文** ✅

#### 复核说明

- R3 时无法可视验证的 pending 基础展示态，本轮借 REQ-003 R-4 造数的真实队列数据完成实测闭环。
- 富展示完整效果（四维/分析/背景）仍待 ai 侧 worker 上线，维持 R3 结论：不构成 xiaobao 阻塞，迭代关闭时记遗留。
- 下一步：Architect 复核 → DevOps 生产部署 → PM 重新执行迭代关闭检查。

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|
| v0.6.1 标准迭代启动 + PRD 范围讨论 | — | 2026-07-04 PM 创建 v0.6.1.md + 与 Owner 讨论范围 + 5 个碰撞点 |
| v0.6.1 PRD R1 产出 | — | 2026-07-05 PM 产出 v0.6.1-prd.md R1 |
| v0.6.1 PRD R1 更新 | — | 2026-07-12 PM 更新 PRD：去掉翻译层，改为两层入库 + 5 态状态机 + 前端三层展示 |
| v0.6.1 PRD R1 Review 完成 | — | 2026-07-12 Architect/Developer/DevOps 三方完成 R1 Review，共 21 条意见（7高/8中/6低） |
| v0.6.1 PRD R2 产出 | — | 2026-07-12 PM 完成 R2 修改：21/21 条全部处理，新增 AD-01~05 架构决策 + 部署协调 + 环境变量增量 + 回滚边界 + 运维事项 + 前端改动清单补全 |
| v0.6.1 PRD R2 定稿 | — | 2026-07-12 Architect/Developer/DevOps 三方 R2 复审全部通过，PRD 定稿 |
| v0.6.1 设计 R1 产出 | — | 2026-07-12 Architect 产出 v0.6.1-design.md R1（5 ADR + GRANT + 触发器 + 迁移方案） |
| v0.6.1 设计 R1 Review 完成 | — | 2026-07-12 PM 🟡有意见（7条）/ Developer ❌需修改（14条）/ DevOps ❌需修改（10条），共 28 条 |
| v0.6.1 设计 R2 产出 | — | 2026-07-12 Architect 完成 R2 修改：28/28 条全部关闭（SECURITY DEFINER + 占位 processed_news + AI_INTEGRATION_MODE 对齐 + 人工 SQL + 运维告警 + dry-check） |
| v0.6.1 设计 R2 PM 复审 | — | 2026-07-12 PM ✅通过（7/7 条全部关闭），Developer ✅通过，待 DevOps 复审 |
| v0.6.1 开发完成（R1） | 469cd9f | 2026-07-12 Developer 完成全部代码实现，55/55 单元测试通过 |
| v0.6.1 实现 R1 Review 完成 | — | 2026-07-14 PM ❌需修改（1高/4低）/ Architect ❌需修改（1高/3低）/ DevOps ❌需修改（2高/4中/2低），共 13 条 |
| v0.6.1 实现 R2 修复 | 12e2d46 | 2026-07-14 Developer 完成 R2 修复（占位 processed_news 移至正确位置 + deploy.sh 迁移步骤 + .env.example 同步 + 默认值对齐 + systemd 模板修正） |
| v0.6.1 实现 R2 定稿 | — | 2026-07-14 PM ✅通过 / Architect ✅通过 / DevOps ✅通过（高严重度全关闭），测试/生产环境部署完成 |
| v0.6.1 测试环境部署 | — | 2026-07-12 部署到 test.huiyiyou.cloud，API 验证通过 |
| v0.6.1 生产环境部署 | — | 2026-07-14 部署到 news.huiyiyou.cloud，API 验证通过，数据库迁移完成 |

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：未验收 / 通过 / 打回 / 有条件通过 —
- Developer 自测结论（证据齐备）：✅ TypeScript 编译零错误；✅ 55/55 单元测试通过；✅ 测试环境 API 验证通过（健康检查 200 / 全局统计接口正确 / 新闻列表含 process_type/l1_status）；✅ 生产环境 API 验证通过（健康检查 200 / 全局统计接口正确 / 数据库迁移完成 637 条）；✅ 数据库迁移（DDL + 索引 + 触发器 + 数据回填）在 test/news_test 和 prod/news 均执行成功
- 关闭结论：可关闭 / 不可关闭 / 有条件关闭
- 未关闭原因或延期项：
- 已生成 summary：
- 已更新 `docs/progress/INDEX.md`：
- 已更新当前角色日志：
- 其他角色待补充/确认：
- 已更新知识库：
- 遗留问题下一步：
