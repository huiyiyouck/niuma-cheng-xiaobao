# 牛马程小报 v0.2 — PM 前端 UI 规范

> 本文档由 PM（产品架构师）产出，供架构师在详细设计阶段和开发在实现阶段参考。
> 视觉 mockup 源文件位于 `.superpowers/brainstorm/22710-1779598548/content/` 目录。

## 文档状态
- 整体状态：待Review
- 版本：v0.2
- 日期：2026-05-24
- 关联 PRD：[v0.2-prd.md](v0.2-prd.md)

---

## 1. 全局布局

| 要求 | 说明 |
|------|------|
| 深色顶栏导航 | `background: #1a1a2e; color: #fff;`，左侧品牌名"牛马程小报"，右侧导航 Tab（浏览 / 管理），当前页下划线高亮 |
| 管理页 Tab 导航 | Tab 切换：频道管理 / Source 管理 / 子频道管理 / 系统日志 |

## 2. NewsPage（新闻浏览页）

| 区域 | 视觉要求 |
|------|---------|
| 统计卡片 | 4 张卡片横排，白底圆角（border-radius: 10px），阴影；数字大号加粗，颜色区分：总新闻数 #1a1a2e、今日新增 #2ecc71、活跃 Source #3498db、子频道数 #9b59b6 |
| 频道空间切换 | Pill 标签横排，选中态：深色背景 #1a1a2e 白字，未选中：白底灰色边框 |
| 子频道筛选 | 圆角 Pill（border-radius: 20px），选中态：蓝底 #e8f4fd + 蓝色边框 #3498db + 蓝色文字，未选中：白底灰色边框 |
| 排序控件 | 右对齐，下拉选择，选项：时间倒序 / 评分高→低 / 评分低→高 |
| 新闻卡片 | 白底圆角，左侧 4px 彩色竖线（评分≥8 金 #f39c12、6-8 绿 #2ecc71、<6 蓝 #3498db）；右侧圆形评分徽章（36px, border-radius: 50%），颜色与左侧竖线一致；标题 15px 加粗，摘要 13px #666 |
| 标签 Chip | 彩色半透明背景圆角 Pill（border-radius: 12-14px），11-12px 字体 |
| 空状态 | "暂无新闻，请先在管理页添加信息来源" 居中提示 |
| 详情面板 | 点击卡片后右侧滑入，宽度 400px（max-width: 92vw）；顶部 sticky header"新闻详情"+ 关闭按钮 X；背景列表不可滚动（body scroll lock）；关闭方式：遮罩层 / 关闭按钮 / Esc |
| 面板内容 | 标题 18px 加粗 → 来源+时间+评分（圆形小徽章）→ 摘要（灰底圆角块）→ 核心要点（项目符号列表）→ 标签（Chip）→ 关键实体（按类型分组）→ 原文链接 |
| 面板动画 | Transition: translateX 40px + opacity 0→1, 0.25s |

## 3. SourceManager（Source 管理）

| 区域 | 视觉要求 |
|------|---------|
| 工具栏 | 状态筛选下拉 + 类型筛选下拉 + 「+ 添加 Source」按钮（深色背景 #1a1a2e 白字） |
| Source 列表 | **表格布局**，列：名称 / URL / 类型 / 状态 / 绑定空间 / 操作 |
| 类型标签 | 彩色 Badge：X/Twitter 蓝 #e8f4fd/#3498db，RSS 橙 #fef3e2/#f39c12，HF Papers 紫 #f3e8ff/#8e44ad，未识别灰 |
| 状态标签 | 彩色圆角 Pill：已启用/已验证 绿底绿字，未验证 灰底灰字，错误 红底红字 |
| 操作列 | 文字链接，蓝色（编辑/验证/绑定），红色（删除），竖线分隔 |
| 添加弹窗 | 居中 Modal，字段：名称* / URL* / 类型（显示自动识别结果 + 可手动覆盖下拉）/ 配置 JSON 文本框；底部"取消"+"创建" |

## 4. VerifyDialog（验证弹窗）

| 区域 | 视觉要求 |
|------|---------|
| 头部 | "验证 {Source名称} — 测试抓取" |
| 成功状态 | 顶部绿色提示条"抓取成功 — 共获取 N 条" |
| 预览列表 | 每条：标题（13px 加粗）+ 时间信息，灰底圆角卡片，列表最大高度 300px 可滚动 |
| 底部按钮 | "关闭"（白底灰边）+ "标记为已验证"（绿底 #27ae60 白字） |
| Loading | 弹窗内 spinner，可关闭弹窗取消等待 |

## 5. SubChannelManager（子频道管理）

| 区域 | 视觉要求 |
|------|---------|
| 工具栏 | 频道空间下拉选择器 + 「+ 新建子频道」按钮 |
| 列表项 | 拖拽手柄（:: 图标）+ 序号 + 名称 + 绑定 Source 数 + 操作（重命名/删除） |
| 内联编辑 | 点击重命名 → 当前行变为输入框，绿色"保存"+ 灰色"取消" |
| 新建行 | 列表底部蓝色虚线边框行，输入框 + 排序号 + "添加"按钮 |

## 6. LogViewer（日志查看器）

| 区域 | 视觉要求 |
|------|---------|
| 筛选区 | **Chip 按钮组**（非下拉框）：级别用彩色 Chip（DEBUG 灰、INFO 蓝、WARNING 橙、ERROR 红），来源用 Chip（API / Worker），时间范围下拉，关键字搜索输入框，右对齐自动刷新开关 + 刷新按钮 |
| 日志行 | 等宽字体（monospace），12px；时间戳（灰）→ 级别 Badge（彩色方块，3px 圆角，白字）→ 来源标签 → 消息 |
| 级别 Badge 色 | INFO: #3498db 蓝，WARNING: #f39c12 橙，ERROR: #e74c3c 红，DEBUG: #aaa 灰 |
| 自动刷新 | 默认 10s 间隔，可开关，刷新时追加新条目不重复 |
| 展开详情 | 单条日志可点击展开，显示完整 extra 字段（JSON 格式化） |

## 7. TagChip / EntityBadge

| 组件 | 视觉要求 |
|------|---------|
| TagChip | 圆角 Pill（border-radius: 12-14px），彩色半透明背景，11-12px，可点击 |
| EntityBadge | 按类型分组，每组前有类型标签（人物/组织/产品/技术）；实体名用小方块 Badge（border-radius: 4px）：人物红 #fde8e8/#c0392b、组织蓝 #e8f4fd/#2980b9、产品绿 #e8f8e8/#27ae60、技术紫 #f3e8ff/#8e44ad |

## 8. 视觉参考文件

完整页面 mockup 位于：
- `.superpowers/brainstorm/22710-1779598548/content/all-pages.html` — 全部页面合集
- `.superpowers/brainstorm/22710-1779598548/content/news-browse-v2.html` — 新闻浏览页
- `.superpowers/brainstorm/22710-1779598548/content/admin-sources.html` — Source 管理页
- `.superpowers/brainstorm/22710-1779598548/content/admin-subchannel-logs.html` — 子频道管理 + 日志查看器

架构师和开发在设计和实现阶段应查阅上述 mockup 文件获取完整视觉参考。

---

## Review by 全栈开发 — R1

### 整体评估

UI Spec 覆盖了 PRD 全部 9 个前端需求点，mockup 文件质量高、视觉一致性好。但存在 3 项可实现性冲突和若干细节遗漏——如果带到实现阶段会导致返工或需要反复确认。

---

### 需修改（影响可实现性）

**#1 SubChannelManager 的空间选择器与全局 SpaceSelector 架构冲突（中等）**

§5 给 SubChannelManager 单独加了一个"频道空间下拉选择器"。但当前 AdminPage 架构是：顶部一个全局 SpaceSelector → 选中空间后所有子组件通过 `spaceId` prop 接收。如果 SubChannelManager 内部也有独立空间下拉，两个选择器共存、互相覆盖——用户在 SubChannelManager 里选了"财经"，全局 SpaceSelector 还在"AI 资讯"，其他 Tab（频道管理/Source管理）展示哪个空间的数据？

建议：SubChannelManager 复用全局 SpaceSelector 的选中值，不内嵌独立空间选择器。或明确说明两个选择器的联动关系（谁覆盖谁）。

---

**#2 LogViewer mockup 与 UI Spec 文本矛盾 + 筛选 Chip 多选语义未定义（中等）**

两处不一致：
- §6 表格写了"时间范围下拉"，但 mockup `admin-subchannel-logs.html` 里根本没有时间范围控件——只有级别 Chip + 来源 Chip + 搜索框 + 自动刷新。
- Mockup 中 4 个级别 Chip 全部可以同时高亮选中，来源也是 API+Worker 同时选中。但多选时查询逻辑是取并集（`level IN ('INFO','ERROR')`）还是交集？"同时显示 INFO 和 ERROR"语义合理，但需要有明确定义。

建议：统一 UI Spec 和 mockup（去掉或补上时间范围控件），明确筛选 Chip 单选还是多选、多选时后端查询用 AND 还是 OR。

---

**#3 SourceManager 操作列未区分 Source 状态（中等）**

§3 操作列固定为"编辑 / 验证 / 绑定 / 删除"。但 PRD 3.1.4 的状态机下不同状态可用操作不同：

| Source 状态 | 合理操作 |
|------------|---------|
| unverified | 验证、编辑、删除 |
| verified | 绑定、编辑、删除 |
| active | 编辑、删除 |
| error | 验证（重试）、编辑、删除 |

Mockup 也体现了这一点——error 状态行操作是"重试 | 编辑 | 删除"。但 UI Spec 正文没有区分，按字面实现所有 Source 都会展示"验证/绑定"按钮。

建议：补充操作列按 Source 状态动态展示的规则，或引用 PRD 3.1.4 状态机。

---

### 建议改进（细节补全）

**#4 标签颜色分配策略缺失（轻微）**

§2 标签 Chip "彩色半透明背景"，§7 TagChip "彩色半透明背景"。但一个新闻有 3-5 个标签，颜色如何分配？按内容哈希？按出现顺序轮转？按语义分类？

建议：给出 4-5 色调色板（蓝 `#e8f4fd/#3498db`、橙 `#fef3e2/#f39c12`、绿 `#e8f8e8/#27ae60`、紫 `#f3e8ff/#8e44ad`），明确分配方式（如"按标签首字母哈希循环轮转"）。

---

**#5 颜色体系未集中管理（轻微）**

颜色值散落在 §1-§7 各段，以 `#3498db` 为例出现于：统计数字、子频道 Pill 选中、标签 Chip 蓝、Source 类型 Badge 蓝、级别 Badge INFO、链接。如果调整主题色需改 6 处以上。

建议：在文档末尾增加色彩体系汇总表，列出主色（#1a1a2e）、强调蓝（#3498db）、成功绿（#2ecc71）、警告金（#f39c12）、错误红（#e74c3c）、紫色系（#9b59b6/#8e44ad）及其使用场景。

---

**#6 深色顶栏与 Vue Router 路由模型的关系未说明（轻微）**

§1 定义深色顶栏（品牌名 + "浏览/管理" Tab），但当前前端是 Vue Router 双路由（`/` → NewsPage，`/admin` → AdminPage），没有全局顶栏组件。需说明：
- 顶栏是新增全局 Layout 组件还是页面内实现？
- "浏览/管理" Tab 高亮如何与路由对应？
- 管理页内部 Tab 和顶栏"管理" Tab 是父子还是同级？

---

**#7 空状态 / 加载态 / 错误态覆盖不全（轻微）**

| 场景 | 当前状态 |
|------|---------|
| 统计卡片数据为 0 或加载失败 | 未定义（mockup 硬编码 1,247/32/5/3） |
| SourceManager 列表为空 | 未定义 |
| VerifyDialog 验证失败（status=error） | mockup 只展示成功态 |
| SubChannelManager 列表为空 | 未定义 |
| LogViewer 日志文件不存在 | 未定义 |
| 详情面板摘要为空 | 未定义 |

建议：各区域补充空状态/错误态的简要规范（1-2 句话即可）。

---

**#8 SourceManager 添加弹窗"自动识别 + 手动覆盖"交互流程未定义（轻微）**

§3 添加弹窗"类型（显示自动识别结果 + 可手动覆盖下拉）"。交互有两种理解：
- A：用户先输入 URL → 失焦后自动识别 → 展示结果 → 确认或手动改
- B：输入 URL 同时下方展示"识别中…"→ 完成后回填下拉框

建议：用 2-3 句话明确交互流程。

---

**#9 EntityBadge 未预知类型的降级策略缺失（轻微）**

§7 只定义 4 种实体颜色（人物/组织/产品/技术）。但 `ProcessedNews.entities` 的 type 来自 LLM 输出，可能有 `location`、`event`、`topic` 等未预知值。

建议：补充"未匹配类型降级为灰色"或类似的兜底策略。

---

### PRD 覆盖对照

| PRD 需求 | UI Spec 对应 | 状态 |
|---------|-------------|------|
| 3.3.1 侧边栏详情面板 | §2 详情面板 + 面板内容 + 面板动画 | ✅ |
| 3.3.2 子频道筛选 UI | §2 子频道筛选（Pill + URL 同步） | ✅ |
| 3.3.3 子频道管理 UI | §5 CRUD + 拖拽 + 内联编辑 | ✅ |
| 3.3.4 评分排序 | §2 排序控件（右对齐下拉） | ✅ |
| 3.3.5 标签展示与筛选（可点击触发筛选） | §2 标签 Chip + §7 TagChip | ⚠️ 点击筛选行为未定义 |
| 3.3.6 实体展示（可点击预留） | §2 关键实体 + §7 EntityBadge | ⚠️ 可点击预留未提 |
| 3.3.7 统计真实数据（含空状态） | §2 统计卡片 | ⚠️ 空/0 值展示未定义 |
| 3.3.8 Source 管理 | §3 + §4 表格/弹窗/验证 | ⚠️ 操作列不分状态 |
| 3.4.3 日志查看器（含 from/to 时间） | §6 Chip 筛选 | ⚠️ mockup 缺时间范围 |

9 项全部有覆盖，5 项存在细节缺失。

---

### 全栈开发评审结论

**❌需修改（R1）**，3 项需修改（#1 空间选择器架构冲突、#2 mockup/文本矛盾及多选语义、#3 操作列状态未区分）+ 6 项建议改进（#4-#9）。前 3 项不修正会导致实现阶段返工或反复确认，其余 6 项可在 R2 快速修正。
