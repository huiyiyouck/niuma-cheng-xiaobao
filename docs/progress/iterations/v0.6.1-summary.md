# v0.6.1 迭代归档摘要

- 迭代周期：2026-07-04 ~ 2026-07-27（有条件关闭）
- 目标：AI 中枢接入模式变更（HTTP 同步 → 数据库契约边界异步解耦）+ 两类新闻产品更正（direct/ai 处理类型分流）+ 前端展示分层
- 关闭结论：✅ **有条件关闭**（Owner 验收通过，2026-07-27）

## 交付了什么

**后端 / 数据库契约（核心交付）**：

- `raw_items.process_type`（direct/ai 分流）+ 5 态 L1 状态机（queued/processing/completed/retryable_failed/final_failed）+ 卡死回收
- `ai_worker` 数据库角色：列级 GRANT + REVOKE CREATE + SECURITY DEFINER 触发器（news_positions 自动关联）——共享库权限边界落地
- `l1_ai_process` task 队列（claim 模式，max_attempts=3 + 退避），占位 `processed_news`（AC-01：L0 通过立即可见）
- 公开接口安全：`l1_error` 原文仅 admin 可见，公开请求归一化为分类文案（R4 #A-R3-1）
- 迁移在 test/news_test 与 prod/news 双库执行成功（生产回填 637 条 ai 类型）；生产库 `ai_worker` GRANT 就绪

**前端展示分层（R3 补课交付）**：

- 卡片状态徽章（待解析/解析中角标）+ 直显/AI 类分层；`retryable_failed` 对用户呈现「解析中」不吓人（R4 #A-R3-2）
- 抽屉：状态条（L0→AI 双段）+ 四维评分条形图 + AI 分析 + 背景补全 + 正文（原文）+ 查看原文外链
- 监控页「AI 处理概览」Tab（AI 口径 `ai_*` 字段，与全量口径分离）+ 侧栏 AI 待处理/处理中指标（故障静默降级）

**跨项目（REQ-003）**：

- 契约 `news-l1-db` 从 v1 演进至 v1.3（O-1 score_total 归属 / O-5 枚举 / C-10 tags_v2 / C-7 language / C-2/C-3/C-5 阻塞项全部闭合）
- R-1~R-5 就绪度全交付（GRANT verify / 迁移确认 / 凭据渠道 / 造数脚本 / content jsonb 结构说明）
- ai 侧 v0.2 的 4 条 PRD 阻塞全部解除

## 流程亮点与教训

- 实现阶段走了 **4 轮**（R1 后端 → R2 修复 → R3 前端补课 → R4 三方修复）：R2 定稿后迭代关闭检查抓出「前端展示层整层漏审」——三方 Review 全部盯着后端，PRD「UI 变更轻并入 PM 自审」的安排在实现阶段失效。教训已沉淀知识库。
- PM R3 意见 #PM-R3-1 系误报（被污染工具输出残留引用），R4 经 git 历史定案后公开撤回，记入 pm-corrections。
- Owner 订正过一次流程：R3 验收从「PM 单方」改回「PM/Architect/DevOps 三方 Review」，违规经历记入 developer-corrections。
- 契约两次笔误（score_total 归属 / tags_v2 第五类）均为 PM 起草 DB 契约时凭记忆书写所致，两次都由 ai 侧逐字段核对抓出。教训已沉淀知识库。

## 已知限制（双侧已对齐留痕）

1. **富展示端到端未验**：ai 侧 worker（ai v0.2）未上线，四维评分/AI 分析/背景补全在生产无真实产出数据；前端条件渲染已就绪，ai v0.2 交付后自然点亮。
2. **DB 模式 `context` 大概率恒空**（ai 侧防编造过滤所致，非 bug）——前端条件渲染零副作用，不作为质量问题反馈。
3. **验收分层**：AI 链路真实数据仅 x_twitter 类；rss/jin10_flash 近期不接入 AI 链路，届时另行提报联调。

## 关闭遗留清单（7 项，带归属与触发时机）

| # | 事项 | 归属 | 优先级 | 触发时机 |
|---|------|------|--------|----------|
| 1 | ~~GRANT 追加 3 列~~ ✅ **已完成**（2026-07-27 DevOps，test+prod 双库 + verify，回帖 `f72616d`；契约已升 v1.4） | DevOps | ~~高~~ | 已闭合 |
| 2 | `x-stream-manager` 入库路径适配 v0.6.1（现绕开新链路走旧 processLLM，l1_status 停 not_started → 前端假「待解析」） | Developer | **高（潜伏）** | ✅ 已完成（2026-07-27，commit `744d20a`，见 [ad-hoc 记录](../ad-hoc/2026-07-27-bugfix-xstream-v061-adapt.md)）|
| 3 | `l0-classifier` 置 queued + 建 task 包显式事务（毫秒级黑洞窗口） | Developer | 中 | ✅ 已完成（2026-07-27，同批）|
| 4 | 占位行 `published_at` 写真值（现 NULL 致列表沉底）+ `language` 统一 'zh' | Developer | 中 | ✅ 已完成（2026-07-27，同批）|
| 5 | `score_total` database 路径补算触发点 | ~~PM 决策~~ ✅ **PM 已拍（2026-08-01，契约 v1.9）：轮询补算**（worker tick + `calcScoreTotal` 单一真源）→ Developer 实现 | 中 | ai v0.2 联调启动时实现（与 #7 同批，决策已清不再有前置） |
| 6 | `max_attempts` 双真源订正 | Developer | 低 | ✅ 已完成（2026-07-27，同批：`maxAttemptsForTaskType()` 单一真源 + requeue 以行内值为准）|
| 7 | 手动重试接口支持 `l1_ai_process`（同 R3 #A-R3-4 延期项） | Developer | 低 | ai v0.2 联调时一并 |

另有 Developer 低严重度三项（#A-R4-3 外链协议白名单 / #A-R4-5 `??` 兜底 / isAdminAuthenticated 抽公共）已于 2026-07-27 完成（commit `a3e8e53` + `6d5c0ed`）。

## 关键 Git 节点

| 节点 | commit |
|------|--------|
| 实现 R1（后端全量） | `469cd9f` |
| 实现 R2 修复（占位 processed_news 归位等） | `12e2d46` |
| 实现 R3（前端展示分层补课） | `0c733c5` |
| 实现 R4（三方 Review 同批修复 6 项，含 l1_error 安全归一化） | `5ab883f` |
| 生产 R4 部署（Architect 三项核实全过） | `acf592f`（部署至 `2c20da6`） |
