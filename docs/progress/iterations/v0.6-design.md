# v0.6 设计文档

## 文档状态
- 整体状态：待Review
- Review 轮次：R1
- 关联 PRD：`iterations/v0.6-prd.md`（R2 定稿版）
- 关联 UI 规范：`iterations/v0.6-ui-spec.md`（R2 定稿版）

## Review 计划
- Review 模式：动态 Review
- 影响领域：
  - 架构、数据流、接口：L0/L1 数据模型、Worker 两阶段处理、外部依赖（LLM/搜索/链接读取）、重试机制
  - 实现复杂度、工程成本：前端重构（12+ 新建组件 + 22 迁移组件）、后端 schema 扩展（4 表 DDL）、图标上传存储
  - 验收标准、边界条件：L0 skip 边界、错误分类三档、评分四维公式、图标上传并发/存储异常、v0.5 不回归
  - 部署方式、环境变量、云服务：外部搜索/链接读取/LLM 调用成本与超时、图片上传存储路径、nginx 配置变更
- 本轮指定 Review 方：
  - PM（产品经理）：设计是否对齐 PRD R2 定稿版决策、范围边界是否保持
  - Developer（开发工程师）：schema DDL 可行性、dispatcher 改造对标 tasks 表现有机制、API 契约覆盖率
  - DevOps（运维/部署工程师）：外部依赖 timeout/网络出口、部署机磁盘与日志轮转、nginx 变更加图标上传路径
  - Tester（测试工程师）：L0/L1 状态机可测性、13 种可重试/降级/不可重试错误的可 mock 性、28 AC 映射
- 未指定角色及原因：
  - UI：UI 阶段已完成，设计文档不涉及 UI 决策；前端架构章节（§5）只影响 Developer
- 少于 2 个 Review 方时的用户确认：不适用
- 定稿条件：本轮指定 Review 方全部通过，且无未处理高严重度阻塞问题

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| PM | 设计对齐 PRD R2 定稿版、范围边界保持 | ⚠️ 有条件通过 | R1 |
| Developer | schema DDL、dispatcher 改造、API 契约 | 待 Review | R1 |
| DevOps | timeout/网络出口/nginx/磁盘/日志轮转 | 待 Review | R1 |
| Tester | L0/L1 状态机可测性、错误分类可 mock 性 | 待 Review | R1 |

## 1. 概述

### 1.1 范围

v0.6 在 v0.5 全栈基线上做加法。设计范围对应 PRD R2 三条主线 + 4 项衍生工作：

| 主线 | 设计覆盖 | 不覆盖（PRD §4.2） |
|------|----------|-------------------|
| L0/L1 分层处理 | 数据模型（raw_items + processed_news 扩展）、L0 classifier worker、L1 processor worker（翻译/补全/分析/评分/标签 5 阶段）、错误分类三档重试、库内检索、链接读取、条件式 X/Web 搜索 | L2 事件追踪、反馈/评论、独立详情页、深度/时间线分析 |
| 前端重构 | UI 规范 5 页面（NewsPage / AdminPage / MonitoringPage / SourceDetailPage + 旧路由跳转）、抽屉 6 段式、12+ 新建/22 迁移组件、空间图标上传 4 视觉态 | React 组件直接迁入、Mock 进入生产构建 |
| 空间图标上传 | `channel_spaces.icon` 字段扩展（emoji + url 双承载）、`POST /v1/spaces/:id/icon`、后端持久目录、nginx `/uploads/` location | bytea DB 存储、前端打包目录 |
| 监控/路由 | `/monitoring` 路由 + Tab 切换、旧 `/alerts`/`/logs` 重定向、顶导 3 项 + 角标、AC-32 新增告警 type 枚举（4 种） | 完整原始数据治理后台 |

### 1.2 设计前提

- 本条全部基于 v0.5 设计文档已落地、v0.6 PRD R2 已定稿、v0.6 UI spec R2 已定稿
- 所有 schema 变更走 `drizzle-kit generate` 增量迁移（ADR-001），不复制粘贴 baseline
- `tasks` 表复用为本期 L0/L1 队列，按 `tasks.type` 区分退避策略（不新建表）
- `processed_news` 表不做物理拆分，采用 jsonb 增量扩展（不拆 `processed_news_l1_meta`）

### 1.3 关键设计决策（ADRs）

| 编号 | 决策 | 位置 |
|------|------|------|
| ADR-002 | L0/L1 状态字段落在 `raw_items` 双字段（`l0_status` + `l1_status`）而非 `tasks` 表单字段承载 | §2.1 |
| ADR-003 | `processed_news` 表 jsonb 增量扩展，不拆子表 | §2.2 |
| ADR-004 | L0/L1 复用 `tasks` 表，按 `type` 分支退避策略，不新建任务表 | §4.2 |
| ADR-005 | 图标上传存储：后端持久目录 + nginx 反代静态服务，不存 DB bytea 或对象存储 | §7.1 |

## 2. 数据模型

### 2.1 `raw_items` 扩展（L0/L1 状态字段）

新增列（v0.6 增量，不走 rename）：

```sql
-- 新增列
ALTER TABLE raw_items
  ADD COLUMN l0_status    varchar(30) NOT NULL DEFAULT 'pending',
  ADD COLUMN l0_label     varchar(50),            -- AC-04: normal_candidate / high_priority_candidate / needs_context_candidate
  ADD COLUMN l0_processed_at  timestamptz,
  ADD COLUMN l0_error     text,
  ADD COLUMN l1_status    varchar(30) NOT NULL DEFAULT 'not_started',
  ADD COLUMN l1_error     text,
  ADD COLUMN l1_attempt   integer NOT NULL DEFAULT 0,
  ADD COLUMN l1_next_retry_at  timestamptz,
  ADD COLUMN l1_processed_at  timestamptz;

-- 索引
CREATE INDEX IF NOT EXISTS ix_raw_items_l0_queue
  ON raw_items(l0_status, l1_status, published_at)
  WHERE l0_status = 'passed' AND l1_status = 'not_started';

CREATE INDEX IF NOT EXISTS ix_raw_items_l1_queue
  ON raw_items(l1_status, l1_next_retry_at, published_at)
  WHERE l1_status IN ('queued', 'retryable_failed');
```

**ADR-002**：L0/L1 状态落在 `raw_items` 双字段独立
- 方案 A（采用，推荐）：`raw_items.l0_status` + `raw_items.l1_status` 双字段
  - 优点：L0/L1 状态解耦，L1 重试不影响 L0 历史状态；单行查询无需 JOIN tasks 表
  - 代价：多 2 个索引
- 方案 B（否决）：`tasks` 表单字段承载
  - 理由：tasks 行是任务粒度，L0/L1 是信息粒度；一条 raw_item 产生 1 L0 task + 1 L1 task，L0 task completed 后 raw_item 的 L0 状态丢失，需额外查 tasks 历史才能追溯
- 方案 C（否决）：独立 `processing_states` 表
  - 理由：增加 JOIN，churn 与双字段相当；PRD §2.3 状态机要求 L0/L1 状态"独立且可追溯"，双字段满足

状态枚举映射（PRD §2.3 产品状态机 → schema 枚举值）：

```text
raw_items.l0_status:
  'pending'    ← PRD: L0 pending（原始信息已入库，尚未开始 L0 判断）
  'processing' ← PRD: L0 processing
  'skipped'    ← PRD: L0 skipped（终态）
  'passed'     ← PRD: L0 passed（已准备送入 L1）
  'failed'     ← PRD: L0 failed（终态，非重试失败——设 failed 表示已超过重试上限）
  'retryable'  ← 新增设计层：L0 失败可重试（transfer 图中 L0 failed → L0 pending 用此态标识）

raw_items.l1_status:
  'not_started'   ← PRD: L1 not_started
  'queued'        ← PRD: L1 queued
  'processing'    ← PRD: L1 processing
  'retryable_failed' ← PRD: L1 retryable_failed
  'final_failed'  ← PRD: L1 final_failed（终态）
  'completed'     ← PRD: L1 completed（终态）
```

**额外说明：L0 `retryable` 状态**
- PRD §2.3 状态机表中 L0 只有 `pending/processing/skipped/passed/failed` 5 态，但转移图中 `L0 failed → L0 pending / L0 processing` 暗示 L0 可重试。
- 设计层新增 `retryable` 状态：L0 执行失败但属于可重试错误时 → `l0_status = 'retryable'`，由新的 worker 轮询或 `l1_next_retry_at` 触发重试；达到重试上限后 → `l0_status = 'failed'`（终态）。
- 与 L1 的 `retryable_failed` 字段命名不对称但语义一致：L1 叫 `retryable_failed` 是因为 L1 多一个 `completed` 终态；L0 叫 `retryable` 是因为 L0 只有 `passed / skipped / failed` 三个终态，不需要 `retryable_failed` 区分"等待重试"和"最终失败"。

### 2.2 `processed_news` 扩展（L1 输出结构）

```sql
-- 新增列（全部 jsonb，非破坏性扩展）
ALTER TABLE processed_news
  ADD COLUMN translation     jsonb,   -- { zh: "中文翻译", original?: "原文" }
  ADD COLUMN context         jsonb,   -- ContextParagraph[] see §3.3
  ADD COLUMN analysis        text,    -- AI 分析评价文本
  ADD COLUMN score_total     numeric,   -- 1-10 保留 1 位小数
  ADD COLUMN score_dimensions jsonb,  -- { timeliness: {score, reason}, impact: {score, reason}, confidence: {score, reason}, clarity: {score, reason} }
  ADD COLUMN tags_v2         jsonb;   -- 五类标签结构 { domain: string[], entity: string[], event: string[], content_type: string[], processing: string[] }
```

**ADR-003**：新增字段全部 jsonb 增量扩展，不拆子表
- 方案 A（采用，多 jsonb 列）：`processed_news` 表上直接加 6 列 jsonb/text
  - 理由：v0.5 `processed_news` 已有 `tags / entities / source_refs / bullets` 4 个 jsonb 列，模式成熟；新增列全部是非结构化/半结构化数据，拆子表会增加 SELECT 复杂度
  - 代价：单行可能增长到 10-100KB，但 v0.5 月增长约 3000-15000 行，一年不到 2GB，PG jsonb 可以承受
- 方案 B（否决）：拆 `processed_news_l1_meta` 一对一子表
  - 理由：Developer PRD R1 已推荐（不阻塞观察），但经评估 v0.5 增长量级下拆表增加 JOIN 开销且无实质收益，不拆。
- 字段迁移说明：
  - `importance_score numeric`（v0.5 现存）保留不动；`score_total numeric` 是 v0.6 新增加权计算后的综合分字段；两者在 L0/L1 处理完成前分别为 `0` / `NULL`
  - `tags jsonb`（v0.5 现存，`string[]` 结构）保留不动；`tags_v2 jsonb` 是 v0.6 五类分类结构。v0.5 历史数据兼容：前端在 `tags_v2` 为 NULL 时降级为 `tags` 平铺数组作为 `processing` 类显示
  - `entities jsonb`（v0.5 现存，`any[]` 结构）保留不动；`translation / context / analysis / score_dimensions` 为完全新增字段

### 2.3 `channel_spaces` 扩展（图标上传）

```sql
ALTER TABLE channel_spaces
  ADD COLUMN icon_url    text,      -- 上传图片的相对 URL (/uploads/spaces/{id}/{hash}.{ext})
  ADD COLUMN icon_type   varchar(20) NOT NULL DEFAULT 'emoji';  -- 'emoji' | 'image' | 'fallback'

-- 注：既有 icon(text) 列保留，承载 emoji 文本或默认 📁
-- icon_type 决定前端渲染行为：
--   'emoji'  → 展示 icon 列的 emoji 文本
--   'image'  → 展示 icon_url 路径的 <img>；icon 列的 emoji 文本作为回退
--   'fallback' → 展示默认 🔖
```

三个字段共存规则（UI spec D6 实现）：

| icon_type | icon | icon_url | 前端展示 |
|-----------|------|----------|----------|
| 'emoji'（默认） | '🤖' | NULL | 渲染 emoji 字符 |
| 'emoji'（自定义） | '🚀' | NULL | 渲染 emoji 字符 |
| 'image' | '🤖'（保留回退） | '/uploads/spaces/xxx/abc.png' | `<img>`；加载失败回退 icon |
| 'fallback' | NULL | NULL | 🔖 |

### 2.4 `tasks` type 扩展 + 退避配置化

新增 `tasks.type` 枚举值：

```text
'fetch'               ← v0.5 已有
'process'             ← v0.5 已有
'l0_classify'         ← 新增：L0 判断（单条 raw_item）
'l1_process'          ← 新增：L1 综合处理（翻译/补全/分析/评分/标签）
'l1_retry'            ← 新增：L1 重试（在 `retryable_failed` 重试入队时用）
```

`l0_classify` 和 `l1_process` 共用现有 `tasks.source_id` / `tasks.raw_item_id` 字段（v0.5 `process` 类型已在用）。不新增列。

### 2.5 `alerts` type 扩展（AC-32）

PRD R2 AC-32 要求：外部依赖连续失败、L0/L1 队列长时间无进展、L1 final_failed 数量异常增长时应产生可见告警。

新增 type 枚举值（建议，设计阶段可调整）：

```text
'task_stale'              ← tasks.locked_by 卡住 > TASK_STALE_SECONDS
'external_dep_down'       ← 连续 N 条 L1 因同一外部依赖失败
'task_backlog'            ← raw_items 排队数 > 阈值
'final_failed_surge'      ← 单位时间 final_failed > 阈值
```

v0.5 告警类型 `x_stream_disconnected / fetch_failure / zero_new / circuit_open / fetch_auth_failed` 不受影响。

### 2.6 不新建的表

经评估，以下候选不新建表：

| 候选表 | 否决原因 |
|--------|----------|
| `l1_tasks`（独立任务表） | 复用 `tasks` 表 + `type` 分支退避足以，不增加表数 |
| `processed_news_l1_meta` | jsonb 足够，不增加 JOIN |
| `space_icons`（独立图标表） | 空间 1:1 图标，放 `channel_spaces` 即可 |
| `processing_states`（独立状态表） | 状态在 `raw_items.l0/l1_status` 双字段承载 |
| `external_dep_logs` | 失败信息已进 `tasks.last_error` + 日志行 |

### 2.7 ER 层改动汇总

```
┌───────────────────┐       ┌──────────────────┐       ┌─────────────────────┐
│    raw_items      │       │  processed_news  │       │   channel_spaces    │
├───────────────────┤       ├──────────────────┤       ├─────────────────────┤
│ ... (v0.5 existing)│       │ ... (v0.5 existing) │    │ ... (v0.5 existing) │
│ l0_status   NEW   │       │ translation  NEW │       │ icon_url      NEW  │
│ l0_label    NEW   │       │ context     NEW  │       │ icon_type     NEW  │
│ l0_processed_at   │       │ analysis    NEW  │       └─────────────────────┘
│ l0_error    NEW   │       │ score_total NEW  │
│ l1_status   NEW   │       │ score_dim   NEW  │       ┌─────────────────────┐
│ l1_error    NEW   │       │ tags_v2     NEW  │       │   alerts            │
│ l1_attempt  NEW   │       └──────────────────┘       ├─────────────────────┤
│ l1_next_retry_at  │                                  │ ... (v0.5 existing) │
│ l1_processed_at   │       ┌──────────────────┐       │ type: NEW values    │
└───────────────────┘       │     tasks        │       └─────────────────────┘
        ↑                   ├──────────────────┤
        │                   │ ... (v0.5 existing)│
        └───────────────────│ type: NEW values  │
              (v0.5 FK)     └──────────────────┘
```

## 3. API / 接口契约

### 3.1 设计原则

- 所有新增 endpoint 均走 `fastify` route + Zod `@fastify/type-provider-typebox` 或预定义 schema
- URL 路径使用复数名词，遵循 v0.5 模式（`/v1/news`、`/v1/spaces`）
- 响应 JSON 使用 camelCase（与 v0.5 一致）
- 前端 TS 类型在 `frontend/src/lib/types.ts` 定义，与 API 响应字段 1:1 映射

### 3.2 新增 endpoint

#### `GET /v1/news` 扩展字段

v0.5 现有字段保留。响应新增字段（list item 模式）：

```json
{
  "items": [
    {
      "id": "uuid",
      "title": "string",
      "summary": "string",
      "published_at": "ISO8601",
      "source": { "id": "uuid", "name": "string" },
      "channel_name": "string",
      "score_total": 8.5,
      "tags_v2": {
        "domain": ["AI", "金融"],
        "entity": ["OpenAI", "GPT-5"],
        "event": ["发布"],
        "content_type": ["市场动态"],
        "processing": ["需补充", "高优先"]
      }
    }
  ]
}
```

列表不返回 `translation / context / analysis / score_dimensions`（这些只在详情端返回）。

#### `GET /v1/news/:id`（新增）

UI spec §6.1 `NewsDetail` 完整结构：

```json
{
  "id": "uuid",
  "title": "string",
  "source": { "id": "uuid", "name": "string", "removed": false },
  "channel_name": "string",
  "published_at": "ISO8601",
  "summary": "string",

  "score_total": 8.5,
  "score_dimensions": {
    "timeliness": { "score": 4, "reason": "24 小时内最热" },
    "impact": { "score": 5, "reason": "影响整个 AI 行业格局" },
    "confidence": { "score": 4, "reason": "官方公告，可信度高" },
    "clarity": { "score": 3, "reason": "部分技术细节尚不明确" }
  },

  "translation": { "zh": "中文翻译...", "original": "English original..." },
  "context": [
    {
      "text": "段落内容",
      "confidence": "fact",
      "sources": ["source", "link"]
    }
  ],
  "analysis": "AI 分析全文...",

  "tags_v2": {
    "domain": ["AI"],
    "entity": ["OpenAI"],
    "event": ["发布"],
    "content_type": ["产品公告"],
    "processing": ["已补全"]
  }
}
```

**UI spec §6.1 契约对齐**：
- `source` 对象名从约定的 `source.id` 改为 `source.id` 以匹配 v0.5 现有模式 → 接受
- 分数精度统一 1 位小数 → 接受
- `content` 字段名(UI spec §6.1 用了 `context` ≠ 不冲突, 但 `raw_items.content` 已是 jsonb column 名, API 不用 `content` 避免混淆 → 用 `context` 一致

#### `POST /v1/spaces/:id/icon`（新增）

`multipart/form-data`:
- field name: `file`
- content-type: `image/png | image/jpeg | image/webp`
- max file size: 1MB
- recommended resolution: ≤ 512×512px

Response:

```json
{
  "icon_url": "/uploads/spaces/{space_id}/{hash}.{ext}",
  "icon_type": "image"
}
```

Server 行为：
- 校验 MIME / 文件大小 → 不合法则返回 `400 {"error": "...", "code": "INVALID_FILE"}`
- 写入持久目录：`/var/lib/niuma-news/uploads/spaces/{space_id}/{content_hash}.{ext}`
- 更新 `channel_spaces.icon_url` + `channel_spaces.icon_type = 'image'`
- 替换已有图片文件时先物理删除旧文件（AC-28b）
- 写入失败时保留原值，不损坏已有数据（AC-28a）

#### `DELETE /v1/spaces/:id/icon`（新增）

```json
{
  "success": true,
  "icon_type": "emoji",
  "icon": "🤖"
}
```

Server 行为：
- 物理删除磁盘旧图标文件
- 设置 `channel_spaces.icon_url = NULL` + `icon_type = 'emoji'`
- 如果原 icon 字段也为空，设 `icon_type = 'fallback'`

#### `GET /v1/sources/:id/level-status-counts`（新增）

UI spec §6.4 契约：

```json
{
  "completed": 142,
  "skipped": 38,
  "retryable_failed": 2,
  "final_failed": 1,
  "window_started_at": "2026-06-10T14:00:00Z"
}
```

**数据来源 与 语义映射**（回应 UI R1 #A4）：

每个 count 的 SQL 语义：

```sql
-- completed = L1 completed
SELECT count(*) FROM raw_items
 WHERE source_id = $1
   AND l1_status = 'completed'
   AND (l1_processed_at > now() - interval '24h' OR l1_processed_at IS NOT NULL);

-- skipped = L0 skipped
SELECT count(*) FROM raw_items
 WHERE source_id = $1
   AND l0_status = 'skipped'
   AND (l0_processed_at > now() - interval '24h' OR l0_processed_at IS NOT NULL);

-- retryable_failed = L1 retryable_failed
SELECT count(*) FROM raw_items
 WHERE source_id = $1
   AND l1_status = 'retryable_failed'
   AND (l1_processed_at > now() - interval '24h' OR l1_processed_at IS NOT NULL);

-- final_failed = L1 final_failed（不包括 L0 failed——L0 failed 较少、日志查 L0 error 即可）
SELECT count(*) FROM raw_items
 WHERE source_id = $1
   AND l1_status = 'final_failed'
   AND (l1_processed_at > now() - interval '24h' OR l1_processed_at IS NOT NULL);
```

**为什么按 `l1_processed_at` 而不是 `published_at`**：窗口是"最近处理时间"维度，不是"信息发布"维度。

**为什么 `final_failed` 只包含 L1 不含 L0**：L0 failed 层数量极少（原始数据格式问题才会 L0 failed），归入手动排查场景；L1 final_failed 是"有价值但处理失败"的核心指标。

#### `POST /v1/l1-tasks/:task_id/retry`（新增 AC-31）

```json
{
  "success": true,
  "new_task_id": "uuid",
  "status": "queued"
}
```

Server 行为：
- 接收 `task_id` = 对应 `tasks.id` 且 `tasks.type IN ('l1_process', 'l1_retry')` 且 `tasks.status IN ('failed', 'retryable_failed', 'final_failed')`
- 重置 `raw_items.l1_status = 'queued'`, `raw_items.l1_attempt = 0`, `raw_items.l1_error = NULL`
- 创建新 task（或复用旧 task 行重置 `status='queued', attempt=0, last_error=NULL, run_after=now()`）

#### `GET /v1/global-level-status-counts`（新增 UI spec R2 新增 endpoint）

全局 L0/L1 状态计数，用于监控页全局视图：

```json
{
  "completed": 5230,
  "skipped": 890,
  "retryable_failed": 12,
  "final_failed": 5,
  "window_started_at": "2026-06-10T00:00:00Z"
}
```

SQL 语义与 `GET /v1/sources/:id/level-status-counts` 相同，只是无 `source_id` 过滤。

### 3.3 修改的 endpoint

#### `GET /v1/stats` 统计卡片口径

v0.5 返回值扩展/调整（UI spec D2b）：

```json
{
  "today_completed": 142,       -- 新增，替换 v0.5 'today' 口径：SELECT count(*) FROM processed_news pn JOIN raw_items ri ON ri.id = pn.raw_item_id WHERE ri.l1_status = 'completed' AND ri.l1_processed_at > today
  "total_completed": 5230,      -- 新增，替换 v0.5 'total' 口径
  "enabled_sources": 12,        -- 沿用 v0.5（保持不变）
  "channels": 8                 -- 沿用 v0.5（保持不变）
}
```

#### `GET /v1/alerts`（新增 type 筛选）

v0.5 已有。本期新增：
- `type` 筛选支持新增的 `task_stale / external_dep_down / task_backlog / final_failed_surge`
- 响应 `alerts.type` 枚举值不变（string），仅扩展

### 3.4 前端 TS 类型关键新增

`frontend/src/lib/types.ts` 新增：

```ts
// L0/L1 状态枚举
type L0Status = 'pending' | 'processing' | 'skipped' | 'passed' | 'failed' | 'retryable';
type L1Status = 'not_started' | 'queued' | 'processing' | 'retryable_failed' | 'final_failed' | 'completed';

// 五类标签
interface TagV2 {
  domain: string[];
  entity: string[];
  event: string[];
  content_type: string[];
  processing: string[];
}

// 评分维度
interface ScoreDimension {
  score: number;   // 1-5
  reason: string;  // ≤ 80 chars
}

interface ScoreDimensions {
  timeliness: ScoreDimension;
  impact: ScoreDimension;
  confidence: ScoreDimension;
  clarity: ScoreDimension;
}

// 新闻详情
interface NewsDetail {
  id: string;
  title: string;
  source: { id: string; name: string; removed?: boolean };
  channel_name: string;
  published_at: string;
  summary: string;
  score_total: number;
  score_dimensions?: ScoreDimensions;
  translation?: { zh: string; original?: string };
  context?: ContextParagraph[];
  analysis?: string;
  tags_v2?: TagV2;
}

// 背景补全段落
interface ContextParagraph {
  text: string;
  confidence: 'fact' | 'inference' | 'uncertain';
  sources: Array<'source' | 'link' | 'library' | 'x_search' | 'web_search'>;
}

// L0/L1 计数（适配 `<StatusBadge .status>` 枚举）
type LevelStatus =
  | 'l0_pending' | 'l0_processing' | 'l0_skipped' | 'l0_passed' | 'l0_failed'
  | 'l1_not_started' | 'l1_queued' | 'l1_processing'
  | 'l1_retryable_failed' | 'l1_final_failed' | 'l1_completed';
```

## 4. 核心流程

### 4.1 L0/L1 整体流水线

```text
                    ┌──────────────────┐
                    │ X Stream / Fetch │
                    └────────┬─────────┘
                             │ RAW ITEM INSERTED
                             ▼
                    ┌──────────────────┐
                    │   l0_classify    │  ← dispatcher claimTask(type='l0_classify')
                    └────────┬─────────┘
                             │
                    ┌────────▼────────┐
                    │  L0 判定         │
                    │  ┌─ 规则引擎     │  (空文本/乱码/纯emoji/重复→skipped)
                    │  └─ LLM (可选)   │  (Source 主题无关→LLM 判断)
                    └────────┬─────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
        l0_status      l0_status       l0_status
        ='skipped'     ='passed'       ='retryable'/'failed'
        (终态,         (设置L0标签,      (按重试策略处理)
         不入L1)       入L1队列)
                        │
                        ▼
               ┌──────────────────┐
               │   l1_process      │  ← dispatcher claimTask(type='l1_process')
               └────────┬─────────┘
                        │
               ┌────────▼────────────────────────────┐
               │  L1 综合处理                         │
               │  1. 库内相关新闻检索                   │
               │  2. 链接读取（推文含链接时）            │
               │  3. LLM 判断 needs_context            │
               │  4. X/Web 搜索（needs_context=true）  │
               │  5. LLM 最终生成（全部上下文入 prompt） │
               │     5a. 翻译 / 摘要 / 标题             │
               │     5b. 背景补全（含来源+可信度标签）   │
               │     5c. AI 分析评价                    │
               │     5d. 四维评分 + 综合分计算           │
               │     5e. 五类标签输出                    │
               └────────┬────────────────────────────┘
                        │
              ┌─────────┼───────────┐
              ▼         ▼           ▼
        l1_status   l1_status    l1_status
        ='completed' ='retryable' ='final_failed'
        (进入新闻流)  (等待重试)   (终态)
```

### 4.2 L0 Classifier（新增 Worker 模块）

**文件路径**：`server/src/worker/l0-classifier.ts`

**入参**：`raw_item_id: string`（通过 tasks 表 claim）

**处理逻辑**（PRD R2 AC-03/AC-04）：

```text
1. 读取 raw_items.content + sources.*（identity / domain_tags / attention_level）
2. 规则引擎判定（以下条件若命中→skipped）：
   a. content 文本为空或仅空白字符 → skipped
   b. content.Hash 与已处理的 raw_item.contentHash 重复 → skipped
   c. 纯 emoji（字母+数字占比 < 5%）→ skipped
   d. 纯转发无新增文本（content.retweet && !content.added_text）→ skipped
   e. 抽奖/招聘/无关广告（关键词正则匹配）→ skipped
3. LLM 判定（以下条件若规则引擎不跳过→调 LLM）：
   f. 与 Source 主题明显无关 → skipped
   g. 乱码（U+FFFD 高比例 / 不可猜测模式）→ skipped
4. 通过时：
   - l0_status = 'passed', l0_label = 'normal_candidate' / 'high_priority_candidate' / 'needs_context_candidate'
   - 创建 l1_process task：INSERT INTO tasks(type='l1_process', source_id, raw_item_id, status='queued', ...)
5. 失败（规则引擎 + LLM 都 fail）：
   - 可重试错误 → l0_status = 'retryable', 设 l0_error + 创建 l0_classify task 重试
   - 不可重试错误 → l0_status = 'failed'（终态）, 设 l0_error
6. 更新 l0_processed_at
```

**规则引擎优先**：规则引擎不走的 case（"与 Source 主题无关"需要语义理解）才走 LLM，以减少 LLM 调用量。

**L0 Label 选择策略**（PRD AC-04）：
- `high_priority_candidate`：Source attention_level = 'high' + 推文含关键词 / 高互动指标
- `needs_context_candidate`：推文含链接 / 含项目代号 / 含缩写
- `normal_candidate`：以上都不满足

#### L0 错误分类与重试（对应 PRD §3.2 三档表 + AC-08）

| 错误类型 | 示例 | L0 行为 |
|----------|------|---------|
| 可重试 | LLM 超时/5xx、网络错误 | `l0_status='retryable'`, 创建重试 task；3 次上限 [60s, 300s, 900s] |
| 不可重试 | raw_item 不可解析、source_type 未注册 | `l0_status='failed'`（终态）, 不重试 |
| 降级 | —（L0 无降级，跳过即终态或 passed 即进 L1） | — |

### 4.3 L1 Processor（扩展 v0.5 processor.ts）

**文件路径**：`server/src/worker/l1-processor.ts`（新建，非破坏 processor.ts）

**入参**：`raw_item_id: string`（通过 tasks 表 claim），`source_id: string`

**阶段分解**（5 个子阶段，串行，每个阶段失败按错误类型分类）：

#### 阶段 1：库内相关新闻检索（AC-11）

```text
策略：ILIKE 全文检索（当前处理量 100-500 条/日，不需要向量）
条件：每次必做
输入：raw_item 的标题/摘要关键词
输出：5 条相关 processed_news.title + summary
失败：记录"kb_search 异常"（不阻断——背景补全时基于 Source + LLM 知识继续）
```

SQL（ILKE 基础版）：

```sql
SELECT pn.id, pn.title, pn.summary, pn.published_at
FROM processed_news pn
JOIN raw_items ri ON ri.id = pn.raw_item_id
WHERE ri.source_id != $1   -- 排除当前 Source
  AND (pn.title ILIKE '%' || $2 || '%' OR pn.summary ILIKE '%' || $3 || '%')
  AND pn.published_at > now() - interval '7 days'
ORDER BY pn.importance_score DESC, pn.published_at DESC
LIMIT 5;
```

`$2`, `$3` 从 raw_item 的文本中提取关键词（取前 3 个单词/术语）；关键词提取方法：取 title 中非停用词的前 3 个有含义的 token（英文）或取 2-4 个汉字短语（中文）。

**是否同步执行**：同步。LLM 主调用（阶段 5）需要库内检索结果作为上下文，所以是 sync 的。单次检索耗时 < 50ms，可以接受。

#### 阶段 2：链接读取（AC-12）

```text
条件：推文 content.url / source_item_url 存在
工具：fetch + HTML text extraction（cheerio 或 built-in Node 文本抽取）
超时：10s
失败：记录 error, 标记 `sources: ["link"]` 不存在于该段落的 source 列表
       不阻断 L1（AC-12 明确"不阻塞"）
```

#### 阶段 3：外部搜索（AC-13）

```text
条件：LLM 判断 needs_context=true（阶段 4 内完成判断，合并在 prompt 内，
       由 LLM 在输出的 JSON 中标记 needs_context）
触发：needs_context=true → X 搜索 + Web 搜索
      否则跳过

X 搜索：搜索该 user 最近 5 条推文（使用 X API v2 Recent Search 或抓取式搜索）
Web 搜索：搜索关键词（使用 Tavily / Serper / Google Custom Search 之一，
          设计阶段暂不锁定具体服务，PRD §5 边界表已定义"可选增强，失败不阻断"）

失败：记录 error，不阻断 L1
```

**关于 X 搜索和 Web 搜索的触发条件建议细分**（Architect R2 观察 4）：
- X 搜索补全 Source 自身上下文，在 `needs_context=true && 推文内容涉及该账号历史行为或立场` 时最有价值
- Web 搜索补全外部上下文，在 `needs_context=true && 推文引用外部事件/项目/概念` 时最有价值
- 初步实现可以两个都发（成本低时），第一个版本简化为 needs_context=true 时两个都触发，后续优化

#### 阶段 4：LLM 主调用（AC-05/AC-16/AC-19）

这是 L1 核心产出。使用 v0.5 `llm.ts` 的现有基础设施（`processLLM`）。

**LLM 调用策略**（Developer PRD R1 #D5 关闭方案）：
- **单次调用**：将所有上下文（库内检索结果、链接内容、X/Web 搜索摘要、Source 身份信息、原始推文）拼入一个 prompt，一次返回所有产出
  - 理由：v0.5 路线已稳定；v0.5 同场景使用单次 LLM + `extractFirstJsonObject`，成功率约 90%
  - 风险：JSON 体量大（11+ top-level key）可能增加解析失败率
  - 兜底：按 PRD AC-07/§3.2 "AI 输出连续结构不合法" → 不可重试的错误档位，N=2 次后进入 final_failed

**Prompt 结构**（新增超参数模板，不随代码 hardcode）：

```
你的角色：信息分析助手
输入：
- Source 身份：{source.identity}（{source.domain_tags}）
- 原始推文：{content}
- 库内相关新闻：{kb_results}
- 链接内容：{link_content | "无"}
- 搜索摘要：{search_summary | "无"}

请输出以下 JSON：
{
  "title": "中文标题",
  "summary": "中文摘要（80-200字）",
  "translation": { "zh": "中文翻译", "original": "原文" },
  "context": [
    {
      "text": "一段背景补全文本",
      "confidence": "fact | inference | uncertain",
      "sources": ["source" | "link" | "library" | "x_search" | "web_search"]
    }
  ],
  "analysis": "AI 分析与评价",
  "scores": {
    "timeliness": { "score": 1-5, "reason": "..." },
    "impact": { "score": 1-5, "reason": "..." },
    "confidence": { "score": 1-5, "reason": "..." },
    "clarity": { "score": 1-5, "reason": "..." }
  },
  "tags": {
    "domain": ["标签"],
    "entity": ["标签"],
    "event": ["标签"],
    "content_type": ["标签"],
    "processing": ["标签"]
  },
  "needs_context": true | false
}
```

**综合分计算（服务端，不由 AI 直接决定）**：

```text
weighted = (scores.timeliness × 0.25 + scores.impact × 0.35 + scores.confidence × 0.25 + scores.clarity × 0.15)
score_total = Math.round(weighted × 2 × 10) / 10   -- 1.0-10.0 1位小数
```

**后处理**：JSON 解析失败 → 按 AC-07/§3.2 错误分类表处理（可重试 2 次 → 仍失败则 final_failed）

#### 阶段 5：写入 + 新闻位置分发

```text
1. INSERT/UPDATE processed_news（通过 raw_item_id ON CONFLICT 去重）
   - title, summary, translation, context, analysis, score_total, score_dimensions, tags_v2
   - source_refs 扩展为包含库内检索/链接/搜索来源 URI
   - published_at 沿用 raw_items.published_at（原始时间）
2. raw_items.l1_status = 'completed', l1_processed_at = now(), l1_attempt = attempt
3. 创建 news_positions 记录（与 v0.5 processor.ts 一致——查找该 Source 的 display_position 并 fan-out）
```

### 4.4 Dispatcher 改造（type 分支退避）

v0.5 `dispatcher.ts` 的 `requeueTask` 当前使用线性退避（`delaySeconds = min(300, 10 * (tries + 1))`，max 5 次）。

**改造后** —— 按 `tasks.type` 分支退避（PRD AC-08 "不影响既有任务类型"）：

```ts
const BACKOFF_CONFIG: Record<string, { maxAttempts: number; backoff: number[] }> = {
  // v0.5 既有
  fetch:   { maxAttempts: 5, backoff: [10, 20, 40, 80, 160] },      // 线性退避（接近原有）
  process: { maxAttempts: 5, backoff: [10, 20, 40, 80, 160] },      // 同 fetch

  // v0.6 新增
  l0_classify: { maxAttempts: 3, backoff: [60, 300, 900] },         // 1min / 5min / 15min（PRD AC-08）
  l1_process:  { maxAttempts: 3, backoff: [60, 300, 900] },         // 同上
  l1_retry:    { maxAttempts: 1, backoff: [0] },                    // 手动重试不自动重试
};
```

**`requeueTask` 改造要点**：

```ts
async function requeueTask(conn: PoolClient, task: any, lastError: string): Promise<void> {
  const attempt = parseInt(task.attempt || "0");
  const cfg = BACKOFF_CONFIG[task.type] || BACKOFF_CONFIG.process;  // 默认用 process 配置
  const maxAttempts = cfg.maxAttempts;
  if (maxAttempts > 0 && attempt >= maxAttempts) {
    // 达到上限 → final_failed
    if (task.type === 'l1_process' || task.type === 'l1_retry') {
      await setL1FinalFailed(conn, task);  // 更新 raw_items.l1_status = 'final_failed'
    } else if (task.type === 'l0_classify') {
      await setL0Failed(conn, task);        // 更新 raw_items.l0_status = 'failed'
    }
    await finishTask(conn, task.id, "failed", lastError);
    return;
  }
  const delay = cfg.backoff[Math.min(attempt - 1, cfg.backoff.length - 1)];
  // todo: 写入 tasks.run_after + 更新 raw_items.l1_next_retry_at
}
```

### 4.5 外部服务集成策略

| 服务 | 集成方式 | timeout | fallback |
|------|----------|---------|----------|
| LLM | HTTP POST（复用现有流程, `config.openaiBaseUrl`） | 60s | 重试 2 次 → final_failed |
| 库内检索 | SQL ILIKE | 5s | 降级，无检索结果继续 |
| 链接读取 | `fetch(url)` + 简单文本提取 | 10s | 降级，无链接内容继续 |
| X 搜索 | X API v2 Recent Search or fetch | 15s | 降级（P2 实现） |
| Web 搜索 | Tavily / Serper / Google CSE (候选) | 15s | 降级（P2 实现） |

**网络出口配置建议**：
- LLM / X API：复用现有 `X_PROXY_URL`（v2ray 三进程）或者直连（当前 `token-plan-cn.xiaomimimo.com` 国内直达）
- Web 搜索 / 链接读取：建议直连 | 如果部署机国内 → 走代理。设计阶段暂不锁定，DevOps 部署就绪前做联通 dry-check

**环境变量增量**（DevOps PRD R1 #O6 联动）：

```dotenv
# L0/L1
L0_LLM_MODEL=claude-sonnet-4-20250514    # L0 可用更小模型
L1_LLM_MODEL=claude-sonnet-4-20250514    # L1 需要更强模型
LLM_L0_MAX_RETRIES=3
LLM_L1_MAX_RETRIES=3
LLM_TIMEOUT_MS=60000

# 库内检索
KB_SEARCH_LIMIT=5
KB_SEARCH_TIMEOUT_MS=5000

# 链接读取
LINK_READER_TIMEOUT_MS=10000

# 上传
UPLOAD_MAX_BYTES=1048576
UPLOAD_ALLOWED_MIME=image/png,image/jpeg,image/webp
UPLOAD_DIR=/var/lib/niuma-news/uploads

# 搜索（可选）
WEB_SEARCH_PROVIDER=tavily      # tavily | serper | google_cse | none
WEB_SEARCH_API_KEY=
X_SEARCH_PROVIDER=x_api         # x_api | none
X_SEARCH_API_KEY=
```

### 4.6 监控告警（AC-32 / §3.7）

4 种新增告警类型的生成触发条件（代号与 `alerts.type` 枚举值一致）：

| alerts.type | 触发条件 | severity |
|-------------|---------|----------|
| `task_stale` | `tasks.locked_by` 持续 > `TASK_STALE_SECONDS=600` 且 `tasks.type IN ('l0_classify','l1_process','l1_retry')` | `warning` |
| `external_dep_down` | 同一 raw_item 连续 5 次 L1 重试因同一外部依赖（LLM / 搜索 / 链接）失败 | `high` |
| `task_backlog` | raw_items alive 分区（`l0_status IN ('pending','processing')`）> 1000 条，且处理速度跟不上 | `info` |
| `final_failed_surge` | 1 小时内新增 `final_failed` > 50 条（初版阈值，可调整） | `warning` |

检测位置：新模块 `server/src/worker/l1-monitor.ts`。运行于现有 `workerLoop` 的空闲循环或独立间隔（每 5 分钟检查一次）。

### 4.7 空间图标上传流程

```text
用户选择文件
  → 前端校验 MIME + 大小（后端仍校验，前端为快速反馈）
  → POST multipart field=file to /v1/spaces/:id/icon
  → Fastify 路由解析 multipart（需要新增 @fastify/multipart 依赖）
  → 验证 MIME / 大小 / 空间存在
  → 写入持久目录: /var/lib/niuma-news/uploads/spaces/{space_id}/{content_hash}.{ext}
  → 更新 DB channel_spaces.icon_url + icon_type = 'image'
  → 返回 { icon_url, icon_type }
  → 前端更新 SpaceIcon 组件展示

删除:
  → DELETE /v1/spaces/:id/icon
  → 删除磁盘文件
  → 更新 DB: icon_url=NULL, icon_type='emoji'（保留 icon 字段原值作为回退）
```

**Nginx 配置变更**（需要新增 location block）：

```nginx
# v0.6 新增：图标上传文件静态服务
location /uploads/ {
    alias /var/lib/niuma-news/uploads/;
    expires 1y;
    add_header Cache-Control "public, immutable";
    add_header X-Content-Type-Options "nosniff";

    # gzip 不应用于已压缩图片，禁用
    gzip off;
}
```

此配置变更同步到 `deploy/nginx` 和 `deploy/scripts/deploy.sh`。

**回滚风险**：回滚到 v0.5 代码时，`channel_spaces.icon_type` 和 `icon_url` 字段在 DB 保留但 v0.5 代码不识别，视觉退化为仅展示 `icon` 字段的 emoji 文本（不影响功能）。旧上传文件留在磁盘，由 DevOps 后续手工清理或保留为可选。

## 5. 前端或交互影响

### 5.1 路由变更

`frontend/src/router.ts`：

```diff
- import LogsPage from "@/views/LogsPage.vue";
- import AlertsPage from "@/views/AlertsPage.vue";
+ import MonitoringPage from "@/views/MonitoringPage.vue";

const router = createRouter({
  routes: [
    { path: "/", redirect: "/news" },
    { path: "/news", component: NewsPage },
    { path: "/admin", component: AdminPage },
+   { path: "/monitoring", component: MonitoringPage },
+   { path: "/alerts", redirect: "/monitoring?tab=alerts" },
+   { path: "/logs", redirect: "/monitoring?tab=logs" },
    { path: "/sources/:id", component: SourceDetailPage },
  ],
});
```

**Vue Router redirect 而非 nginx rewrite**（UI spec §1.2 已定）：本项目无 SEO 需求，Vue Router 内 redirect 最简、不需要 nginx 修改、不影响旧书签/内部链接。

**TopNav 顶导改造**（3 项 + 角标）：

```
顶部导航：浏览 | 管理 | 监控[N]
- 删除 v0.5 的「告警」导航项
- 监控菜单项带未处理告警数红色角标（alerts 表 unhandled 计数 + 新增 AC-32 告警类型）
- 路由激活态：底部 2px 主题色横线
```

### 5.2 组件清单

UI spec R2 已定。此处列出后端 API 数据流与前端组件的对接表（Developer 实现参考）：

| 前端组件 | 数据来源 endpoint | key props |
|-----------|------------------|-----------|
| NewsPage | `GET /v1/news` | `items[].score_total, items[].tags_v2` |
| NewsDetailDrawer | `GET /v1/news/:id` | `NewsDetail` 完整结构 |
| StatsCards × 4 | `GET /v1/stats` | `today_completed, total_completed, enabled_sources, channels` |
| SpacePills mini | `GET /v1/spaces` | `icon, icon_url, icon_type` |
| StatusBadge（扩展） | — | `LevelStatus` 枚举 11 值 + 4 色映射 |
| SpaceIcon | `GET /v1/spaces/:id` | `icon, icon_url, icon_type` |
| IconUploader | `POST /v1/spaces/:id/icon` | `file, maxSize, acceptMime` |
| SourceLevelStatusCounts | `GET /v1/sources/:id/level-status-counts` | `completed, skipped, retryable_failed, final_failed` |
| MonitoringPage AlertsTab | `GET /v1/alerts` | 同 v0.5 + 新增 type |
| MonitoringPage LogsTab | `GET /v1/logs` | 同 v0.5 |

### 5.3 前端新建组件（UI spec R2 定稿清单）

12-16 个 `.vue` 文件：

```
核心 6 组件（必须独立 .vue）:
- NewsDetailDrawer
- IconUploader
- SpaceIcon
- SourceLevelStatusCounts
- SourceRecentNewsTimeline
- MonitoringPage (含 MonitoringTabs)

子组件 6-10 个（可按需合并到父组件）:
- DrawerSummary / DrawerTranslation / DrawerContext / DrawerScore / DrawerTags
- ConfidenceBadge / SourceTagRow
- IconEditField
```

## 6. 迁移和兼容性

### 6.1 Schema 迁移

走 `drizzle-kit generate` 增量迁移（ADR-001），不复制粘贴 baseline。

迁移文件命名：`server/drizzle/0001_<slug>.sql`

迁移内容 ORDER 依赖：

```text
1. raw_items × 7 新增列
2. processed_news × 6 新增列
3. channel_spaces × 2 新增列
4. raw_items × 2 新增索引
5. (可选) alerts 表 type 枚举的 CHECK 约束扩展 —— 如果 v0.5 用了 CHECK，则在本次迁移中 ALTER 约束
```

### 6.2 数据迁移

**零数据迁移**：v0.6 所有新增列都有 DEFAULT（`l0_status='pending'`, `l1_status='not_started'`, `icon_type='emoji'`），无需跑数据迁移脚本。存量行自动赋值 default。

**v0.5 历史数据兼容性**（vs UI R1 #A1）：

| 字段 | v0.5 现存 | v0.6 字段 | 兼容方式 |
|------|-----------|-----------|----------|
| `importance_score` numeric | 已有数据 | 保留不变；`score_total` 新增 | 历史数据 importance_score 仍展示；`score_total=NULL` 时前端隐藏 |
| `tags jsonb string[]` | 1000+ 行 | 保留；`tags_v2 jsonb` 新增 | `tags_v2=NULL` 时前端将 `tags` 平铺数组归入 `processing` 类展示 |
| `entities jsonb any[]` | 1000+ 行 | 保留；无替代 | 前端不展示旧 entities（由 tags_v2 覆盖） |
| `source_refs jsonb` | 1000+ 行 | 保留不变 | — |

### 6.3 代码迁移

| 文件 | 操作 | 说明 |
|------|------|------|
| `server/src/worker/l0-classifier.ts` | 新建 | L0 判定模块 |
| `server/src/worker/l1-processor.ts` | 新建 | L1 综合处理模块 |
| `server/src/worker/l1-monitor.ts` | 新建 | L0/L1 监控告警模块 |
| `server/src/worker/dispatcher.ts` | 改造 | `requeueTask` 按 type 分支退避 |
| `server/src/worker/processor.ts` | 保留不变 | v0.5 现有 processor 仍处理 `type='process'` 任务，与 l1-processor 共存 |
| `server/src/api/routes/news.ts` | 扩展 | 新增 `GET /v1/news/:id` + `GET /v1/news` 扩展字段 |
| `server/src/api/routes/spaces.ts` | 扩展 | 新增图标上传/删除 endpoint |
| `server/src/api/routes/sources.ts` | 扩展 | 新增 level-status-counts endpoint（或新建 `routes/level-status.ts`） |
| `server/src/api/routes/stats.ts` | 修改 | 统计口径切换 |
| `server/src/worker/llm.ts` | 扩展 | 新增 `processL1LLM()` 函数（区别于现有 `processLLM` v0.5 版） |
| `frontend/src/router.ts` | 修改 | 路由变更 |
| `frontend/src/views/MonitoringPage.vue` | 新建 | 合并 AlertsPage + LogsPage |
| `frontend/src/views/NewsPage.vue` | 微调 | 抽屉替换、统计口径切换 |
| `frontend/src/views/SourceDetailPage.vue` | 重构 | 双栏、时间轴、L0/L1 计数 |
| `frontend/src/lib/types.ts` | 扩展 | 新增 TS 类型 |
| Various frontend new components | 新建 | 12-16 个 .vue |

## 7. 风险和取舍

### 7.1 设计决策保留（ADRs）

#### ADR-002：L0/L1 状态字段落在 raw_items 双字段
- 已在 §2.1 记录

#### ADR-003：processed_news 表 jsonb 增量扩展，不拆子表
- 已在 §2.2 记录

#### ADR-004：L0/L1 复用 tasks 表按 type 分支退避
- 已在 §4.2 记录

#### ADR-005：图标上传存储选型

| 方案 | 优点 | 缺点 | 为什么不选 |
|------|------|------|-----------|
| A. 后端持久目录 + nginx 反代（采用, 推荐） | 代码分离、nginx 缓存可控、备份独立 | 需要修改 nginx 配置、部署需同步 | — |
| B. DB bytea | 统一备份、回滚无文件残留 | SELECT 拖慢、上传大图跨网络传输加倍 | 图标是静态文件不是数据 |
| C. 对象存储（S3/minio） | 高可用、无磁盘占满风险 | 新增账号/密钥/部署复杂度、需改 nginx 反代 | v0.5 项目规模不需要 |

**最终方案 A**：持久目录 `/var/lib/niuma-news/uploads/`（见 §4.7）

#### ADR-006：库内检索走 ILIKE 基础版，不上向量

| 方案 | 优点 | 缺点 | 为什么不选 |
|------|------|------|-----------|
| A. ILIKE 基础版（采用） | 零新依赖、零新 infrastructure、5ms-50ms | 召回质量低、只匹配关键词 | 当前 100-500 条/日规模够用 |
| B. pgvector + embedding | 召回质量高、语义而非词匹配 | 新增 embedding 调用成本 + DB 500MB+ + 迁移脚本 | 在下个量级（日 > 1000 条）时再评估 |
| C. Elasticsearch 全文 | 高质量中文分词、可扩展 | Docker 部署 + 系统资源占用 + 索引同步 | 作为一个人的项目过于重量级 |

### 7.2 已知风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| LLM 输出结构不稳定（11+ key 的 JSON 体量大） | 增加 L1 final_failed 率 | 按 §3.2 三档分类 + 重试 2 次后 final_failed；prompt template 可调 |
| 外部依赖（搜索 / 链接读取）在部署机网络不通 | L1 降级运行，核心流程仍可用 | 库内检索是唯一必需的（失败可降级）；X/Web 搜索是可选的（失败不影响 L1 completed） |
| 图标上传目录 /var/lib/niuma-news/uploads 权限不当或未创建 | 上传接口 500 | 部署清单 + health check 确认目录存在且 node 用户可写 |
| 磁盘容量（raw_items 增长 + 日志放大） | 6 个月内告警概率高，1 年内撞顶 | PRD R2 §6.1 已登记风险，本期不治理；DevOps 上线后设置磁盘 80% 告警 |
| v0.5 历史 `processed_news.tags` 数据兼容 | 旧新闻在前端 tags_v2 为 NULL | 前端降级：`tags_v2 || { processing: tags }` |
| 日志放大（L0/L1 每行 7-15 行日志 × 1000 条/日 = 10-50MB/天） | 1 年内显著 | logrotate daily + 保留 14 天 + 压缩（DevOps #O8，部署就绪前实施） |

### 7.3 开放问题（设计阶段不关闭，移交实施或 DevOps）

1. **外部搜索候选选型**（Web 搜索 / X 搜索 具体服务商）：PRD R2 §5 边界表已收敛边界（可选增强、失败可降级）；具体选型由 Developer 在实现阶段确认，本设计文档不锁定。
2. **X 搜索实现方式**：X API v2 Recent Search（需升级到 Basic 套餐或更高）vs 抓取式（受限于 X 页面结构变化频繁）。建议在实现时评估成本后决定。
3. **文件目录权限方案**：`/var/lib/niuma-news/uploads/` 的创建时机（deploy.sh 处理 vs systemd ExecStartPre）由 DevOps 在部署前确认。
4. **磁盘告警触发线**：DevOps 上线后设置。

## Review 记录

### PM R1 Review（2026-06-12）

**结论：⚠️ 有条件通过。**

产品侧判断：设计文档整体承接 PRD R2 与 UI spec R2 的定稿范围，三条主线（L0/L1 分层处理、前端重构、空间图标上传）均有落点；未发现重新引入 L2、评论/反馈、独立全屏详情页、原始数据治理后台、生产 mock 或成本熔断等已明确不做事项。本轮不要求因 PM Review 进入设计 R2 大改。

**通过依据：**
- L0/L1 数据模型、状态机、错误分类、重试和新闻流可见性已从 PRD 层落到 schema / worker / API / TS 类型。
- 统一监控页、旧 `/alerts` `/logs` 跳转、监控角标与 AC-32 自动告警在 §4.6 / §5.1 有承接。
- 空间图标上传采用后端持久目录 + nginx `/uploads/`，符合 Owner / PRD 对“不得进入前端构建目录”的裁定。
- 外部搜索和 X 搜索保持“可选增强、失败降级”，没有把未锁定服务商变成上线阻断。

**PM 条件项：**
- #P1（中）：L0 `retryable` 是设计层新增状态，PRD 产品层 L0 只定义 `pending / processing / skipped / passed / failed`。可接受前提是它不作为前台用户态直接展示；如需展示，应映射到 UI 既有“待重试/异常”表达，并同步 Tester 断言。
- #P2（中）：§3.2 `level-status-counts` 的 SQL 示例中 `OR ... IS NOT NULL` 会让“24h 窗口”退化为全量历史计数，和 `window_started_at` 语义冲突。需在 Developer / Tester Review 前明确：到底是最近处理窗口还是全量累计。
- #P3（低）：文档口径需统一。当前存在 `raw_items × 7 列` vs DDL 9 列、`4 个 ADR` vs ADR-002~006 共 5 个、`28 AC` vs AC-01~AC-35 的残留表述，建议 Architect 后续修正文案，避免实施拆任务误读。
- #P4（低）：开放问题数量口径需同步。正文 §7.3 为 4 项，迭代摘要/收尾摘要仍写 3 项；不影响方案，但影响状态索引可信度。

**PM 不阻断事项：**
- 外部搜索服务商和 X 搜索方式继续留给实现阶段评估，符合 PRD 边界。
- DevOps 文件目录权限方案、磁盘告警线继续留给部署就绪阶段确认，符合角色分工。
- 库内检索使用 ILIKE 基础版符合当前信息量级；后续数据量上升再评估向量化。
