# 牛马程小报 v0.1 PRD — 数据层完善

| 字段 | 值 |
|------|-----|
| 版本 | v0.1 |
| 状态 | 定稿 |
| 日期 | 2026-05-23 |
| 作者 | Claude + 用户协作 |
| 迭代号 | v0.1 — 数据层 |

---

## 1. 产品概述

### 1.1 产品名称

牛马程小报（News Aggregator）

### 1.2 产品定位

面向个人/小团队的新闻聚合阅读平台。从多个外部渠道（RSS、学术论文、社交媒体、开源社区）自动抓取原始素材，经 AI 处理后生成结构化中文新闻卡片，按频道空间和子频道组织展示。

### 1.3 目标用户

一人公司内部使用，暂不对外开放。产品所有者即唯一用户。

### 1.4 产品愿景

打造一个可扩展的多领域新闻聚合平台，当前以 AI 领域为 MVP 切入点，后续逐步扩展至财经、体育、历史、军事等方向。每个领域作为一个独立的频道空间，空间内可按子频道进一步分类。

### 1.5 当前状态

项目已完成 MVP 基础框架，包含：
- 后端 API（FastAPI + PostgreSQL/Supabase）
- Worker（抓取 + LLM 处理）
- 前端（Vue3 + TypeScript，浏览页 + 管理页）
- 已支持的 Source 类型：RSS、HuggingFace Daily Papers、HackerNews、GitHub Trending、Semantic Scholar

> **前端类型定义现状**：前端 `types.ts` 中 `ProcessedNews` 缺少 `sub_channel_id`、`entities` 字段，`ChannelSource` 缺少 `sub_channel_id` 字段；`api.ts` 中 `listNews` 缺少 `sub_channel_id` 查询参数，`bindSource`/`updateChannelSource` 缺少 `sub_channel_id` 参数。v0.1 需同步这些类型和 API 参数以匹配后端响应变更。

### 1.6 现有架构参考

参见项目根目录的 `README.md`（项目概览）、`docs/progress/iterations/v0.1-design.md`（详细设计）、`docs/使用说明.md`（使用手册）、`db/schema.sql`（数据库全量 schema）。

> **与 v0.1-design.md 的关系**：`docs/progress/iterations/v0.1-design.md` 是本文档的配套技术设计，包含具体的代码实现细节和文件变更清单。本 PRD 描述产品需求与验收标准，设计文档描述实现方案。

---

## 2. v0.1 迭代目标

### 2.1 核心目标

完善数据层，为后续前端体验迭代打下基础。本迭代结束后，后端数据模型和 API 能够支持子频道分类、完整结构化的新闻处理、以及 X/Twitter 数据源。

### 2.2 价值主张

- 子频道概念让新闻分类更精细，用户可按"AI论文"、"AI应用"、"前沿新闻"等维度组织内容
- LLM 全字段输出提升新闻卡片信息密度，为前端展示提供更丰富的数据
- X/Twitter 接入拓展了重要的实时信息来源
- 子频道内去重避免重复信息干扰

### 2.3 前置条件

- 已部署项目基础框架（后端 API + Worker + 数据库）
- 已配置 OpenAI 兼容 API Key
- 已配置 HTTP/HTTPS 代理（国内环境访问 X API 等境外服务时必需）
- X Basic 套餐 API Key（如需启用 Twitter 抓取）

---

## 3. 用户故事

### 3.1 子频道管理

**US-3.1.1** 作为产品所有者，我希望在频道空间下创建子频道（如"AI论文"、"AI应用"），以便将新闻按更细的维度分类。

**US-3.1.2** 作为产品所有者，我希望在绑定 Source 到频道空间时，指定它归属哪个子频道，以便自动分类新闻。

**US-3.1.3** 作为产品所有者，我希望在绑定后还能修改 Source 归属的子频道，以便调整分类策略。

**US-3.1.4** 作为产品所有者，我希望能够重命名、删除子频道，以便管理分类体系。

### 3.2 X/Twitter 数据源

**US-3.2.1** 作为产品所有者，我希望创建 X/Twitter 类型的 Source，通过关键词搜索拉取相关推文，以便捕获 Twitter 上的 AI 动态。

**US-3.2.2** 作为产品所有者，我希望创建 X/Twitter 类型的 Source，通过追踪特定账号（如 @OpenAI、@AnthropicAI）拉取其推文，以便关注关键人物的动态。

### 3.3 AI 处理增强

**US-3.3.1** 作为产品所有者，我希望 LLM 处理后生成结构化要点（bullets），以便快速了解新闻核心内容。

**US-3.3.2** 作为产品所有者，我希望 LLM 处理后生成标签（tags），以便后续按标签筛选。

**US-3.3.3** 作为产品所有者，我希望 LLM 处理后生成重要性评分（0-10），以便按重要性排序新闻。

**US-3.3.4** 作为产品所有者，我希望 LLM 处理后生成识别实体（entities，如人名、组织、产品名），以便了解新闻涉及的关键实体。

### 3.4 新闻浏览

**US-3.4.1** 作为产品所有者，我希望按子频道筛选新闻列表，以便聚焦特定类别的新闻。

**US-3.4.2** 作为产品所有者，我希望同一子频道内不会出现重复新闻，以便阅读体验更清爽。

---

## 4. 功能需求

### 4.1 子频道（Sub-Channel）

#### 4.1.1 数据模型

新增 `sub_channels` 表，挂在 `channel_spaces` 下：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | UUID | PK, default gen_random_uuid() | |
| `channel_space_id` | UUID | FK → channel_spaces.id, ON DELETE CASCADE, NOT NULL | |
| `name` | VARCHAR(100) | NOT NULL | 同一空间下唯一 |
| `sort_order` | INT | DEFAULT 0 | 越小越靠前 |
| `created_at` | TIMESTAMPTZ | DEFAULT now(), NOT NULL | |

约束：`UNIQUE(channel_space_id, name)`

索引：`ix_sub_channels_space_sort(channel_space_id, sort_order)`

`channel_sources` 表新增字段：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `sub_channel_id` | UUID | FK → sub_channels.id, ON DELETE SET NULL, NULLABLE | 可选，指定绑定的子频道 |

`processed_news` 表新增字段：

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `sub_channel_id` | UUID | FK → sub_channels.id, ON DELETE SET NULL, NULLABLE | 冗余字段，从 channel_sources 继承 |

新增索引：
```sql
CREATE INDEX ix_processed_news_sub_published
  ON processed_news(channel_space_id, sub_channel_id, published_at DESC);
```

**`processed_news.sub_channel_id` 数据流**（解决 #1）：

`process_one()` 在处理 raw_item 时，通过以下路径获取 `sub_channel_id`：

```
raw_item.source_id + raw_item.channel_space_id
  → channel_sources（WHERE source_id = ? AND channel_space_id = ?）
    → channel_sources.sub_channel_id
      → 写入 processed_news.sub_channel_id
```

在 `worker/main.py` 的 `process_one()` 函数中，需要新增一次查询：

```python
sub_channel_id = await conn.fetchval(
    """
    SELECT sub_channel_id FROM channel_sources
    WHERE source_id = $1 AND channel_space_id = $2
    LIMIT 1
    """,
    row["source_id"],
    row["channel_space_id"],
)
```

如果一个 source 绑定到同一空间的多个 channel_source（理论上不应出现，因为 `UNIQUE(channel_space_id, source_id)` 约束），取第一个即可。

**现有级联行为**（已由数据库 schema 定义，非 v0.1 新增）：
- 删除 Source → `ON DELETE CASCADE` 删除关联的 `channel_sources`、`raw_items`
- 删除 `raw_items` → `ON DELETE CASCADE` 删除关联的 `processed_news`
- 删除 `channel_sources` → `ON DELETE CASCADE` 删除关联的 `source_states`
- 删除子频道 → `ON DELETE SET NULL` 将 `channel_sources.sub_channel_id` 和 `processed_news.sub_channel_id` 置 NULL

#### 4.1.2 CRUD API

- `POST /v1/channel-spaces/{channel_space_id}/sub-channels` — 创建子频道。`name` 不可为空字符串或纯空格（返回 422）；同一空间下名称重复时返回 `409 Conflict`，响应格式 `{"detail": "子频道名称 'XXX' 已存在"}`（不暴露数据库 raw error）
- `GET /v1/channel-spaces/{channel_space_id}/sub-channels` — 列出子频道（按 sort_order 排序）
- `PUT /v1/sub-channels/{sub_channel_id}` — 更新子频道（名称、排序），至少需传一个字段；空 body 返回 400
- `DELETE /v1/sub-channels/{sub_channel_id}` — 删除子频道。关联的 `channel_sources.sub_channel_id` 和 `processed_news.sub_channel_id` 自动置 NULL（ON DELETE SET NULL），已分类新闻的分类信息将丢失且不可自动恢复

#### 4.1.3 绑定变更

- `POST /v1/channel-spaces/{channel_space_id}/sources`（绑定）body 新增可选字段 `sub_channel_id`
- `PUT /v1/channel-sources/{channel_source_id}` body 新增可选字段 `sub_channel_id`
  - 传 `null` 表示清除子频道绑定（将 `sub_channel_id` 置为 NULL）
  - 不传该字段表示保持现有绑定不变

> **实现注意**：Pydantic 的 `Optional[UUID] = None` 默认无法区分"不传"和"传 null"（两者都解析为 None）。实现时需用 `exclude_unset` + 检查 `model_fields_set`，或在 schema 中使用 sentinel 值区分。否则用户无法通过 API 清除绑定。
- 绑定后更改子频道时，已处理的新闻 `sub_channel_id` 不追溯更新（仅影响新入库的新闻）
- 已抓取但尚未处理的 raw_item，其 `sub_channel_id` 在 process 时实时查询 `channel_sources`——如果用户在 fetch 之后、process 之前修改了绑定，该条新闻将归属新子频道。v0.1 接受此行为（竞态窗口短且影响小），后续可在 `raw_items` 表冗余 `sub_channel_id` 彻底消除

#### 4.1.4 新闻查询

- `GET /v1/channel-spaces/{channel_space_id}/news` 新增查询参数 `sub_channel_id`（可选），按子频道筛选
- 不传 `sub_channel_id` 时返回该空间下所有新闻（行为不变）

### 4.2 X/Twitter 数据源

#### 4.2.1 Source 类型定义

`type` = `x_twitter`

> **config 校验策略**：v0.1 不在 API 层校验 Source config 的结构合法性（如 mode 字段是否缺失）。创建 Source 时 config 为自由 JSON，错误配置将在 Worker 执行 fetch 任务时报错。后续迭代可在 API 层引入 per-type config schema 校验。

#### 4.2.2 Source config 结构

v0.1 支持两种独立模式，每种模式创建一个独立的 Source。用户如需同时使用关键词搜索和账号追踪，创建两个 Source 即可。

**搜索模式：**
```json
{
  "mode": "search",
  "search_query": "LLM OR GPT OR Claude"
}
```

API 请求的 `max_results` 直接取 `fetch_policy.budget.max_items_per_run`（上限 100），无需在 config 中单独配置。since_id 取返回结果中**最新**推文的 tweet_id，确保增量抓取不重复（API 只请求将被保留的条数，不存在截断丢弃，因此不会丢失中间推文）。

> **搜索范围限制**：X Basic 套餐的 recent search 端点只能搜索最近 7 天的推文。首次拉取仅能获取 7 天内的内容，无法回溯历史。完整历史搜索需 X Pro（$5,000/月）或 Academic Research 权限，不在 v0.1 范围内。

**账号追踪模式：**
```json
{
  "mode": "user_timeline",
  "usernames": ["OpenAI", "AnthropicAI"],
  "max_results_per_user": 20
}
```

字段说明：
- `mode`：必填，`search` 或 `user_timeline`
- `search_query`：mode = search 时必填
- `usernames`：mode = user_timeline 时必填，数组
- `max_results_per_user`：账号追踪模式每个账号每次 API 最大返回数，默认 20，上限 100

> v0.1 不引入 `combined` 模式。理由：用户可创建多个 Source 分别配置，合并模式增加游标管理复杂度且一个 API 限流会影响另一个。

> `tweet_fields` 和 `user_fields` 不暴露给用户配置，在 `worker/fetch_x_twitter.py` 中硬编码默认值。

#### 4.2.3 环境变量新增

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `X_BEARER_TOKEN` | 无 | X API v2 Bearer Token（App-Only 认证） |

v0.1 仅使用 Bearer Token（App-Only），满足 Basic 套餐的 recent search 和 user timeline 需求。OAuth 1.0a 凭证暂不引入，如需 User Context 认证时再扩展。

#### 4.2.4 游标（Source State Cursor）

搜索模式游标：
```json
{
  "since_id": "1760012345678900000"
}
```

账号追踪游标：
```json
{
  "user_cursors": {
    "OpenAI": "1760012345678900000",
    "AnthropicAI": "1759999999999900000"
  }
}
```

增量逻辑：
- 搜索模式：每次请求带上 `since_id`（取上一批返回结果中**最新**推文的 tweet_id），只拉取 ID > since_id 的新推文。搜索模式 API 请求量 = max_items_per_run（不存在截断丢弃），因此用最新 ID 不会丢失数据，也不会产生重复。首次拉取不设 `since_id`，拉取最近推文。
- 账号追踪：每个用户名维护独立的游标，逐用户增量拉取。

#### 4.2.5 Raw Item 内容结构

```json
{
  "tweet_id": "1760012345678900000",
  "text": "推文文本...",
  "author_username": "OpenAI",
  "author_name": "OpenAI",
  "created_at": "2026-05-20T10:30:00Z",
  "public_metrics": {
    "retweet_count": 120,
    "reply_count": 45,
    "like_count": 890,
    "quote_count": 30,
    "bookmark_count": 200,
    "impression_count": 50000
  },
  "entities": {
    "urls": [{"expanded_url": "...", "title": "..."}],
    "mentions": [{"username": "..."}],
    "hashtags": [{"tag": "..."}]
  },
  "referenced_tweets": [{"type": "quoted", "id": "..."}],
  "url": "https://x.com/OpenAI/status/1760012345678900000"
}
```

#### 4.2.6 LLM 渲染文本

`render_text_for_llm` 输出格式：

```
[Tweet by @OpenAI]
文本内容...

Likes: 890 | Retweets: 120 | Replies: 45

引用链接:
- https://...
```

#### 4.2.7 Worker 实现文件

新增 `worker/fetch_x_twitter.py`，包含：
- `async def fetch_x_search(client, config, cursor, max_items)` — 关键词搜索
- `async def fetch_user_timeline(client, username, cursor, max_items)` — 单账号追踪
- `async def parse_x_twitter(config, cursor, max_items)` — 主入口，根据 mode 调度
- `def render_text_for_llm(content)` — 渲染给 LLM 的文本

X API 请求参数（tweet_fields、user_fields、expansions）在此文件中硬编码，不从 config 读取。

#### 4.2.8 限流与预算

- X Basic 套餐限制：recent search 每月 10,000 条，60 requests / 15 min，user lookup 每月 25,000 条
- `fetch_policy.budget.max_items_per_run` 控制每次抓取入库上限（总条数，跨两种模式统一）
- 搜索模式：API 请求 `max_results` 直接取 `max_items_per_run`（≤100），请求量与入库量一致，无截断丢弃
- 账号追踪模式：`max_results_per_user` 控制每个用户单次请求量（上限 100）。拉取所有用户后**按 `published_at` 倒序排列**，再截断至 `max_items_per_run`——确保时间最新的推文被优先保留，不受网络响应时序影响
- Worker 应在请求失败 429 时触发退避重试（已有 `_requeue_task` 机制）

**空结果、认证失败与异常用户名的处理：**

- 某次抓取返回 0 条新推文时，游标保持不变（下次用同一 since_id 继续增量查询，不会产生重复数据）
- 账号追踪模式中，不存在的用户名（404）静默跳过，只拉取存在的用户；全部用户名均不存在时产生一条 alert
- 用户名解析失败（非 404 的网络错误）应触发重试，不静默跳过
- **认证失败处理**：`X_BEARER_TOKEN` 未配置或无效导致 401/403 时，应立即创建 alert 告知用户认证失败（区别于 429 的退避重试）。首次 401/403 即标记任务失败，不消耗重试次数——重试不会改变认证结果

#### 4.2.9 Worker 调度集成

在 `worker/main.py` 的 `fetch_and_ingest()` 函数中，新增 `x_twitter` 分支：

```python
elif source_type == "x_twitter":
    cursor_data = cursor  # 当前游标
    items = await parse_x_twitter(source_config, cursor_data, max_items)
    cursor_updates = items.cursor_updates  # parse_x_twitter 返回游标更新
```

`parse_x_twitter` 返回结构改为 `(items: list[dict], cursor_updates: dict)`，以便外部拿到更新后的游标。

### 4.3 LLM 处理增强

#### 4.3.1 输出结构变更

现有输出：
```json
{"title": "...", "summary": "...", "language": "zh"}
```

改为：
```json
{
  "title": "中文标题",
  "summary": "80-200字中文摘要",
  "bullets": ["要点1", "要点2", "要点3"],
  "tags": ["标签1", "标签2", "标签3"],
  "entities": [
    {"name": "实体名", "type": "person|org|product|tech"}
  ],
  "importance_score": 7.5,
  "language": "zh"
}
```

字段说明：
- `bullets`：≤5 条核心要点（prompt 引导生成 3-5 条），每个不超过 50 字。代码仅校验 ≤5 条和字段类型，不强制下限
- `tags`：≤5 条分类标签（prompt 引导生成 3-5 条），代码仅校验 ≤5 条和字段类型
- `entities`：识别的关键实体（人名、组织、产品、技术）
- `importance_score`：0-10 浮点数，综合考虑时效性、影响力、相关性（**v0.1 仅验证类型和范围**，评分质量依赖 prompt 调优，不设自动化质量验收标准）
- `source_refs`：保持现状，由 `process_one` 从 `raw_items.source_item_url` + `source_id` 构造，不在 LLM 输出中

#### 4.3.2 Prompt 变更

`worker/llm.py` 中 `translate_and_summarize` 的 prompt 替换为：

```
把下面内容处理成中文新闻卡片，做翻译与深度摘要。
要求：
1) 如果原文是英文，翻译成中文
2) 输出中文标题(title)和中文摘要(summary)
3) summary 80-200 字
4) 提炼 3-5 个核心要点(bullets)，每个不超过 50 字
5) 生成 3-5 个分类标签(tags)
6) 识别关键实体(entities)，每个包含 name 和 type(person/org/product/tech)
7) 评估重要性评分(importance_score)，0-10 浮点数，考虑时效性、影响力、相关性
8) 只输出严格 JSON，不要输出多余文字
JSON 格式：{"title":"...","summary":"...","bullets":["..."],"tags":["..."],"entities":[{"name":"...","type":"..."}],"importance_score":7.5,"language":"zh"}
source_url: {source_url}

content:
{text}
```

#### 4.3.3 输出校验规则

LLM 输出解析后需进行字段校验和修正（在 `translate_and_summarize` 返回前执行）：

| 字段 | 校验规则 | 异常处理 |
|------|---------|---------|
| `title` | 必须为非空字符串 | 缺失时用 `"无标题"` 填充 |
| `summary` | 必须为非空字符串 | 缺失时用 `title` 填充 |
| `bullets` | 必须为字符串数组 | 非数组时置 `[]`；超过 5 条时截断至 5 |
| `tags` | 必须为字符串数组 | 非数组时置 `[]`；超过 5 条时截断至 5 |
| `entities` | 必须为对象数组，每项含 name | 非/缺失时置 `[]`；过滤掉无 name 的项 |
| `importance_score` | 必须为 0-10 数字 | 缺失/非数字时置 `0`；超出范围时 clamp 到 [0, 10] |
| `language` | 必须为非空字符串 | 缺失/非字符串时默认 `"zh"`；有值时保留原值（不硬覆盖） |

校验函数签名：

```python
def validate_llm_output(result: dict) -> dict:
    """校验并修正 LLM 输出，确保字段完整且类型正确。"""
```

#### 4.3.4 数据写入变更

`worker/main.py` 中 `process_one` 函数的 INSERT 语句更新：

- 将 `'[]'::jsonb, '[]'::jsonb, '[]'::jsonb, 0` 替换为从 LLM 结果读取实际值
- 新增 `sub_channel_id` 字段写入（通过 4.1.1 中描述的查询获取）
- `source_refs` 保持现有逻辑（从 `raw_items.source_item_url` + `source_id` 构造），不依赖 LLM 输出

### 4.4 子频道内去重

#### 4.4.1 去重策略

- 去重范围：同一 `(channel_space_id, sub_channel_id)` 内
- 去重依据：`raw_items.source_item_url` 精确匹配
- 去重时机：`processed_news` 入库前检查
- v0.1 **不做 URL 规范化**，仅精确匹配。已知限制：`https://arxiv.org/abs/2401.12345` 和 `https://arxiv.org/pdf/2401.12345` 不被视为同一条（后续迭代可引入 URL 规范化）
- 如果 `source_item_url` 为 NULL，不做额外去重（后续迭代可引入标题相似度）

**已知限制**：当不同 Source 抓到相同 URL 的新闻时，先完成 process 任务的 raw_item 被保留，后到达的被跳过。由于 Worker 并发处理，哪个 Source 的版本被保留取决于任务执行时序，结果不可预期。v0.1 接受此非确定性，后续可引入优先级规则（如按 importance_score 高的保留）。

#### 4.4.2 实现方式

`process_one` 在 INSERT `processed_news` 前，先查询：

```python
if sub_channel_id and row["source_item_url"]:
    exists = await conn.fetchval(
        """
        SELECT 1 FROM processed_news pn
        JOIN raw_items ri ON ri.id = pn.raw_item_id
        WHERE pn.channel_space_id = $1
          AND pn.sub_channel_id = $2
          AND ri.source_item_url = $3
        LIMIT 1
        """,
        row["channel_space_id"],
        sub_channel_id,
        row["source_item_url"],
    )
    if exists:
        return  # 跳过，不入库
```

该查询已有 `ix_processed_news_sub_published(channel_space_id, sub_channel_id, published_at)` 索引可加速。`raw_items.source_item_url` 建议补充索引（参见 7.4 迁移脚本）。

#### 4.4.3 与源内去重的关系

现有 `raw_items` 的 `UNIQUE(source_id, source_item_id)` 保证同一 Source 内不重复入库，这层去重不变。

子频道内去重是额外的跨源去重层：不同 Source（如 HackerNews 和 Twitter）抓到相同 URL 时，同一子频道内只保留第一条。

**约束链保证**：由于 `UNIQUE(channel_space_id, source_id)` 约束，同一频道空间下不会有两个绑定指向同一个 Source。因此每个 raw_item 在该空间内最多对应一个 `channel_sources` 记录，`sub_channel_id` 的映射是确定性的，不会出现同一 raw_item 在不同子频道间竞争的情况。跨频道空间的重复 URL 不去重（符合预期：不同空间的新闻各自独立）。

### 4.5 API 变更汇总

#### 4.5.1 新增接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/v1/channel-spaces/{id}/sub-channels` | 创建子频道 |
| GET | `/v1/channel-spaces/{id}/sub-channels` | 列出子频道 |
| PUT | `/v1/sub-channels/{id}` | 更新子频道 |
| DELETE | `/v1/sub-channels/{id}` | 删除子频道 |

#### 4.5.2 变更接口

| 方法 | 路径 | 变更内容 |
|------|------|---------|
| POST | `/v1/channel-spaces/{id}/sources` | body 新增 `sub_channel_id`（可选） |
| PUT | `/v1/channel-sources/{id}` | body 新增 `sub_channel_id`（可选） |
| GET | `/v1/channel-spaces/{id}/news` | 新增查询参数 `sub_channel_id`（可选） |

#### 4.5.3 响应变更

- `ChannelSourceOut` 新增 `sub_channel_id` 字段（可为 null）
- `ProcessedNewsOut` 新增 `sub_channel_id` 字段（可为 null）
- `ProcessedNewsOut` 的 `bullets`、`tags`、`entities`、`importance_score` 字段由空值变为实际数据

### 4.6 前端数据层适配

v0.1 不包含前端交互功能（子频道筛选 UI、评分排序、详情面板等），但因 API 响应格式已变更，前端类型定义和 API 调用层必须同步，否则前端无法正确消费新增字段。

#### 4.6.1 TypeScript 类型同步

`frontend/src/lib/types.ts` 当前状态与目标状态对照：

**ProcessedNews**：

| 字段 | 当前 types.ts | 后端响应 | 操作 |
|------|-------------|---------|------|
| `sub_channel_id` | **缺失** | 已返回 | 新增 `sub_channel_id?: UUID \| null` |
| `entities` | **缺失** | 已返回 | 新增 `entities?: { name: string; type: string }[]` |
| `bullets` | 已有 | 已返回 | 无需改动 |
| `tags` | 已有 | 已返回 | 无需改动 |
| `importance_score` | 已有 | 已返回 | 无需改动 |

**ChannelSource**：

| 字段 | 当前 types.ts | 后端响应 | 操作 |
|------|-------------|---------|------|
| `sub_channel_id` | **缺失** | 已返回 | 新增 `sub_channel_id?: UUID \| null` |

改动后类型定义：
```typescript
export type ProcessedNews = {
  // ... 现有字段保持不变 ...
  sub_channel_id?: UUID | null;                          // 新增：子频道归属
  entities?: { name: string; type: string }[];           // 新增：识别实体
};

export type ChannelSource = {
  // ... 现有字段保持不变 ...
  sub_channel_id?: UUID | null;                          // 新增：绑定的子频道
};
```

#### 4.6.2 API 函数参数同步

`frontend/src/lib/api.ts`：

**listNews** 新增 `sub_channel_id` 查询参数：
```typescript
export async function listNews(
  spaceId: UUID,
  limit: number,
  offset: number,
  subChannelId?: UUID,             // 新增：可选子频道筛选
): Promise<ProcessedNews[]> {
  const params = new URLSearchParams();
  params.set("limit", String(limit));
  params.set("offset", String(offset));
  if (subChannelId) params.set("sub_channel_id", subChannelId);
  return requestJson(buildUrl(`/v1/channel-spaces/${spaceId}/news?${params.toString()}`));
}
```

**bindSource** 新增 `sub_channel_id` 参数：
```typescript
export async function bindSource(
  spaceId: UUID,
  payload: {
    source_id: UUID;
    enabled: boolean;
    fetch_policy: FetchPolicy;
    sub_channel_id?: UUID;         // 新增：可选子频道绑定
  },
): Promise<ChannelSource> { ... }
```

**updateChannelSource** 新增 `sub_channel_id` 参数：
```typescript
export async function updateChannelSource(
  channelSourceId: UUID,
  payload: {
    enabled?: boolean;
    fetch_policy?: FetchPolicy;
    sub_channel_id?: UUID;         // 新增：可选子频道修改
  },
): Promise<ChannelSource> { ... }
```

#### 4.6.3 新闻卡片数据展示

`frontend/src/components/NewsListItem.vue` 当前已展示 `bullets`、`tags`、`importance_score`（通过 `ScoreBadge`）。代码已有 `v-if="item.bullets && item.bullets.length"` 防御性检查，从空数组变为非空数据时不会导致 UI 崩坏。需验证非空 bullets 的渲染效果（列表项间距、字数溢出）。`entities` 字段暂不展示（卡片信息密度已足够，v0.2 详情面板再引入）。

#### 4.6.4 新增 API 函数

前端需新增子频道相关 API 函数（供 v0.2 管理页使用，v0.1 仅定义）：

```typescript
// 子频道 CRUD（v0.1 定义函数签名，v0.2 接入 UI）
export async function listSubChannels(spaceId: UUID): Promise<SubChannel[]>
export async function createSubChannel(spaceId: UUID, payload: { name: string; sort_order?: number }): Promise<SubChannel>
export async function updateSubChannel(subChannelId: UUID, payload: { name?: string; sort_order?: number }): Promise<SubChannel>
export async function deleteSubChannel(subChannelId: UUID): Promise<void>
```

对应的 `SubChannel` 类型：
```typescript
export type SubChannel = {
  id: UUID;
  channel_space_id: UUID;
  name: string;
  sort_order: number;
  created_at: string;
};
```

> **说明**：4.6.4 的 API 函数和类型在 v0.1 作为**占位定义**提供，不接入 UI（因此无调用方可验证）。v0.2 开发时有权根据实际 UI 需求修改函数签名，这些定义仅为减少重复对接工作。另外，WebSocket `news_processed` 推送消息体保持现有字段（id、channel_space_id、title、published_at），无需新增 `sub_channel_id`——前端收到推送后调用 `refreshNews()` 全量拉取，即时推送仅作通知用途。

#### 4.6.5 文件变更清单（前端）

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `frontend/src/lib/types.ts` | ProcessedNews 新增 sub_channel_id、entities；ChannelSource 新增 sub_channel_id；新增 SubChannel 类型 |
| 修改 | `frontend/src/lib/api.ts` | listNews 新增 sub_channel_id 参数；bindSource/updateChannelSource 新增 sub_channel_id；新增子频道 CRUD 函数 |

---

## 5. 验收标准

### 5.1 子频道

- [ ] 可通过 API 在指定频道空间下创建子频道（含 name、sort_order）
- [ ] 可列出指定频道空间下所有子频道，按 sort_order 排序
- [ ] 可更新子频道的 name、sort_order
- [ ] 可删除子频道（`channel_sources.sub_channel_id` 和 `processed_news.sub_channel_id` 自动置 NULL）
- [ ] 绑定 Source 到频道空间时可指定 `sub_channel_id`（可选）
- [ ] 绑定后可通过 PUT 修改 `sub_channel_id`；传 `null` 可清除绑定
- [ ] 新闻列表 API 可按 `sub_channel_id` 筛选
- [ ] `process_one` 正确获取并写入 `sub_channel_id`（通过 `channel_sources` 反查）

### 5.2 X/Twitter

- [ ] 可创建 `type=x_twitter`、`mode=search` 的 Source，config 中包含 `search_query`
- [ ] 可创建 `type=x_twitter`、`mode=user_timeline` 的 Source，config 中包含 `usernames`
- [ ] Worker 能正确调度 X/Twitter 类型的 fetch 任务
- [ ] 搜索模式：从 X API v2 recent search 端点拉取推文，解析为 raw_items 入库
- [ ] 账号追踪模式：从 X API v2 user timeline 端点拉取推文，解析为 raw_items 入库
- [ ] 游标正确更新（since_id / user_cursors），增量抓取不重复。验证方式：检查 `source_states.cursor` JSON 值
- [ ] 搜索模式：`since_id` 取返回结果中**最新**推文的 tweet_id，下次请求只拉 ID > since_id 的新推文（搜索模式 API 请求量 = 入库量，不存在截断）
- [ ] 账号追踪模式：所有用户拉取完成后按 `published_at` 倒序排列，再截断至 `max_items_per_run`；仅在截断后更新游标，被截断丢弃的推文对应的游标不得更新（验证方式：检查 `source_states.cursor.user_cursors` 中各用户游标是否与被保留推文的 tweet_id 一致）
- [ ] 抓取到的推文能正确触发 process 任务，经 LLM 处理后生成 processed_news
- [ ] 请求限流（429）时触发退避重试，不直接失败

### 5.3 LLM 增强输出

- [ ] LLM 处理后 `processed_news` 的 `bullets` 字段包含 3-5 个要点
- [ ] LLM 处理后 `processed_news` 的 `tags` 字段包含 3-5 个标签
- [ ] LLM 处理后 `processed_news` 的 `entities` 字段包含识别的实体列表
- [ ] LLM 处理后 `processed_news` 的 `importance_score` 为 0-10 的浮点数
- [ ] LLM 输出 JSON 解析失败时，任务重试（现有 `_extract_first_json_object` + `_requeue_task`）
- [ ] LLM 输出字段缺失或类型错误时，校验函数修正后正常入库，不丢弃
- [ ] `importance_score` 超出 0-10 范围时被 clamp 到 [0, 10]
- [ ] 所有已有 Source 类型（rss、hf_daily_papers、hacker_news、github_trending、semantic_scholar）的 LLM 输出均升级为完整结构

### 5.4 去重

- [ ] 同一 `(channel_space_id, sub_channel_id)` 内，`raw_items.source_item_url` 相同的新闻不会重复入库（`processed_news` 层跳过）
- [ ] 不同 `(channel_space_id, sub_channel_id)` 下，相同 URL 的新闻可以各自存在
- [ ] `sub_channel_id` 为 NULL 时不做跨源去重（仅依赖 `raw_items` 的源内去重）

### 5.5 向后兼容

- [ ] `sub_channel_id` 为可选字段，不传时行为与 v0.1 之前一致
- [ ] 现有 API 路径和参数不变，仅新增可选字段和查询参数
- [ ] 现有 Source 类型的抓取和处理逻辑不受影响
- [ ] 数据库迁移仅做 ADD COLUMN（nullable）+ CREATE TABLE + CREATE INDEX，不删改现有字段

### 5.6 前端数据层适配

- [ ] `ProcessedNews` 类型新增 `sub_channel_id`、`entities` 字段
- [ ] `ChannelSource` 类型新增 `sub_channel_id` 字段
- [ ] 新增 `SubChannel` 类型定义
- [ ] `listNews()` 支持可选的 `sub_channel_id` 查询参数
- [ ] `bindSource()` 支持可选的 `sub_channel_id` 参数
- [ ] `updateChannelSource()` 支持可选的 `sub_channel_id` 参数
- [ ] 新增 `listSubChannels`、`createSubChannel`、`updateSubChannel`、`deleteSubChannel` 四个 API 函数
- [ ] 不传 `sub_channel_id` 时，现有调用方行为不受影响
- [ ] 前端 TypeScript 编译通过，无类型错误

---

## 6. 不在范围内

### 6.1 前端边界说明

v0.1 仅做前端**数据层适配**（类型定义 + API 函数参数同步 + 子频道 CRUD 函数定义），属于后端 API 变更的必然前端联动。以下前端工作不在 v0.1：

- **子频道筛选 UI**（频道空间下拉 + 子频道切换控件）→ v0.2
- **评分排序 UI**（按 importance_score 排序/筛选）→ v0.2
- **新闻卡片增强交互**（entities 展示、要点折叠、标签点击筛选）→ v0.2
- **侧边栏详情面板**（点击新闻展开侧边栏）→ v0.2
- **管理页子频道管理 UI**（创建/编辑/删除子频道的界面）→ v0.2

> **已知 UX 债务**：WebSocket 实时推送消息触发 `refreshNews()` 后，前端会全量重新请求新闻列表，产生 loading 闪烁。v0.2 考虑改为增量插入（仅将新消息 prepend 到本地列表），减少不必要的网络请求和 UI 跳动。

### 6.2 其他不在范围内

- **Source 创建 UI**：前端管理页面创建 Source → v0.3
- **统计卡片真实数据**：前端统计卡片仍为占位数据 → v0.3
- **X/Twitter combined 模式**：v0.1 只支持 search 和 user_timeline 两种独立模式
- **X/Twitter 网页爬虫**：仅使用官方 API，不引入爬虫方案
- **X Academic Research**：v0.1 仅支持 recent search（7天），不接入 full archive search
- **URL 规范化去重**：v0.1 只做精确匹配，不处理同内容不同 URL 的情况
- **processing_pipelines 表**：LLM 处理不引入独立 pipeline 管理
- **channel_space_shares 表**：频道空间间共享功能
- **全文搜索**：标签搜索、关键词搜索
- **通知推送**：Webhook、Email、Telegram 等
- **对外 API 开放**：第三方 API Key 申请和鉴权体系

---

## 7. 依赖与约束

### 7.1 外部依赖

| 依赖 | 用途 | 风险 |
|------|------|------|
| X API v2 (Basic) | Twitter 推文拉取 | 月费约 $100，有请求频率限制 |
| OpenAI 兼容 API | 翻译 + 摘要 + 结构化输出 | 成本随处理量增长 |
| Supabase PostgreSQL | 数据存储 | 免费版有配额和休眠风险 |

### 7.2 技术约束

- 保持"单体 + 双进程"运行形态（API + Worker）
- Worker 使用现有 asyncpg 直连（不引入 SQLAlchemy），保持与现有 `worker/db.py` 一致
- API 层使用 SQLAlchemy（保持与现有 `app/` 代码一致）
- **LLM 调用期间不持有数据库连接**：process_one 应在调用 `translate_and_summarize` 前释放数据库连接，LLM 返回后重新获取连接执行 INSERT。当前 `process_concurrency=1` 不暴露此问题，但设计上应尽早分离
- **部署顺序**：先执行数据库迁移（`db/migrations/v0.1.sql`），再部署新代码。迁移新增 `sub_channel_id` 列为 nullable，旧代码不受影响；新代码 INSERT 语句引用该列，必须先于代码存在

### 7.3 配置新增

`app/settings.py` 新增：
- `x_bearer_token: Optional[str] = None`

`.env.example` 同步更新，新增 `X_BEARER_TOKEN`。

> **命名约定**：pydantic-settings 自动将 `.env` 中的 `X_BEARER_TOKEN`（UPPER_CASE）映射为 Python 的 `x_bearer_token`（snake_case）。代码中引用 `settings.x_bearer_token`，环境变量中设置 `X_BEARER_TOKEN`。

### 7.4 数据库迁移

迁移脚本路径：`db/migrations/v0.1.sql`

> **迁移注意事项**：迁移使用 `IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` 确保幂等性，但无法处理部分执行失败的回滚场景（如 sub_channels 表已创建但后续 ALTER TABLE 失败）。建议在迁移脚本头部加注释提醒执行前备份，或在一个事务中执行全部 DDL（PostgreSQL 支持事务性 DDL）。

```sql
-- v0.1 数据层完善迁移

-- 1. 创建 sub_channels 表
CREATE TABLE IF NOT EXISTS sub_channels (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  channel_space_id UUID NOT NULL REFERENCES channel_spaces(id) ON DELETE CASCADE,
  name VARCHAR(100) NOT NULL,
  sort_order INT NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT uq_sub_channels_space_name UNIQUE (channel_space_id, name)
);

CREATE INDEX IF NOT EXISTS ix_sub_channels_space_sort
  ON sub_channels(channel_space_id, sort_order);

-- 2. channel_sources 新增 sub_channel_id 列
ALTER TABLE channel_sources
  ADD COLUMN IF NOT EXISTS sub_channel_id UUID REFERENCES sub_channels(id) ON DELETE SET NULL;

-- 3. processed_news 新增 sub_channel_id 列
ALTER TABLE processed_news
  ADD COLUMN IF NOT EXISTS sub_channel_id UUID REFERENCES sub_channels(id) ON DELETE SET NULL;

-- 4. processed_news 新增复合索引（子频道筛选 + 去重查询）
CREATE INDEX IF NOT EXISTS ix_processed_news_sub_published
  ON processed_news(channel_space_id, sub_channel_id, published_at DESC);

-- 5. raw_items.source_item_url 补充索引（去重查询加速）
CREATE INDEX IF NOT EXISTS ix_raw_items_url
  ON raw_items(source_item_url)
  WHERE source_item_url IS NOT NULL;
```

同时更新 `db/schema.sql`，将上述表定义合并到全量 schema 中（保持新部署时一份 SQL 可用）。

> **回滚说明**：v0.1 迁移为纯增量（ADD COLUMN nullable + CREATE TABLE IF NOT EXISTS），不修改或删除现有数据。如需回滚，执行以下 SQL 即可恢复 v0.1 之前的状态（不影响已有数据）：
> ```sql
> DROP TABLE IF EXISTS sub_channels CASCADE;
> ALTER TABLE channel_sources DROP COLUMN IF EXISTS sub_channel_id;
> ALTER TABLE processed_news DROP COLUMN IF EXISTS sub_channel_id;
> DROP INDEX IF EXISTS ix_processed_news_sub_published;
> DROP INDEX IF EXISTS ix_raw_items_url;
> ```
> 建议将上述回滚 SQL 保存为 `db/migrations/v0.1_rollback.sql`。

---

## 8. 风险与缓解

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|---------|
| X API 限流导致抓取失败 | 中 | 部分推文缺失 | fetch_policy 控制频率，429 自动退避重试 |
| LLM token 成本显著上升 | 高 | 单条处理成本上涨 2-3 倍 | Prompt 增强后输出从 ~100 token 增至 ~400 token（约 ¥0.0004/条）。对于个人使用规模（日处理 ~100 条），月成本约 ¥1-2，增长不显著。通过 `fetch_policy.budget.max_items_per_run` 控制处理量，后续可引入成本监控 |
| LLM 输出 JSON 解析失败 | 低 | 单条新闻处理失败 | 已有 `_extract_first_json_object` 兜底 + 输出校验修正 |
| LLM 输出字段缺失或类型错误 | 中 | 字段不完整 | 校验函数修正默认值，不丢弃 |
| 子频道概念增加绑定复杂度 | 低 | 用户需多选一个字段 | sub_channel_id 可选，不强制 |
| 删除子频道导致分类信息不可恢复 | 低 | 已分类新闻失去子频道归属 | v0.1 接受此风险；后续迭代可引入软删除或删除确认机制 |
| 数据库迁移影响现有数据 | 低 | 现有数据丢失 | 迁移只做 ADD COLUMN（nullable）+ CREATE TABLE，不删改现有字段 |

---

## 9. 后续迭代预览

### v0.2 — 前端体验
- 侧边栏新闻详情面板
- 子频道筛选 UI（后端 API + 前端类型已就绪）
- 评分排序、标签展示（后端 importance_score + tags 已就绪）
- 新闻卡片展示增强（实体识别 entities 展示）

### v0.3 — 生产就绪
- 子频道管理 UI（创建/编辑/删除，后端 API + 前端函数已就绪）
- Source 创建管理 UI
- 统计卡片真实数据
- 部署文档更新
- 管理页面完善

### 未来方向
- 财经频道空间（新 Source 类型：财经 RSS、股市 API 等）
- 体育、历史、军事等频道空间
- 对外 API 开放（API Key 申请、鉴权、配额）
- URL 规范化去重
- 全文搜索
- 通知推送（Webhook、Telegram）
