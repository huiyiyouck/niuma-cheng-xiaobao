# v0.6 迭代记录

## 概览
- 目标：Figma 原型前端重构 + X 信息 L0/L1 分层处理 + 空间图标上传
- 开始时间：2026-06-09
- 关闭时间：
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：实现
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版：明确 v0.6 三项范围、L0/L1 处理链路、UI 重构目标和空间图标上传边界 | UI / Architect / Developer / Tester / DevOps | Architect 需修改（11 条：3高/5中/3低）；UI 需修改（12 条：4高/5中/3低）；Developer 需修改（13 条：4高/6中/3低）；Tester 需修改（12 条：4高/5中/3低）；DevOps 需修改（12 条：4高/5中/3低，详见 v0.6-prd.md Review 记录） | R1 完成，等待 PM 汇总产出 R2 |
| R2 | PM 汇总五方 R1 + Owner 决策：收敛右侧抽屉布局、告警/日志路由合并、L0/L1 状态机、外部依赖降级、图标上传持久目录约束、mock 边界和不做原始数据治理能力 | UI / Architect / Developer / Tester / DevOps | Architect ⚠️ 有条件通过（R1 11 条全收敛；R2 新增内容追审 9 条：4 中 / 5 低）；UI ⚠️ 有条件通过（R1 12 条 3 完关 + 4 基本关 + 1 引入 #U13 + 4 未回应；R2 新增 #U13 路由对接细节 / #U14 上传交互形态）；Developer ⚠️ 有条件通过（R1 13 条：4 完关 + 5 基本关 + 1 合理分流 + 3 未关闭 #D2/#D5/#D7；R2 新增 #D14 错误分类语义冲突 / #D15 AC-32 阈值方向）；Tester ⚠️ 有条件通过（R1 12 条：2 完关 + 4 基本关 + 4 未关闭 #T5/#T6/#T8/#T10 + 2 低基本关；R2 新增 #T13 测试 fixtures 产出归属 / #T14 AC-33 测试基线不回归；4 阻断项全部到达门槛）；DevOps ⚠️ 有条件通过（R1 12 条：5 完关 + 4 基本关 + 2 合理分流 + 1 未关闭 #O8 日志轮转；R2 新增 #O13 LLM 供应商切换告警 / #O14 生产 .env 真实性验证；4 阻断项全到门槛）；PM 裁定不进入 R3，剩余条件由 UI 方案 / 设计 / 测试 / DevOps 阶段承接 | 已定稿 |

### UI 方案阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | UI 基于 PRD R2 定稿版 + Owner 对齐的 16 项决策清单（原型优先原则）产出 `iterations/v0.6-ui-spec.md`：覆盖 5 个核心页面 + 抽屉四段式 + 空间图标上传 + 监控页合并 + L0/L1 状态可视化 + PRD R2 AC 可见性映射；新建组件清单 + v0.5 组件迁移矩阵 + Tailwind/CSS 变量映射表 + API 契约清单（6 个新 endpoint） | Architect / Developer / Tester / PM | PM ⚠️ 有条件通过（#P1 已由 Owner 确认关闭；剩余 1 中 / 3 低）；Architect ⚠️ 有条件通过（10 条：2 高 / 5 中 / 3 低）；Developer ⚠️ 有条件通过（12 条：3 高 / 6 中 / 3 低）；Tester ⚠️ 有条件通过（11 条：3 高 / 5 中 / 3 低，详见 v0.6-ui-spec.md Review 记录） | R1 全部完成 |
| R2 | UI 精简修订：改 Developer #D1-#D3 三处事实错误（已删组件 / API 路径名 / CSS 变量名）+ #D4-#D5 SlidePanel 改造范围明确化 + PM #P3「28 条 AC」措辞统一为 AC-01~AC-35 + PM #P4 / Architect #A8 新建组件数口径统一 + §9 风险段扩到 9 条 + §9.2 设计阶段事项扩到 12 项 + 新增 §9.3 测试阶段承接 + §9.4 实施阶段建议；4 方 R1 共 38 条意见 0 高阻断，UI 裁定不开 R3 由后续阶段承接 | UI 自裁定 + 4 方 R1 承接 | UI ✅ 已定稿（R2） | 已定稿 |

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | Architect 基于 PRD R2 定稿版 + UI spec R2 定稿版产出 `iterations/v0.6-design.md`（1064 行 7 章）：覆盖 §2 数据模型（raw_items 状态字段 + processed_news × 6 列 + channel_spaces × 2 列 + tasks/alerts type 扩展 + ER 图）+ §3 API 契约（6 个新 endpoint + 现有扩展 + TS 类型新增）+ §4 核心流程（L0 classifier / L1 processor 5 子阶段 / dispatcher type 分支退避 / 外部服务集成 / 监控告警）+ §5 前端路由变更 + §6 schema/数据/代码迁移 + §7 7 项已知风险 + 5 个 ADR（ADR-002 raw_items 双字段 / ADR-003 jsonb 增量不拆表 / ADR-004 tasks 复用按 type 分支 / ADR-005 后端持久目录 / ADR-006 ILIKE 不上向量）；4 项开放问题移交实施和 DevOps。指定 Review 方：PM / Developer / DevOps / Tester | PM ⚠️ 有条件通过（无产品范围阻断；条件项：L0 `retryable` 不应作为未对齐前台用户态、level-status-counts 24h 窗口 SQL 口径需澄清、raw_items/ADR/AC 数量口径需统一、开放问题数量需同步）；Tester ⚠️ 有条件通过（11 条：3 高 / 5 中 / 3 低；高严重度集中在错误分类落地到代码层未定 / L1 5 阶段失败传播未定 / AC-32 告警 SQL 不完整；与 PM #P2 同源 #T5）；DevOps ⚠️ 有条件通过（11 条：3 高 / 5 中 / 3 低；高严重度集中在 uploads 目录创建/权限可执行清单缺失、nginx 部署同步路径与仓库现状脱节、npm 依赖增量未列 + native binding 红线未划）；Developer ⚠️ 有条件通过（10 条：2 高 / 5 中 / 3 低；2 高严重度都是工程量遗漏 #D1 workerLoop 主循环改造缺失 / #D2 llm.ts 双模型路径未给；详见 v0.6-design.md Review 记录）；4 方设计 R1 Review 全部收齐共识门槛到 | Review中（待 Architect 汇总 R2 或直接进实施阶段）|
| R2 | Architect 汇总 5 方 R1 共 41 条意见（8 高 / 17 中 / 16 低）产出 R2：关闭全部 8 高（#T1/#D6/#A12→§4.5 错误归档对照表 / #T2→§4.3.6 L1 失败传播+重试粒度 / #T3→§4.6 检测 SQL + tasks.last_error_kind / #O1→§4.7.1 部署前置工作 / #O2→选项 A 最小变动 / #O3→§6.3 npm 依赖增量清单 + 禁止 native binding / #D1→§4.4 workerLoop 完整改造 / #D2→§4.5+§6.3 callLLM<T> 抽象 helper）+ 5 关键中（#P2/#T5 SQL bug / #T8 上传兜底 / #O8 client_max_body_size / #D4 retry task type=l1_process / #D6 score_dimensions 统一）；24 条中/低承接到实施阶段 | PM / Developer / DevOps / Tester | PM ⚠️ 有条件通过（R2：PM R1 条件 #P1/#P2/#P4 已关闭，#P3 未关闭；R2 修改摘要与正文存在 5 处不一致 #P5）；Developer ⚠️ 有条件通过（R2：R1 高严重度 2 条全部关闭，R1 中低 4 条承接到实施阶段，R2 新增 #D11 l1_retry 创建时机歧义 + #D12 §4.3.6 交叉引用断链）；Tester ⚠️ 有条件通过（R2：R1 3 高全部关闭/基本关闭，R2 新增 #T13 R2 摘要与正文不一致影响 #T8 闭环）；DevOps ⚠️ 有条件通过（R2：R1 11 条 — 1 完全关闭 #O6 / 3 基本关闭 #O1/#O2/#O3（方向对、正文未落） / 6 合理分流 / 1 未关闭 #O8 client_max_body_size 1MB 边界 P0 风险；R2 新增 #O15 R2 摘要与正文 4 处不一致致部署侧执行依据缺失，与 PM #P5 / Tester #T13 同源）；PM 裁定不开 R3，R2 有条件定稿，摘要与正文不一致项不静默改 Architect 正文，转为实施/部署阶段显式承接 | 已定稿（有条件） |

#### 设计 R2 PM 裁定（2026-06-13）

- 裁定：不开 R3，设计 R2 有条件定稿，v0.6 进入实现阶段。
- 理由：
  - 四方 R2 复审均已完成且均为有条件通过，没有新的产品范围高阻断。
  - R1 高严重度工程与测试硬伤已由 R2 在方向上关闭或基本关闭：workerLoop、callLLM<T>、错误归档、L1 失败传播、重试粒度、tasks.last_error_kind 等核心接力点已具备。
  - 剩余共性问题集中在 R2 修改摘要与正文不一致，不改变 v0.6 三条主线范围，也不要求重新设计主方案。
- 实施阶段必须显式承接：
  - Developer：以 `v0.6-design.md` 正文为主，结合 R2 Review 记录承接 #D11 / #D12；首个实现记录需明确 `l1_retry` 创建语义、`§4.3.6 → §6.2` 断链如何落到迁移 DML 或实现记录。
  - Tester：测试计划承接 #T13；上传失败兜底、告警 SQL、v0.5 历史数据不回归先按 R2 Review 条件写成可验证假设，待 Developer 实现时对齐。
  - DevOps：部署就绪前自治承接 #O1 / #O2 / #O3 / #O8 / #O15，尤其是 `/var/lib/niuma-news/uploads/` 前置目录、nginx `/uploads/` 与 `client_max_body_size 2m`、依赖增量预审、native binding 红线和 .env 真实性检查。
  - PM：接受 #P3 / #P5 作为文档可信度遗留，不再要求 Architect 开 R3；后续若实现阶段发现范围或验收变化，再按 Change Note 或回到对应阶段处理。
- 下一步：切换到 Developer（开发工程师）启动实现阶段 R1。

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R1 | Developer 批次 A+B+C：schema + Worker + API（6 新增 endpoint + 3 修改 + multipart 上传） | d8e72db | 6e813ab | Architect（schema/退避/数据流）+ Tester（错误分类/退避可断言） | — | tsc 0 错误 | 已完成 |
| R2 | Developer 前端联调精修 + 左中右布局 + 管理页交互连续修复 + display_positions 软删除约束修复 | ce62e54 | 6e085d2 | Owner 浏览器验证 | 待 Owner 验证 | `npm run build` 0 错误 + test 环境部署通过；test 库手动 DROP 残留唯一约束后 curl 添加位置 409→成功 | 实现中 |
| R3 | Developer bug 收尾：v0.6 不放开 AI 处理；X/Twitter raw item 直接生成新闻页可展示内容；新增历史 X raw item 直显回填迁移 | 3c38859 | 待提交 | Owner 浏览器验证 | 待 Owner 验证 | `npm test -- src/__tests__/x-direct-display.test.ts` 通过；`npm run build` 通过 | 实现中 |

#### 前端联调精修——R2 概要（2026-06-14）

- **测试环境部署**：`frontend/dist/` rsync → `/var/www/test.huiyiyou.cloud/`，HTTPS + API 反代 → :8001 全链路通
- **角标**：`RootLayout.tsx` mock=3 → `getUnreadAlertsCount()` 真实轮询
- **NewsPage**：sort 参数对齐后端枚举 + stats 调 `/v1/stats?space_id=` + entities 对象数组/ tags_v2 {} 兜底修复 + 详情面板回滚嵌入式 + min-h-0 修可滚
- **监控页**：一键处理 `PATCH /v1/alerts/batch` + 日志按级别染色 + 告警→日志 60s 时间窗/关键词跳转+高亮
- **添加位置**：新建 `AddPlacementDialog`（复用 shadcn Dialog/Select/Button/Label）→ SourceDetailPage 接真实 API

#### 前端联调精修——R2 续修概要（2026-06-15）

- **左中右布局**：RootLayout 改 AppSidebar + 中栏，左栏一级导航/全局统计；test 增 IP 直连临时入口 `http://115.191.43.79`
- **浏览页**：空间/频道合并一行、频道 loading、评分四档配色、新闻卡精修、详情抽屉交互修复（异步回弹、ESC/X、列表让位）
- **管理页**：空间 append 末尾、拖拽排序、暂停/恢复乐观更新、状态色区分、全局 Toast、统一 Loading、创建/编辑提交动画、空间级重复拦截（一空间一位置）
- **源详情**：展示位置添加/暂停/恢复/移除反馈即时化；源级「恢复抓取」按钮移除，避免误导为全源恢复
- **监控页**：告警/日志加载态补齐，告警卡铺满内容区
- **后端修复**：`display_positions` 历史残留唯一约束未排除软删除导致软删除后无法重加，新增迁移 `0006_drop_dp_channel_unique`；test 库已手动 DROP 并 curl 实测成功
- **未完成/待验证**：Owner 继续 test 验证；生产部署需 DevOps 跑迁移并处理去软链；7+ 本地 commit 未 push

#### Bug 收尾——AI 不放开 + X/Twitter 直显（2026-06-28）

- Owner 决策：v0.6 当前只做 bug 收尾，**AI 处理不放开**；Twitter 抓到的内容直接展示在新闻页，后续 AI 处理由独立 AI 中枢管理。
- 代码收口：
  - 新增 `ENABLE_AI_PROCESSING` 开关，默认 `false`；默认状态下 worker 不 claim `l0_classify` / `l1_process` 任务。
  - `fetchAndIngest` 对 X/Twitter 新 raw item 默认排普通 `process` 任务；只有显式 `ENABLE_AI_PROCESSING=true` 时才进入 L0。
  - `processOne` 对 X/Twitter 始终直显；AI 未启用时其他 source 也走直显兜底，不调用 `processLLM`。
  - 直显新闻写入 `processed_news` + `news_positions`，并把 raw item 标记为 `l0_status='skipped' / l1_status='completed'`，避免统计缺口。
  - 新增迁移 `0007_backfill_x_direct_display.sql`：回填已有未处理 X raw item，关联展示位置，并把遗留 queued L0/L1 任务标记为 succeeded。
- 验证：`npm test -- src/__tests__/x-direct-display.test.ts` 通过；`npm run build` 通过。
- 下一步：Owner 继续报具体 bug；若无 bug，切 DevOps 部署生产（需执行迁移 0006 + 0007，并保持不设置 `ENABLE_AI_PROCESSING=true`）。

#### #D11 处置（2026-06-13 实现阶段 R1 定稿）

- 决定：不创建 `l1_retry` task，workerLoop 不实现 `l1_retry` claim 分支。手动重试（POST /v1/l1-tasks/:id/retry，批次 C）创建 `l1_process` + 应用层标注触发来源
- 与设计正文 §4.4 偏差：§4.4 BACKOFF_CONFIG 含 `l1_retry` 配置 + workerLoop 含第 5 个 claim 分支——代码不照搬
- 授权：PM 设计 R2 裁定授权 Developer 承接 #D11，非静默改设计

#### L1 外部搜索 / OpenClaw 集成（2026-06-13；2026-06-16 smoke 更新）

- 决定：v0.6 L1 Stage 3 外部搜索保持空壳；后续将整个 L1 处理链路替换为 OpenClaw Agent 调用
- 方案：主 Server 只做 raw_item fetch → 调 OpenClaw Agent（`openclaw agent --json`，tool-use 自主调度 KB/链接/Web/X 搜索）→ 存结果
- 调研结论：
  - OpenClaw 已部署在服务器（18789 端口），提供 WebSocket Gateway + CLI
  - CLI 已验证可安装（`npx openclaw` v2026.6.6），`agent` 子命令支持 `--json` 结构化输出
  - 本地可 SSH 隧道连接 Gateway（需要在服务器侧运行 CLI，因认证 token 在服务器本地）
- 2026-06-16 smoke 结论：
  1. `openclaw status` 可见 `news-l1` agent 会话和本地 Gateway 服务；Gateway 有 scope upgrade pending 提示，但不阻断 `--local` agent smoke
  2. `openclaw capability web search --query ... --json --limit 3` 通过，provider=tavily，返回真实搜索结果
  3. `openclaw agent --agent news-l1 --local --message ... --json --timeout 120` 通过，返回严格 JSON，且 `toolSummary` 显示实际调用 `web_search`
  4. 直接调用当前平台代码 `webSearch()` + `processL1ViaAgent()` 通过，可得到平台 `L1Output`，`npm run build` 0 错误
  5. `news_test` 单条真实 raw_item / worker 端到端通过：插入 `l1_process` queued 任务 `6de4139b-3284-421f-bc2d-f91d240f1b62`，调用一次 `workerLoop()` 后任务 `succeeded`；`raw_items.l1_status=completed`；新建 `processed_news` `609c20aa-ab65-4b95-8933-ce8d7d5f40ff`；`tags_v2.processing` 含 `engine:agent`；`source_refs.search_fetched=true`；fan-out 到 1 个 `news_positions`；耗时约 117s
- 偏差修正：`tasks` 表当前无 `metadata` 列，#D11 原记录中的 `metadata.triggered_by='manual'` 不成立；实际实现为创建新的 `l1_process` task，不记录 metadata 触发来源
- 2026-06-28 收口修正：该技术验证结果保留，但 v0.6 **不放开 AI 处理**；不再执行 OpenClaw 3-5 条小批量观察。当前生产策略为 `ENABLE_AI_PROCESSING=false`，X/Twitter raw item 直接展示，后续 AI 处理由独立 AI 中枢管理。
- 注册：已在 INDEX.md 跨任务待办登记为 P1，归属 Developer；状态已按 2026-06-28 收口决策更新。

#### 实现阶段任务拆分（Developer，2026-06-13 产出，待 Owner 审）

> 本拆分以 `v0.6-design.md` 正文为主，承接 PM 设计 R2 裁定指派给 Developer 的条件。工程量大，按依赖顺序分 4 批，每批独立 commit + 独立验证，不在一轮塞完。每批后端契约变更同 commit 跑前端引用 grep（依据 `role-developer.md §跨轮契约变更同步`）。
>
> 批次依赖：A（schema/测试基建）→ B（Worker，依赖 A 的列）→ C（API，依赖 A 的列 + B 的产出字段）→ D（前端，依赖 C 的契约）。A 是所有后续批次的前置。

**承接条件登记（来自设计 R2 PM 裁定 + Developer R2 挂账）**：

| 编号 | 内容 | 处置批次 | 处置方案（待动手定稿） |
|------|------|----------|----------------------|
| #D11 | `l1_retry` task type 创建时机歧义（§3.2 说手动重试创建 `l1_process`，§4.4 又给 `l1_retry` 独立退避配置 + workerLoop claim 分支 → 无人创建则为死代码） | B | 倾向方案 B：**不创建 `l1_retry` task**，手动重试创建 `l1_process`；workerLoop 不实现 `l1_retry` claim 分支。代码与设计正文 §4.4 的偏差在首个实现记录显式注明（PM 裁定授权 Developer 承接 #D11，非静默改设计）。2026-06-16 复核：`tasks` 表无 `metadata` 列，不能记录 `metadata.triggered_by='manual'`，按现有 schema 不额外记录触发来源 |
| #D12 | §4.3.6 引用「§6.2 迁移 DML」做 v0.5 历史保护，但 §6.2 是「零数据迁移」无对应 DML（断链） | A | 倾向方案 (a)：迁移内补保护性 `UPDATE`，把存量 `processed_news` 对应的 `raw_items` 直接置 `l1_status='completed'`（避免被 L1 重新处理）；在迁移文件注明此 DML 补 §4.3.6 引用缺口 |
| #D3 | uploads 路径暴露 `space_id` UUID，nginx `/uploads/` 无 `internal` 指令 | C / DevOps 协同 | 评估 API 透传或访问限制；nginx 部分由 DevOps 承接（#O15），Developer 侧确保接口不依赖目录可直接遍历 |
| #D5 | `GlobalLevelStatusCounts` 前端无落点（§5.2 组件表缺行） | C+D | 后端 `GET /v1/global-level-status-counts` 已在 §3.2 定义；前端在 MonitoringPage 全局视图补对接组件 |
| #D7 | `source_refs` 扩展结构 0 字定义 | B | L1 Stage 5 实现时定义具体结构，或在实现记录声明本期不扩展（沿用 v0.5 结构） |
| #D9 | 旧路由文件 `AlertsPage.vue` / `LogsPage.vue` 删除归属 | D | 路由合并同 commit `git rm`，走受保护路径删除门禁（`frontend/src/` 受保护，需 Architect Review 或 Owner 零引用授权） |
| 条件 D | v0.5 测试当前禁用（`__tests_disabled__/` + `passWithNoTests`） | A | 实施前恢复独立 test DB + `.env.test` + vitest include + 修复 4 个已有失败测试 |

**批次 A · 数据层 + 测试基建**（R1 首轮，无前置依赖）

- 涉及文件：`server/src/db/schema.ts`（drizzle 表定义扩展）、`server/drizzle/0001_*.sql`（生成的增量迁移）、`server/.env.test`（新建）、`server/vitest.config.ts`（恢复 include）、`server/src/__tests_disabled__/` → 恢复
- 任务：
  1. drizzle schema 扩展：`raw_items` ×7 列（含 `l0_label`/`l1_next_retry_at`，修正 R2 #P5「7 列实际 9 列」口径——以 §2.1 DDL 9 个 ADD COLUMN 为准）、`processed_news` ×6 列、`channel_spaces` ×2 列、`tasks` ×1 列（`last_error_kind`）、`raw_items` ×2 部分索引
  2. `drizzle-kit generate` 产出 `0001_*.sql` 增量迁移（ADR-001，不复制粘贴 baseline）
  3. **#D12 历史保护 DML**：迁移内追加 `UPDATE raw_items SET l1_status='completed' WHERE id IN (SELECT raw_item_id FROM processed_news)`（方案 a，注明补 §4.3.6 引用缺口）
  4. **条件 D**：恢复 test DB + `.env.test`（独立库，非生产）+ vitest config + 修复 4 个已有失败测试（教训：2026-06-08 测试直连生产 DB 事故）
- 接口契约：本批不涉及 API，纯 schema
- 验证点：`drizzle-kit generate` 无报错；迁移在 test DB 幂等可重跑；`npm test` 恢复后绿；存量行 default 赋值正确 + 历史保护 DML 命中行数与 `processed_news` 行数一致
- 建议 Review 方：Architect（schema DDL / 索引 / ADR 一致性）+ Tester（测试基建恢复 / 不回归基线）

**批次 B · 后端 Worker**（依赖 A 列）

- 涉及文件：新建 `server/src/worker/l0-classifier.ts`、`l1-processor.ts`、`l1-monitor.ts`；改造 `dispatcher.ts`（`requeueTask` type 分支退避）、`worker/index.ts`（workerLoop 加 claim 分支 + `l1Sem`）；扩展 `llm.ts`（`callLLM<T>` 抽象 + 双模型）；`config.ts`（env 增量）；`processor.ts` 保留不变
- 任务：
  1. `llm.ts`：抽 `callLLM<T>(prompt, opts)` helper，`processLLM` 重构为其封装（向前兼容 v0.5），新增 `classifyL0LLM` / `processL1LLM` 双模型（§4.5）
  2. `dispatcher.ts`：`BACKOFF_CONFIG` 按 type 分支退避，`requeueTask` 达上限分流 `setL0Failed` / `setL1FinalFailed`（§4.4）
  3. `worker/index.ts`：workerLoop 加 `l0_classify`/`l1_process` claim 分支 + `l1Sem`（**#D11：不加 `l1_retry` 分支**）
  4. `l0-classifier.ts`：L0 判定 + 错误三档重试（§4.2）
  5. `l1-processor.ts`：5 阶段（库内检索 / 链接读取 / 外部搜索 / LLM 主调用 / 写入分发）+ §4.3.6 失败传播 + `last_error_kind` 归一化（§4.5 对照表）；**#D7：定义或声明 `source_refs` 结构**
  6. `l1-monitor.ts`：AC-32 四类告警生成（§4.6）
  7. config + `.env.example`：L0/L1/KB/link/upload/search 全部 env 增量同步（§4.5）
- 接口契约：内部 worker，不对前端；产出落 `processed_news` 新列 + `raw_items` l0/l1 状态
- 验证点：TDD 单测覆盖 `callLLM` 双模型切换、`BACKOFF_CONFIG` 退避计算、L1 失败传播三档、L0 skip 边界；`npm run build` 0 错误；本地 worker 跑通一条 raw_item 全链路
- 建议 Review 方：Architect（dispatcher/退避/数据流）+ Tester（错误分类可 mock / 退避可断言）

**批次 C · 后端 API**（依赖 A 列 + B 产出字段）

- 涉及文件：扩展 `routes/news.ts`、`spaces.ts`、`sources.ts`（或新建 `routes/level-status.ts`）、`stats.ts`、`alerts.ts`；新增 `@fastify/multipart` 依赖（图标上传）
- 任务：
  1. `GET /v1/news` 扩展 `score_total`/`tags_v2`；新增 `GET /v1/news/:id`（NewsDetail 完整结构，§3.2）
  2. `POST /v1/spaces/:id/icon` + `DELETE /v1/spaces/:id/icon`（multipart，§4.7 + 失败兜底，**#D3** 访问控制）
  3. `GET /v1/sources/:id/level-status-counts` + `GET /v1/global-level-status-counts`（§3.2 修正后 SQL，**#D5**）
  4. `POST /v1/l1-tasks/:task_id/retry`（**#D11：创建 `l1_process` + `metadata.triggered_by='manual'`**）
  5. `GET /v1/stats` 口径切换；`GET /v1/alerts` type 筛选扩展
- 接口契约：6 新增 + 3 修改 endpoint，camelCase，TS 类型与 `frontend/src/lib/types.ts` 1:1
- 验证点：每个 endpoint 单测（含 multipart 校验 / SQL 计数 / retry 幂等）；契约字段与设计 §3.2/§3.4 逐项对齐；`npm run build` 0 错误
- 建议 Review 方：Architect（契约覆盖率 / SQL 语义）+ Tester（28 AC 映射 / 上传异常 3 类）

**批次 D · 前端**（依赖 C 契约）

- 涉及文件：`router.ts`、新建 `MonitoringPage.vue` + 12-16 个新组件（§5.3）、微调 `NewsPage.vue`、重构 `SourceDetailPage.vue`、扩展 `lib/types.ts` + `lib/api.ts`；**#D9：`git rm` `AlertsPage.vue` / `LogsPage.vue`**
- 任务：
  1. `types.ts`：L0/L1 状态枚举 + `TagV2` + `ScoreDimensions` + `NewsDetail` + `ContextParagraph`（§3.4）
  2. 路由合并 `/monitoring` + 旧路由 redirect + TopNav 3 项 + 角标（§5.1）；旧页面 `git rm` 走门禁
  3. MonitoringPage + AlertsTab/LogsTab；NewsDetailDrawer 六段式（复用 `SlidePanel`/`NewsDetailPanel` 外壳）
  4. SpaceIcon / IconUploader（4 视觉态 + 上传交互）；SourceDetailPage 双栏 + 时间轴 + L0/L1 计数（**#D5** 全局计数落点）
  5. v0.5 历史数据降级：`tags_v2 || { processing: tags }`（§6.2）
- 接口契约：消费批次 C 的 endpoint，字段名与后端响应核对
- 验证点：`cd frontend && npm run build` 0 TS 错误（教训：v0.5.1 重构 31 个 TS 错误）；旧路由 redirect 生效；全仓 grep 确认无残留旧组件引用
- 建议 Review 方：UI（视觉对齐原型 / 4 视觉态）+ Tester（前端展示类 AC / 路由跳转）

**待 Owner / PM 确认的决策点**：
1. 实现阶段每轮 Review 方是否按上表「建议」执行（动态 Review 至少 2 方）；前端批次是否纳入 UI 复审。
2. #D11 是否同意「移除 `l1_retry` 创建路径」方向（与设计正文 §4.4 有显式偏差，需 PM/Architect 认可）。
3. #D12 历史保护是否走方案 (a)（迁移内 UPDATE）。
4. 外部搜索选型（§7.3 开放问题）本期是否落地，还是 X/Web 搜索按「失败可降级」先留空壳（P2）。

#### 前端先行实施进展（2026-06-13，Owner 调整为前端先行）

**Owner 决策（覆盖上面 A→D 后端先行拆分）**：前端先行，4 页全部用 mock 搭完后一起 review，再做后端（后端暂停）。前端规则见 memory `v0.6-frontend-rebuild-rules`：Vue + **1:1 还原原型** `/root/news-aggregation-platform` + 引入 **Tailwind v4 / shadcn 主题**（显式偏离 UI spec §4.1「不引入 Tailwind」，Owner 兼 PM 已认可）+ 组件化可复用。

**已完成（vite build 0 错误 + playwright 截图自查 1:1 通过 + test 环境验证）**：
- 底座：`@tailwindcss/vite` + 移植原型 shadcn 主题 `frontend/src/styles/theme.css` + 入口 `styles/index.css` + `lib/utils.ts`(cn，依赖 clsx+tailwind-merge) + vite/main 配置；新增依赖 `tailwindcss` `@tailwindcss/vite` `clsx` `tailwind-merge` `lucide-vue-next`
- `router.ts`：`/news` `/admin` `/monitoring` `/sources/:id` + `/alerts`→`?tab=alerts` `/logs`→`?tab=logs` redirect
- `App.vue`：原型 RootLayout 顶部导航（浏览/管理/监控+红色角标，mock count=3）
- `views/NewsPage.vue`：列表**居中聚焦**（`max-w-3xl mx-auto`，Owner review 后脱离原型左右布局——原型右侧常驻面板是半成品占位）+ `components/news/NewsDetailDrawer.vue` **侧边详情抽屉**（480px 右滑 + 遮罩 + ESC/×/点遮罩关闭，符合 UI spec D12-D14）；抽屉内容当前为标题/来源/综合分/摘要/标签，**完整六段（翻译/背景补全段落+可信度+来源标签/4 维评分/五类标签）待后端 `GET /v1/news/:id` 契约后补**
- `views/MonitoringPage.vue`：1:1（告警/日志双 Tab，初始 tab 读 `?tab=`）
- `views/SourceDetailPage.vue`：1:1 主体（双栏；编辑/删除/添加位置按钮已绑 `editDrawer`/`deleteDialog`/`addPlacementDrawer` 状态，抽屉 UI 待接）
- 复用组件：`components/ui/StatCard.vue` `ScoreBadge.vue` `Badge.vue`、`components/news/NewsCard.vue`；mock 集中 `lib/mock.ts`

**剩余未完成（交下一周期新会话 Developer 接续）**：
1. **AdminPage 双 Tab**（最大块，原型子组件合计 2169 行待转译）：
   - `views/AdminPage.vue`（双 Tab 外壳，原型 `AdminPage.tsx` 56 行，简单）
   - `components/admin/SpacesManagement.vue`（原型 562 行：空间管理 + 频道树 + **空间图标上传 4 视觉态**）
   - `components/admin/SourceLibrary.vue`（原型 404 行：信息源库表格）
   - 共用抽屉/对话框（SourceDetailPage 也用）：`AddSourceDrawer`(536) `SourceEditDrawer`(195) `DeleteConfirmDialog`(260) `SpaceEditDialog`(115) `ChannelEditDialog`(97)
   - 现状：`views/AdminPage.vue` 仍是 v0.5 旧内容，test 环境 `/admin` 显示旧页
2. **SourceDetailPage 抽屉接入**：上述共用抽屉做完后接回源详情页 3 个按钮
3. **#D9 旧路由文件删除**：`views/AlertsPage.vue` `LogsPage.vue` 已被 router 弃用、无引用，待走受保护路径删除门禁 `git rm`
4. **可选**：评分滑块/排序下拉现用原生元素近似，需精确 Radix 视觉可引 `reka-ui`

**接续指引（新会话 Developer）**：原型源码 `/root/news-aggregation-platform/src/app/`（pages/ + components/admin/ + components/ui/）；转译照已完成页面的套路（className 直接搬、`lucide-react`→`lucide-vue-next`、`useState`→`ref`、Radix→原生或 reka-ui）；每完成一页 `npx vite build` 验证；截图自查用 `/tmp/pwenv/bin/python`（playwright+chromium 已装）对 `npm run dev` 起的 server 截图。

**部署**：test 测试环境已上线 **http://test.huiyiyou.cloud**（HTTP，独立目录 `/var/www/test.huiyiyou.cloud`，nginx `sites-available/test.huiyiyou.cloud`；DNS 已解析，无 TLS 证书，需 https 另跑 certbot）。⚠️ **生产 `news.huiyiyou.cloud` 软链接 → `frontend/dist`，已被本会话 `vite build` 覆盖为 v0.6 开发版前端**；如需恢复 v0.5 生产由 DevOps 处理。

### 测试阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|
| ✅ 部署通过 | test（`test.huiyiyou.cloud` / IP 115.191.43.79 / `news_test` / :8001） | news-api-test active、隔离 `/srv/niuma-news/test/server`、0007 回填 154 条 X 直显、公网首页/bundle/反代全通 | 无 |
| ✅ 部署通过 | 生产（`news.huiyiyou.cloud` / `news` / :8000） | news-api active、隔离 `/srv/niuma-news/prod/server`、去软链真目录、schema 对齐、公网首页/bundle/反代全通、canary 实证不污染 | 无（生产库空需 Owner 配源） |

#### 部署阶段记录（DevOps，2026-06-28）

本次同时完成 #142 去软链接化 + 前后端全隔离 + v0.6 上线 test 与生产。

**架构变更**：

| 维度 | 变更前 | 变更后 |
|------|--------|--------|
| 生产前端 | `/var/www/news.huiyiyou.cloud` 软链 → `frontend/dist`（build 即污染生产） | 真实独立目录，`build`→rsync 分发 |
| 后端运行 | 生产/测试共享 `/root/Project/.../server`（git pull 同改两者） | 隔离至 `/srv/niuma-news/{prod,test}/server`（systemd WD） |
| 迁移机制 | unit `ExecStartPre=drizzle-kit migrate` | 去除（库 schema 由 db:push 建、drizzle 元数据脱轨，强行 migrate 会重放撞 schema）；schema 变更走人工 |
| 部署流程 | 手工 | 固化 `deploy/deploy.sh`（build→rsync 分发 test/prod） |

**5 项检查**：环境确认 ✅（prod `.env`→`news` 库/8000/AI 未放开；test→`news_test`/8001；`.env` 不分发）；配置验证 ✅（nginx root 不变、admin 靠 IP 白名单、`nginx -t` 通过）；构建产物 ✅（同步 `92beb8e`、前端 build 0 错误、后端依赖幂等）；健康检查 ✅（两服务 active、`/health` 200、公网全通、canary 实证）；回滚准备 ✅（备份 `/var/backups/niuma-news/*-pre-v0.6-20260628-*` + unit `.bak`）。

**schema 对齐**：生产残留 0006 唯一约束 `display_positions_source_id_channel_space_id_channel_id_key` 已 `DROP CONSTRAINT IF EXISTS`（两库约束 7→6 一致）；0007 直显回填 test `INSERT 7/UPDATE 154`、生产 `INSERT 0/UPDATE 0`（生产空库无影响）。

**遗留/移交（含 2026-07-01 收尾更新）**：① 生产库原几乎空 → 已按 Owner 要求将 `news_test` 业务数据迁移到生产（AI/财经 2 空间 + 4 源 + 154 新闻，worker 持续抓取现增至约 320）；数据来龙去脉见 devops 日志（**非删除**，原数据一直在 news_test，此前生产后端长期 inactive）；② X sync 一度 403（生产 admin token 严格校验），交研发后开发重新部署修复（前端 `index-Ufw1OiMD.js`，转 200），且未破坏隔离架构；③ 迁移机制脱轨（db:push 历史 + drizzle 元数据仅 1 条）是既有技术债，两 unit 去 `ExecStartPre migrate`，后续如恢复 migrate 机制需单独对齐元数据。

## Review 记录

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|

## 迭代关闭归档
- 关闭结论：可关闭 / 不可关闭 / 有条件关闭
- 未关闭原因或延期项：
- 已生成 summary：
- 已更新 `docs/progress/INDEX.md`：
- 已更新角色日志：
- 已更新知识库：
- 遗留问题下一步：
