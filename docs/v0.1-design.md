# 程牛马小报 v0.1 详细设计文档

| 字段 | 值 |
|------|-----|
| 版本 | v0.1 |
| 状态 | 待评审 |
| 日期 | 2026-05-20 |
| 关联 PRD | `docs/v0.1-prd.md` |

---

## 1. 概述

本文档是 v0.1 迭代（数据层完善）的详细设计，供开发者直接参考实现。v0.1 的范围：

- 新增子频道数据模型与 CRUD API
- 新增 X/Twitter Source type
- 增强 LLM 输出为完整结构化字段
- 子频道内跨源去重

前端不在本迭代范围内。

---

## 2. 数据库变更

### 2.1 新增表：sub_channels

```sql
CREATE TABLE IF NOT EXISTS sub_channels (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  channel_space_id UUID NOT NULL REFERENCES channel_spaces(id) ON DELETE CASCADE,
  name VARCHAR(100) NOT NULL,
  sort_order INT NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT uq_sub_channels_space_name UNIQUE (channel_space_id, name)
);

CREATE INDEX IF NOT EXISTS ix_sub_channels_space_sort
  ON sub_channels(channel_space_id, sort_order);
```

### 2.2 ALTER TABLE：channel_sources

```sql
ALTER TABLE channel_sources
  ADD COLUMN IF NOT EXISTS sub_channel_id UUID REFERENCES sub_channels(id) ON DELETE SET NULL;
```

### 2.3 ALTER TABLE：processed_news

```sql
ALTER TABLE processed_news
  ADD COLUMN IF NOT EXISTS sub_channel_id UUID REFERENCES sub_channels(id) ON DELETE SET NULL;
```

### 2.4 新增索引

```sql
-- 子频道筛选 + 去重查询
CREATE INDEX IF NOT EXISTS ix_processed_news_sub_published
  ON processed_news(channel_space_id, sub_channel_id, published_at DESC);

-- 去重查询加速
CREATE INDEX IF NOT EXISTS ix_raw_items_url
  ON raw_items(source_item_url)
  WHERE source_item_url IS NOT NULL;
```

### 2.5 全量 Schema 同步

更新 `db/schema.sql`，将 `sub_channels` 表和新增字段/索引合并进去，保持新部署时一份 SQL 可用。

---

## 3. SQLAlchemy Model 变更

### 3.1 新增文件：`app/models.py` 中增加 SubChannel 类

```python
class SubChannel(Base):
    __tablename__ = "sub_channels"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    channel_space_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("channel_spaces.id"), nullable=False)
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    sort_order: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=utcnow, nullable=False)

    __table_args__ = (
        UniqueConstraint("channel_space_id", "name", name="uq_sub_channels_space_name"),
        Index("ix_sub_channels_space_sort", "channel_space_id", "sort_order"),
    )
```

### 3.2 修改：ChannelSource 类新增字段

```python
sub_channel_id: Mapped[Optional[uuid.UUID]] = mapped_column(
    UUID(as_uuid=True), ForeignKey("sub_channels.id"), nullable=True
)
```

### 3.3 修改：ProcessedNews 类新增字段

```python
sub_channel_id: Mapped[Optional[uuid.UUID]] = mapped_column(
    UUID(as_uuid=True), ForeignKey("sub_channels.id"), nullable=True
)
```

---

## 4. Pydantic Schema 变更

### 4.1 新增文件：`app/schemas.py` 中增加

```python
class SubChannelCreate(BaseModel):
    name: str = Field(min_length=1, max_length=100)
    sort_order: int = Field(default=0, ge=0)

class SubChannelUpdate(BaseModel):
    name: Optional[str] = Field(default=None, min_length=1, max_length=100)
    sort_order: Optional[int] = Field(default=None, ge=0)

class SubChannelOut(BaseModel):
    id: uuid.UUID
    channel_space_id: uuid.UUID
    name: str
    sort_order: int
    created_at: datetime
```

### 4.2 修改：ChannelSourceBind

```python
class ChannelSourceBind(BaseModel):
    source_id: uuid.UUID
    enabled: bool = True
    fetch_policy: dict = Field(default_factory=dict)
    sub_channel_id: Optional[uuid.UUID] = None  # 新增
```

### 4.3 修改：ChannelSourceOut

```python
class ChannelSourceOut(BaseModel):
    id: uuid.UUID
    channel_space_id: uuid.UUID
    source_id: uuid.UUID
    enabled: bool
    fetch_policy: dict
    sub_channel_id: Optional[uuid.UUID] = None  # 新增
    created_at: datetime
```

### 4.4 修改：ChannelSourceUpdatePolicy

```python
class ChannelSourceUpdatePolicy(BaseModel):
    enabled: Optional[bool] = None
    fetch_policy: Optional[dict] = None
    sub_channel_id: Optional[uuid.UUID] = None  # 新增，设为 None 时表示不修改
```

注意：用户如需清除 sub_channel_id，需传一个特殊值或另加 `clear_sub_channel` 字段。v0.1 简化处理：`sub_channel_id` 只能设置，不能清除（除非通过删除子频道触发 ON DELETE SET NULL）。如需清除，可在后续迭代扩展。

### 4.5 修改：ProcessedNewsOut

```python
class ProcessedNewsOut(BaseModel):
    id: uuid.UUID
    channel_space_id: uuid.UUID
    raw_item_id: uuid.UUID
    sub_channel_id: Optional[uuid.UUID] = None  # 新增
    title: str
    summary: str
    language: str
    source_refs: dict
    published_at: Optional[datetime]
    bullets: list[str]
    tags: list[str]
    entities: list[dict]
    importance_score: float
    created_at: datetime
```

---

## 5. API 实现细节

### 5.1 子频道 CRUD

在 `app/routes.py` 中新增以下路由。这些路由均在 `AdminIPWhitelistMiddleware` 保护下（POST/PUT/DELETE 需要白名单 IP）。

#### POST /v1/channel-spaces/{channel_space_id}/sub-channels

```python
@router.post("/channel-spaces/{channel_space_id}/sub-channels", response_model=SubChannelOut)
async def create_sub_channel(
    channel_space_id: uuid.UUID,
    payload: SubChannelCreate,
    session: AsyncSession = Depends(get_session),
):
    # 1. 验证 channel_space_id 存在
    exists = (await session.execute(
        select(ChannelSpace.id).where(ChannelSpace.id == channel_space_id)
    )).scalar_one_or_none()
    if not exists:
        raise HTTPException(status_code=404, detail="channel_space not found")

    # 2. 创建子频道
    obj = SubChannel(
        channel_space_id=channel_space_id,
        name=payload.name,
        sort_order=payload.sort_order,
    )
    session.add(obj)
    await session.commit()
    await session.refresh(obj)
    return SubChannelOut.model_validate(obj, from_attributes=True)
```

#### GET /v1/channel-spaces/{channel_space_id}/sub-channels

```python
@router.get("/channel-spaces/{channel_space_id}/sub-channels", response_model=list[SubChannelOut])
async def list_sub_channels(
    channel_space_id: uuid.UUID,
    session: AsyncSession = Depends(get_session),
):
    rows = (await session.execute(
        select(SubChannel)
        .where(SubChannel.channel_space_id == channel_space_id)
        .order_by(SubChannel.sort_order, SubChannel.created_at)
    )).scalars().all()
    return [SubChannelOut.model_validate(r, from_attributes=True) for r in rows]
```

#### PUT /v1/sub-channels/{sub_channel_id}

```python
@router.put("/sub-channels/{sub_channel_id}", response_model=SubChannelOut)
async def update_sub_channel(
    sub_channel_id: uuid.UUID,
    payload: SubChannelUpdate,
    session: AsyncSession = Depends(get_session),
):
    row = (await session.execute(
        select(SubChannel).where(SubChannel.id == sub_channel_id)
    )).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="sub_channel not found")

    values = {}
    if payload.name is not None:
        values["name"] = payload.name
    if payload.sort_order is not None:
        values["sort_order"] = payload.sort_order
    if values:
        await session.execute(
            update(SubChannel).where(SubChannel.id == sub_channel_id).values(**values)
        )
        await session.commit()
    row = (await session.execute(
        select(SubChannel).where(SubChannel.id == sub_channel_id)
    )).scalar_one()
    return SubChannelOut.model_validate(row, from_attributes=True)
```

#### DELETE /v1/sub-channels/{sub_channel_id}

```python
@router.delete("/sub-channels/{sub_channel_id}", status_code=204)
async def delete_sub_channel(
    sub_channel_id: uuid.UUID,
    session: AsyncSession = Depends(get_session),
):
    row = (await session.execute(
        select(SubChannel).where(SubChannel.id == sub_channel_id)
    )).scalar_one_or_none()
    if not row:
        raise HTTPException(status_code=404, detail="sub_channel not found")
    await session.delete(row)
    await session.commit()
```

### 5.2 绑定接口变更

#### POST /v1/channel-spaces/{channel_space_id}/sources（创建绑定）

在现有 `bind_source` 函数中，`ChannelSource` 构造时增加 `sub_channel_id`：

```python
obj = ChannelSource(
    channel_space_id=channel_space_id,
    source_id=payload.source_id,
    enabled=payload.enabled,
    fetch_policy=payload.fetch_policy,
    sub_channel_id=payload.sub_channel_id,  # 新增
)
```

需新增校验：如果 `sub_channel_id` 非空，检查它是否属于当前 `channel_space_id`：

```python
if payload.sub_channel_id:
    sc_exists = (await session.execute(
        select(SubChannel.id).where(
            SubChannel.id == payload.sub_channel_id,
            SubChannel.channel_space_id == channel_space_id,
        )
    )).scalar_one_or_none()
    if not sc_exists:
        raise HTTPException(status_code=400, detail="sub_channel not found in this channel_space")
```

`_channel_source_out` 转换函数需更新（当前代码 `app/routes.py:31-41`）：

```python
def _channel_source_out(r: ChannelSource) -> ChannelSourceOut:
    return ChannelSourceOut.model_validate(
        {
            "id": r.id,
            "channel_space_id": r.channel_space_id,
            "source_id": r.source_id,
            "enabled": r.enabled,
            "fetch_policy": _as_dict(r.fetch_policy),
            "sub_channel_id": r.sub_channel_id,  # 新增
            "created_at": r.created_at,
        }
    )
```

#### PUT /v1/channel-sources/{channel_source_id}（更新绑定）

在现有 `update_channel_source` 函数中，处理 `payload.sub_channel_id`：

```python
if payload.sub_channel_id is not None:
    # 校验 sub_channel_id 属于同一个 channel_space
    cs_row = (await session.execute(
        select(ChannelSource.channel_space_id).where(ChannelSource.id == channel_source_id)
    )).scalar_one()
    sc_exists = (await session.execute(
        select(SubChannel.id).where(
            SubChannel.id == payload.sub_channel_id,
            SubChannel.channel_space_id == cs_row,
        )
    )).scalar_one_or_none()
    if not sc_exists:
        raise HTTPException(status_code=400, detail="sub_channel not found in this channel_space")
    values["sub_channel_id"] = payload.sub_channel_id
```

### 5.3 新闻列表查询变更

#### GET /v1/channel-spaces/{channel_space_id}/news

在现有 `list_news` 函数中，新增可选查询参数 `sub_channel_id`：

```python
@router.get("/channel-spaces/{channel_space_id}/news", response_model=list[ProcessedNewsOut])
async def list_news(
    channel_space_id: uuid.UUID,
    limit: int = Query(default=20, ge=1, le=100),
    offset: int = Query(default=0, ge=0),
    sub_channel_id: Optional[uuid.UUID] = Query(default=None),
    session: AsyncSession = Depends(get_session),
):
    stmt = (
        select(ProcessedNews)
        .where(ProcessedNews.channel_space_id == channel_space_id)
        .order_by(ProcessedNews.published_at.desc().nullslast(), ProcessedNews.created_at.desc())
        .limit(limit)
        .offset(offset)
    )
    if sub_channel_id is not None:
        stmt = stmt.where(ProcessedNews.sub_channel_id == sub_channel_id)
    rows = (await session.execute(stmt)).scalars().all()
    return [_processed_news_out(r) for r in rows]
```

`_processed_news_out` 转换函数需更新（当前代码 `app/routes.py:44-61`）：

```python
def _processed_news_out(r: ProcessedNews) -> ProcessedNewsOut:
    return ProcessedNewsOut.model_validate(
        {
            "id": r.id,
            "channel_space_id": r.channel_space_id,
            "raw_item_id": r.raw_item_id,
            "sub_channel_id": r.sub_channel_id,  # 新增
            "title": r.title,
            "summary": r.summary,
            "language": r.language,
            "source_refs": _as_dict(r.source_refs),
            "published_at": r.published_at,
            "bullets": r.bullets or [],
            "tags": r.tags or [],
            "entities": r.entities or [],
            "importance_score": float(r.importance_score or 0),
            "created_at": r.created_at,
        }
    )
```

---

## 6. X/Twitter 数据源实现

### 6.1 新增文件：worker/fetch_x_twitter.py

```python
import json
from datetime import datetime, timezone
from typing import Optional

import httpx

from app.settings import settings


# 硬编码的 API 请求参数，不从 config 读取
TWEET_FIELDS = "created_at,public_metrics,author_id,text,entities,referenced_tweets"
USER_FIELDS = "name,username"
EXPANSIONS = "author_id"


async def parse_x_twitter(
    config: dict,
    cursor: dict,
    max_items: int,
) -> tuple[list[dict], dict]:
    """
    主入口。返回 (items, cursor_updates)。
    items: list[dict]，每个 dict 包含 source_item_id, url, published_at, content。
    cursor_updates: dict，需要合并到 source_states.cursor 的更新。
    """
    mode = config.get("mode", "search")
    if mode == "search":
        return await _fetch_search(config, cursor, max_items)
    elif mode == "user_timeline":
        return await _fetch_user_timelines(config, cursor, max_items)
    else:
        raise RuntimeError(f"unsupported x_twitter mode: {mode}")


async def _fetch_search(
    config: dict,
    cursor: dict,
    max_items: int,
) -> tuple[list[dict], dict]:
    """关键词搜索模式。"""
    query = config.get("search_query")
    if not query:
        raise RuntimeError("x_twitter search mode requires config.search_query")

    proxy = settings.https_proxy or settings.http_proxy
    async with httpx.AsyncClient(timeout=30, proxy=proxy) as client:
        url = "https://api.twitter.com/2/tweets/search/recent"
        params = {
            "query": query,
            "max_results": min(max_items, 100),
            "tweet.fields": TWEET_FIELDS,
            "user.fields": USER_FIELDS,
            "expansions": EXPANSIONS,
        }
        since_id = cursor.get("since_id")
        if since_id:
            params["since_id"] = since_id

        r = await client.get(url, headers=_auth_headers())
        _handle_rate_limit(r)
        r.raise_for_status()
        data = r.json()

    items = _parse_tweets(data)
    # 取最新的 tweet_id 作为下次 since_id
    new_since_id = None
    if items:
        tweet_ids = [int(t["source_item_id"]) for t in items if t["source_item_id"].isdigit()]
        if tweet_ids:
            new_since_id = str(max(tweet_ids))

    cursor_updates = {}
    if new_since_id:
        cursor_updates["since_id"] = new_since_id

    return items[:max_items], cursor_updates


async def _fetch_user_timelines(
    config: dict,
    cursor: dict,
    max_items: int,
) -> tuple[list[dict], dict]:
    """账号追踪模式。逐用户拉取。"""
    usernames = config.get("usernames")
    if not usernames:
        raise RuntimeError("x_twitter user_timeline mode requires config.usernames")

    max_per_user = config.get("max_results_per_user", 20)

    proxy = settings.https_proxy or settings.http_proxy
    all_items: list[dict] = []
    user_cursors = dict(cursor.get("user_cursors", {}))
    updated_cursors = dict(user_cursors)

    async with httpx.AsyncClient(timeout=30, proxy=proxy) as client:
        # 先获取 user_id 映射
        user_ids = await _resolve_usernames(client, usernames)

        for username, user_id in user_ids.items():
            if not user_id:
                continue
            url = f"https://api.twitter.com/2/users/{user_id}/tweets"
            params = {
                "max_results": min(max_per_user, 100),
                "tweet.fields": TWEET_FIELDS,
                "exclude": "retweets,replies",
            }
            since = user_cursors.get(username)
            if since:
                params["since_id"] = since

            r = await client.get(url, headers=_auth_headers())
            _handle_rate_limit(r)
            r.raise_for_status()
            data = r.json()

            tweets = _parse_tweets(data)
            all_items.extend(tweets)

            # 更新该用户的游标
            if tweets:
                tweet_ids = [int(t["source_item_id"]) for t in tweets if t["source_item_id"].isdigit()]
                if tweet_ids:
                    updated_cursors[username] = str(max(tweet_ids))

    cursor_updates = {"user_cursors": updated_cursors}
    return all_items[:max_items], cursor_updates


async def _resolve_usernames(
    client: httpx.AsyncClient,
    usernames: list[str],
) -> dict[str, Optional[str]]:
    """通过用户名查询 user_id。"""
    result = {}
    for username in usernames:
        r = await client.get(
            f"https://api.twitter.com/2/users/by/username/{username}",
            headers=_auth_headers(),
        )
        _handle_rate_limit(r)
        if r.status_code == 200:
            data = r.json()
            result[username] = data.get("data", {}).get("id")
        else:
            result[username] = None
    return result


def _parse_tweets(api_response: dict) -> list[dict]:
    """将 X API 响应解析为标准 item 格式。"""
    tweets = api_response.get("data", [])
    includes = api_response.get("includes", {})
    users = {u["id"]: u for u in includes.get("users", [])}

    items = []
    for tweet in tweets:
        tweet_id = tweet.get("id")
        if not tweet_id:
            continue

        author_id = tweet.get("author_id")
        user = users.get(author_id, {})
        username = user.get("username", "")
        author_name = user.get("name", "")

        text = tweet.get("text", "")
        created_at_str = tweet.get("created_at")
        published_at = None
        if created_at_str:
            try:
                published_at = datetime.fromisoformat(created_at_str.replace("Z", "+00:00"))
            except Exception:
                pass

        metrics = tweet.get("public_metrics", {})
        entities_data = tweet.get("entities", {})
        referenced = tweet.get("referenced_tweets", [])

        content = {
            "tweet_id": tweet_id,
            "text": text,
            "author_username": username,
            "author_name": author_name,
            "created_at": created_at_str,
            "public_metrics": metrics,
            "entities": entities_data,
            "referenced_tweets": referenced,
        }

        url = f"https://x.com/{username}/status/{tweet_id}" if username else f"https://x.com/i/status/{tweet_id}"

        items.append({
            "source_item_id": tweet_id,
            "url": url,
            "published_at": published_at,
            "content": content,
        })

    return items


def _auth_headers() -> dict:
    return {"Authorization": f"Bearer {settings.x_bearer_token}"}


def _handle_rate_limit(response: httpx.Response):
    """429 响应抛出特定异常，触发 Worker 退避重试。"""
    if response.status_code == 429:
        raise httpx.HTTPStatusError(
            "X API rate limit exceeded",
            request=response.request,
            response=response,
        )


def render_text_for_llm(content: dict) -> str:
    """将推文内容渲染为给 LLM 的文本。"""
    if isinstance(content, str):
        try:
            content = json.loads(content)
        except Exception:
            content = {}

    username = content.get("author_username") or ""
    text = content.get("text") or ""
    metrics = content.get("public_metrics") or {}
    entities = content.get("entities") or {}

    parts = []
    if username:
        parts.append(f"[Tweet by @{username}]")
    parts.append(text)

    # 互动数据
    metric_parts = []
    for key in ["like_count", "retweet_count", "reply_count"]:
        v = metrics.get(key)
        if v is not None:
            metric_parts.append(f"{key.replace('_count', '').title()}s: {v}")
    if metric_parts:
        parts.append(" | ".join(metric_parts))

    # 引用链接
    urls = entities.get("urls", [])
    if urls:
        link_parts = [u.get("expanded_url") or u.get("url", "") for u in urls]
        parts.append("引用链接:\n" + "\n".join(f"- {u}" for u in link_parts if u))

    return "\n\n".join(parts).strip()
```

### 6.2 Worker 调度集成

在 `worker/main.py` 的 `fetch_and_ingest()` 函数中，新增 `x_twitter` 分支。

**位置**：在现有 `elif source_type == "semantic_scholar":` 之后，`else:` 之前新增：

```python
elif source_type == "x_twitter":
    items, cursor_updates = await parse_x_twitter(source_config, cursor, max_items)
```

同时修改 fetch 完成后的游标更新逻辑。当前代码 `worker/main.py:322-324` 是 HF 专用的：

```python
# 当前代码（需修改）
if source_type == "hf_daily_papers":
    if isinstance(cursor_updates, dict):
        cursor.update(cursor_updates)
```

改为通用模式：

```python
# 修改后
if cursor_updates:
    cursor.update(cursor_updates)
```

仅此一行改动：去掉 `source_type == "hf_daily_papers"` 条件，改为无条件检查 `cursor_updates`。

```python
# 游标更新（通用化）
if cursor_updates:
    cursor.update(cursor_updates)

await conn.execute(
    """
    insert into source_states(channel_source_id, cursor, consecutive_failures, last_success_at, last_error, updated_at)
    values($1, $2::jsonb, 0, now(), null, now())
    on conflict (channel_source_id)
    do update set
      cursor=excluded.cursor,
      consecutive_failures=0,
      last_success_at=now(),
      last_error=null,
      updated_at=now()
    """,
    channel_source_id,
    json.dumps(cursor),
)
```

注意：`parse_x_twitter` 返回 `tuple[list[dict], dict]`，而其他 fetcher 只返回 `list[dict]`。需要统一返回格式。建议：

- 所有 `parse_*` 函数统一返回 `tuple[list[dict], Optional[dict]]`
- 第二个元素为 `cursor_updates`，为 None 时表示无游标更新
- 现有 fetcher 的返回值包装为 `(items, None)`

### 6.3 依赖安装

`requirements.txt` 无需新增依赖。X API 使用现有的 `httpx`。

---

## 7. LLM 处理增强

### 7.1 Prompt 替换

`worker/llm.py` 中 `translate_and_summarize` 函数的 prompt 替换为：

```python
prompt = {
    "role": "user",
    "content": [
        {
            "type": "text",
            "text": (
                "把下面内容处理成中文新闻卡片，做翻译与深度摘要。\n"
                "要求：\n"
                "1) 如果原文是英文，翻译成中文\n"
                "2) 输出中文标题(title)和中文摘要(summary)\n"
                "3) summary 80-200 字\n"
                "4) 提炼 3-5 个核心要点(bullets)，每个不超过 50 字\n"
                "5) 生成 3-5 个分类标签(tags)\n"
                "6) 识别关键实体(entities)，每个包含 name 和 type(person/org/product/tech)\n"
                "7) 评估重要性评分(importance_score)，0-10 浮点数，考虑时效性、影响力、相关性\n"
                "8) 只输出严格 JSON，不要输出多余文字\n"
                'JSON 格式：{"title":"...","summary":"...","bullets":["..."],"tags":["..."],'
                '"entities":[{"name":"...","type":"..."}],"importance_score":7.5,"language":"zh"}\n'
                f"source_url: {source_url or ''}\n\n"
                f"content:\n{text}"
            ),
        }
    ],
}
```

### 7.2 输出校验函数

在 `worker/llm.py` 中新增：

```python
def validate_llm_output(result: dict) -> dict:
    """校验并修正 LLM 输出，确保字段完整且类型正确。"""
    # title
    title = result.get("title")
    if not isinstance(title, str) or not title.strip():
        result["title"] = "无标题"

    # summary
    summary = result.get("summary")
    if not isinstance(summary, str) or not summary.strip():
        result["summary"] = result.get("title", "")

    # bullets
    bullets = result.get("bullets")
    if not isinstance(bullets, list):
        bullets = []
    result["bullets"] = [str(b) for b in bullets if b][:5]

    # tags
    tags = result.get("tags")
    if not isinstance(tags, list):
        tags = []
    result["tags"] = [str(t) for t in tags if t][:5]

    # entities
    entities = result.get("entities")
    if not isinstance(entities, list):
        entities = []
    result["entities"] = [
        e for e in entities
        if isinstance(e, dict) and e.get("name")
    ]

    # importance_score
    score = result.get("importance_score")
    try:
        score = float(score)
    except (TypeError, ValueError):
        score = 0.0
    result["importance_score"] = max(0.0, min(10.0, score))

    # language
    result["language"] = "zh"

    return result
```

### 7.3 调用校验

在 `translate_and_summarize` 函数中，JSON 解析后、返回前，调用校验：

```python
# 现有代码：JSON 解析
try:
    obj = json.loads(content)
except Exception:
    obj = _extract_first_json_object(content)
    if obj is None:
        raise RuntimeError(f"LLM returned unparseable content: {content[:200]!r}")

# 新增：校验并修正
obj = validate_llm_output(obj)

return obj
```

---

## 8. process_one 变更

`worker/main.py` 中 `process_one` 函数需做以下修改：

### 8.0 import 新增

在 `worker/main.py` 顶部新增 import：

```python
from worker.fetch_x_twitter import parse_x_twitter, render_text_for_llm as xt_text
```

### 8.1 source_type switch 新增 x_twitter 分支

当前代码（worker/main.py:370-379）的 source_type switch 中新增 x_twitter 分支：

```python
# 在 elif row["source_type"] == "semantic_scholar": 之后，else: 之前新增：
elif row["source_type"] == "x_twitter":
    text = xt_text(content)
```

如果不加此分支，x_twitter 类型的 raw_item 会走到 `else: text = hf_text(content)`，用 HF 的渲染函数处理推文内容，输出完全不对。

### 8.2 获取 sub_channel_id

在现有 `raw_items JOIN sources` 查询之后，新增一次查询：

```python
sub_channel_id = await conn.fetchval(
    """
    SELECT sub_channel_id FROM channel_sources
    WHERE source_id = $1 AND channel_space_id = $2
    LIMIT 1
    """,
    row["source_id"],
    row["channel_space_id"],
)
```

### 8.3 子频道内去重检查

在 INSERT `processed_news` 之前，执行去重检查：

```python
if sub_channel_id and row["source_item_url"]:
    existing = await conn.fetchval(
        """
        SELECT 1 FROM processed_news pn
        JOIN raw_items ri ON ri.id = pn.raw_item_id
        WHERE pn.channel_space_id = $1
          AND pn.sub_channel_id = $2
          AND ri.source_item_url = $3
        LIMIT 1
        """,
        row["channel_space_id"],
        sub_channel_id,
        row["source_item_url"],
    )
    if existing:
        return  # 跳过，该子频道内已存在相同 URL 的新闻
```

### 8.4 INSERT 语句更新

将现有的硬编码空值替换为 LLM 实际输出：

```python
result = await translate_and_summarize(text=text, source_url=row["source_item_url"])

inserted = await conn.fetchrow(
    """
    insert into processed_news(
      channel_space_id, raw_item_id, title, summary, language, source_refs, published_at,
      bullets, tags, entities, importance_score, sub_channel_id, created_at
    )
    values($1, $2, $3, $4, $5, $6::jsonb, $7, $8::jsonb, $9::jsonb, $10::jsonb, $11, $12, now())
    on conflict (raw_item_id) do nothing
    returning id, channel_space_id, title, published_at
    """,
    row["channel_space_id"],
    raw_item_id,
    result.get("title") or "",
    result.get("summary") or "",
    result.get("language") or "zh",
    json.dumps({"url": row["source_item_url"], "source_id": str(row["source_id"])}),
    row["published_at"],
    json.dumps(result.get("bullets") or []),
    json.dumps(result.get("tags") or []),
    json.dumps(result.get("entities") or []),
    float(result.get("importance_score") or 0),
    sub_channel_id,  # 新增
)
```

---

## 9. Settings 变更

### 9.1 `app/settings.py` 新增

```python
x_bearer_token: Optional[str] = None
```

### 9.2 `.env.example` 新增

```
# X/Twitter API (optional)
X_BEARER_TOKEN=
```

---

## 10. 文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `app/models.py` | 新增 SubChannel 类；ChannelSource、ProcessedNews 新增 sub_channel_id |
| 修改 | `app/schemas.py` | 新增 SubChannelCreate/Update/Out；修改 ChannelSourceBind/Out/UpdatePolicy、ProcessedNewsOut |
| 修改 | `app/routes.py` | 新增 4 个子频道路由；修改绑定/更新/新闻列表路由；更新 `_channel_source_out` 和 `_processed_news_out` 转换函数 |
| 修改 | `app/settings.py` | 新增 x_bearer_token |
| 修改 | `worker/main.py` | fetch_and_ingest 新增 x_twitter 分支 + 游标通用化；process_one 新增 sub_channel_id 获取 + 去重 + INSERT 更新 |
| 修改 | `worker/llm.py` | prompt 替换；新增 validate_llm_output 函数 |
| 新增 | `worker/fetch_x_twitter.py` | X/Twitter 抓取器完整实现 |
| 修改 | `db/schema.sql` | 合并 sub_channels 表和新增字段/索引 |
| 新增 | `db/migrations/v0.1.sql` | 增量迁移脚本 |
| 修改 | `.env.example` | 新增 X_BEARER_TOKEN |
| 修改 | `requirements.txt` | 无变更 |

### 10.1 Import 变更汇总

**`app/routes.py`** — 新增 import：
```python
from app.models import Alert, ChannelSpace, ChannelSource, ProcessedNews, Source, SubChannel
from app.schemas import (
    AlertOut,
    ChannelSpaceCreate,
    ChannelSpaceOut,
    ChannelSourceBind,
    ChannelSourceOut,
    ChannelSourceUpdatePolicy,
    ChannelSourceWithSource,
    ProcessedNewsOut,
    SourceCreate,
    SourceOut,
    SubChannelCreate,
    SubChannelUpdate,
    SubChannelOut,
)
```

**`worker/main.py`** — 新增 import（一行搞定，同时用于 fetch_and_ingest 调度和 process_one 的文本渲染）：
```python
from worker.fetch_x_twitter import parse_x_twitter, render_text_for_llm as xt_text
```

---

## 11. 验证方案

### 11.1 数据库迁移验证

```bash
# 在 Supabase SQL Editor 中执行 db/migrations/v0.1.sql
# 验证：
SELECT * FROM sub_channels LIMIT 1;         -- 应返回空集，不报错
SELECT sub_channel_id FROM channel_sources LIMIT 1;  -- 应有新列，值为 NULL
SELECT sub_channel_id FROM processed_news LIMIT 1;   -- 应有新列，值为 NULL
```

### 11.2 子频道 API 验证

```bash
# 创建频道空间（如果不存在）
curl -X POST http://localhost:8000/v1/channel-spaces \
  -H "Content-Type: application/json" \
  -d '{"name":"AI","description":"AI news"}'

# 创建子频道
curl -X POST http://localhost:8000/v1/channel-spaces/{space_id}/sub-channels \
  -H "Content-Type: application/json" \
  -d '{"name":"AI论文","sort_order":1}'

curl -X POST http://localhost:8000/v1/channel-spaces/{space_id}/sub-channels \
  -H "Content-Type: application/json" \
  -d '{"name":"AI应用","sort_order":2}'

# 列出子频道
curl http://localhost:8000/v1/channel-spaces/{space_id}/sub-channels

# 创建 Source 并绑定到子频道
curl -X POST http://localhost:8000/v1/sources \
  -H "Content-Type: application/json" \
  -d '{"type":"rss","name":"arxiv-cs-AI","config":{"feed_url":"https://rss.arxiv.org/rss/cs.AI"}}'

curl -X POST http://localhost:8000/v1/channel-spaces/{space_id}/sources \
  -H "Content-Type: application/json" \
  -d '{"source_id":"{source_id}","enabled":true,"sub_channel_id":"{sub_channel_id}","fetch_policy":{"schedule":{"every_seconds":600},"budget":{"max_items_per_run":5}}}'

# 按子频道查询新闻
curl "http://localhost:8000/v1/channel-spaces/{space_id}/news?sub_channel_id={sub_channel_id}&limit=10"
```

### 11.3 X/Twitter 验证

```bash
# 创建搜索模式 Source
curl -X POST http://localhost:8000/v1/sources \
  -H "Content-Type: application/json" \
  -d '{"type":"x_twitter","name":"twitter-ai-search","config":{"mode":"search","search_query":"LLM OR GPT OR Claude OR deepseek","max_results_per_query":10}}'

# 创建账号追踪模式 Source
curl -X POST http://localhost:8000/v1/sources \
  -H "Content-Type: application/json" \
  -d '{"type":"x_twitter","name":"twitter-openai","config":{"mode":"user_timeline","usernames":["OpenAI","AnthropicAI"],"max_results_per_user":10}}'

# 绑定到空间
curl -X POST http://localhost:8000/v1/channel-spaces/{space_id}/sources \
  -H "Content-Type: application/json" \
  -d '{"source_id":"{source_id}","enabled":true,"fetch_policy":{"schedule":{"every_seconds":1800},"budget":{"max_items_per_run":20}}}'

# 启动 Worker，观察日志确认抓取成功
# 确认 raw_items 和 processed_news 有新增数据
```

### 11.4 LLM 增强输出验证

```bash
# 等 Worker 处理几条新闻后，检查输出
curl http://localhost:8000/v1/channel-spaces/{space_id}/news?limit=5

# 验证每条新闻的 bullets、tags、entities、importance_score 字段不为空
# 验证 importance_score 在 0-10 范围内
```

### 11.5 去重验证

```bash
# 将同一个 RSS Source 绑定到同一空间的两个子频道
# 等待 Worker 抓取并处理
# 验证两个子频道下各有一条相同 URL 的新闻（跨子频道不去重）
# 验证同一子频道内不会出现重复 URL
```

### 11.6 向后兼容验证

```bash
# 不带 sub_channel_id 创建绑定（旧行为）
curl -X POST http://localhost:8000/v1/channel-spaces/{space_id}/sources \
  -H "Content-Type: application/json" \
  -d '{"source_id":"{source_id}","enabled":true,"fetch_policy":{"schedule":{"every_seconds":600}}}'

# 查询新闻不带 sub_channel_id 参数（应返回所有新闻）
curl "http://localhost:8000/v1/channel-spaces/{space_id}/news?limit=10"
```
